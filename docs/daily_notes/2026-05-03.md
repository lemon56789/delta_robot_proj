# Daily Note - 2026-05-03

## Entry 001
- 무엇을:
  - `docs/plans/2026-05-03_001_apply_nominal_delta_geometry_parameters.md` 계획서를 작성했다.
  - `kinematics/geometry.py`에 nominal delta geometry parameter dataclass와 기본값을 추가했다.
  - `kinematics/inverse_kinematics.py`에 `E/F/G + 2atan(t)` 기반 inverse kinematics 최소 동작 구현을 추가했다.
  - `kinematics/README.md`에 현재 기준 파라미터와 모듈 구성을 반영했다.
- 왜:
  - 모델링 기준으로 확보한 기하 파라미터를 코드 기준값으로 고정해야 이후 IK, FK, 제어, 시뮬레이션 경로가 같은 nominal geometry를 참조할 수 있기 때문이다.
  - 현재 리포에는 IK 문서만 있고 실제 계산 가능한 구현이 없어서 다음 단계 검증을 진행할 수 없었기 때문이다.
- 어떻게:
  - `DeltaGeometry` dataclass에 `L`, `l`, `wB`, `uP`를 `mm` 단위로 정의하고 nominal 상수로 고정했다.
  - arm outward vector와 일반화된 `E_i`, `F_i`, `G_i` 식을 사용해 arm별 해를 계산했다.
  - 반각 치환으로 복원한 algebraic 각도와 프로젝트 `+theta = downward` 정의 사이의 부호 차이를 코드에서 보정했다.
  - 판별식 음수 시 `reject`, 각도 범위는 현재 문서 기준인 `0 deg <= theta_i <= 90 deg`를 적용했다.
  - 후보 해가 둘이면 기본적으로 작은 각도를 선택하고, `previous_theta_deg`가 있으면 가장 가까운 해를 선택하도록 했다.
- 결과:
  - `kinematics/` 아래에 실제 호출 가능한 nominal geometry 및 IK Python 구현이 추가되었다.
  - 현재 프로젝트 문서의 단위와 `theta_i` 정의를 따르는 최소 계산 경로가 생겼다.
  - 파라미터는 nominal 기준이므로 향후 하드웨어 식별값으로 교체하거나 보정할 수 있다.
- 변경 파일:
  - `docs/plans/2026-05-03_001_apply_nominal_delta_geometry_parameters.md`
  - `kinematics/README.md`
  - `kinematics/geometry.py`
  - `kinematics/inverse_kinematics.py`
  - `docs/daily_notes/2026-05-03.md`
- 다음:
  - sample point 세트를 정해 reachable workspace와 branch 선택이 의도대로 동작하는지 검증한다.
  - FK 구현을 추가해 IK 결과를 역검증한다.
  - 하드웨어 연동 전 실제 조립 치수와 nominal parameter 차이를 정리한다.

## Entry 002
- 무엇을:
  - `docs/plans/2026-05-03_002_update_roadmap_and_fulltext_after_ik_impl.md` 계획서를 작성했다.
  - `docs/roadmap.md`와 `fulltext.md`를 현재 구현 상태에 맞게 갱신했다.
- 왜:
  - 기존 문서 상태 요약이 2026-04-20 기준에 머물러 있어, nominal geometry 반영과 IK 코드 구현 완료 사실이 문서에 반영되지 않았기 때문이다.
- 어떻게:
  - Stage 진행 상태를 실제 리포지토리 구현 기준으로 다시 정리했다.
  - `kinematics/geometry.py`, `kinematics/inverse_kinematics.py` 추가 사실과 nominal geometry parameter를 문서에 반영했다.
  - 다음 작업 우선순위를 FK 검증과 workspace/sample 검증 중심으로 갱신했다.
- 결과:
  - `docs/roadmap.md`와 `fulltext.md`가 현재 코드 기준 프로젝트 위치를 반영하도록 갱신되었다.
  - 문서상 `Stage 2 코드 미구현` 상태가 해소되고, 다음 구현 초점이 `FK 검증`으로 명확해졌다.
- 변경 파일:
  - `docs/plans/2026-05-03_002_update_roadmap_and_fulltext_after_ik_impl.md`
  - `docs/roadmap.md`
  - `fulltext.md`
  - `docs/daily_notes/2026-05-03.md`
- 다음:
  - FK 최소 구현 계획서를 작성한다.
  - IK→FK 왕복 검증과 workspace sweep 검증 범위를 정한다.

## Entry 003
- 무엇을:
  - `docs/plans/2026-05-03_003_numeric_forward_kinematics_minimum_impl.md` 계획서를 작성했다.
  - `kinematics/forward_kinematics.py`에 수치해석 기반 FK 최소 구현을 추가했다.
  - `kinematics/validate_roundtrip.py`에 sample point 기준 `IK -> FK` 왕복 검증 스크립트를 추가했다.
  - `kinematics/README.md`에 FK 모듈과 검증 스크립트 설명을 반영했다.
- 왜:
  - 현재 상태에서는 IK가 계산되더라도 결과가 자기 일관적인지 확인할 FK와 왕복 검증 경로가 없었기 때문이다.
  - fake pipeline과 workspace 검증에 들어가기 전에 운동학 신뢰성 확인 경로를 먼저 확보해야 했기 때문이다.
- 어떻게:
  - 현재 IK 구현과 같은 algebraic constraint를 따르도록 각 arm별 constraint sphere center를 구성했다.
  - sphere intersection 구조를 이용해 초기값을 잡고, Newton 방식과 damping으로 `(x, y, z)`를 반복 계산하도록 구현했다.
  - 실패 시 Jacobian singularity, damping 실패, 수렴 실패를 명시적으로 보고하도록 했다.
  - 별도 검증 스크립트에서 sample position에 대해 `position -> IK -> FK` 왕복 오차를 출력하도록 구성했다.
- 결과:
  - `kinematics/` 아래에 FK 최소 구현과 왕복 검증 스크립트가 추가되었다.
  - 현재 IK와 같은 수식 기준에서 위치 복원 검증 경로를 실행할 수 있게 되었다.
  - 다음 단계로 workspace sweep과 FK 정확도 점검을 이어갈 수 있는 기반이 생겼다.
- 변경 파일:
  - `docs/plans/2026-05-03_003_numeric_forward_kinematics_minimum_impl.md`
  - `kinematics/forward_kinematics.py`
  - `kinematics/validate_roundtrip.py`
  - `kinematics/README.md`
  - `docs/daily_notes/2026-05-03.md`
- 다음:
  - 왕복 검증 결과를 보고 FK residual tolerance와 수렴 전략을 다듬는다.
  - workspace sweep 검증 계획서를 작성한다.
  - 필요하면 FK constraint sign convention과 문서 정의의 정합성을 별도로 점검한다.
