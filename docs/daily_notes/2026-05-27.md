# Daily Note - 2026-05-27

## Entry 001
- 무엇을:
  - `simulation/` 아래에 `simulink/` 하위 폴더를 추가하고, 해당 위치를 설명하는 `README.md`를 만들었다.
  - `simulation/README.md`에 `Simulink`를 포함하도록 폴더 설명을 갱신했다.
- 왜:
  - Simulink 원본 파일을 이 저장소에 보관하되, 현재 `.slx`와 보조 파일의 상대경로 의존성을 유지할 수 있는 별도 위치가 필요했다.
  - 실행 환경은 별도이지만 GitHub 상에서 Simulink 자산을 일관되게 정리할 기준점이 필요했다.
- 어떻게:
  - `simulation/simulink/README.md`를 추가해 Git이 추적하는 하위 폴더를 만들었다.
  - README에 원본 파일 포함 범위와 `slprj` 등 제외 대상 생성물을 명시했다.
- 결과:
  - 이후 Simulink 관련 원본 파일을 `simulation/simulink/` 아래에 모아 둘 수 있는 최소 구조가 준비되었다.
  - 현재 데이터 계약, 인터페이스, 제어 흐름 변경 없이 저장소 구조만 보강되었다.
- 다음:
  - 외부 MATLAB 작업 폴더에서 가져올 실제 Simulink 원본 파일과 필수 의존 파일을 선별한다.
  - `slprj`와 기타 생성물은 제외하고, 상대경로 의존성이 있는 파일들은 필요한 단위로 함께 배치한다.

## Entry 002
- 무엇을:
  - `simulation/simulink/model_notes.md`를 추가해 현재 최신 기준 모델과 주요 `m` 파일 역할을 정리했다.
  - `simulation/simulink/.gitignore`를 추가하고, `*.slxc`와 `*.avi` 생성물을 제거 대상으로 정리했다.
- 왜:
  - 현재 어떤 `slx`가 기준본인지와 각 스크립트의 역할을 저장소 안에서 바로 확인할 필요가 있었다.
  - Simulink 실행 과정에서 생기는 생성물을 계속 커밋하지 않도록 관리 기준이 필요했다.
- 어떻게:
  - `simscape_delta_robot_trajectory.slx`를 현재 최신 기준 모델로 문서화하고, `axisfix` 모델은 비교용 보조본으로 구분했다.
  - `init_delta_params.m`, `load_fake_pipeline_theta.m`, `run_fake_pipeline_simscape.m`와 나머지 보조 스크립트의 역할을 정리했다.
  - `slprj/`, `*.slxc`, `*.avi`, autosave 파일을 제외 대상으로 지정했다.
- 결과:
  - Simulink 폴더에서 최신 모델과 주요 보조 파일의 역할을 빠르게 파악할 수 있게 되었다.
  - 버전관리 불필요 생성물 재유입을 막을 기본 규칙이 추가되었다.
- 다음:
  - 실제 실행에 꼭 필요한 추가 의존 파일이 있는지 MATLAB 환경에서 한 번 더 확인한다.
  - 필요하면 `model_notes.md`에 각 보조 `slx`의 사용 시점과 입출력 차이를 더 구체화한다.
