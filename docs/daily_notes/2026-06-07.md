# Daily Note - 2026-06-07

## Entry 001
- 무엇을: Run 02 문서의 실제 실행 순서를 정정하고 circle 출발 시점
  정렬 정책과 이후 분석 workflow를 추가했다.
- 왜: 실제 데이터는 trajectory별로 r01-r03을 수집했으며, circle의
  시작/end hold와 비균일 scheduled timestamp를 분석에서 일관되게
  처리할 기준이 필요했기 때문이다.
- 어떻게: 실제 순서를 trajectory별 `r01 OFF -> ON`부터 `r03 OFF -> ON`
  구조로 기록했다. 실제 실험일은 2026-06-07이지만 기존 raw/calibration
  ID의 2026-06-06 표기를 의도된 식별자로 유지한다고 명시했다. circle은
  시작 hold departure를 main/vision 공통 `t=0` anchor로 사용하고 실제
  timestamp 간격을 보존하도록 정의했다. raw validation, alignment,
  processed 생성, run metric, 12개 paired 비교, repetition/trajectory
  집계와 output 저장 절차를 추가했다.
- 결과: Run 02 문서가 실제 수행 순서와 일치하며, raw 24개 run을
  OFF/ON 성능 비교 결과로 연결하는 분석 기준이 문서화되었다. raw CSV,
  calibration, 코드와 데이터 contract는 변경하지 않았다.
- 변경 파일: `docs/plans/2026-06-07_001_run02_execution_order_and_analysis_workflow.md`,
  `docs/hardware_experiment_run_02_corrected_comparison.md`,
  `docs/daily_notes/2026-06-07.md`
- 다음: 문서화된 workflow에 따라 Run 02 전처리/정렬 구현 계획을
  별도로 승인받고, 12개 OFF/ON pair comparison report를 생성한다.

## Entry 002
- 무엇을: Run 02 24개 run의 vision-main 정렬, Simscape 병합,
  measured-position 생성과 12개 OFF/ON tracking 비교를 구현하고
  실행했다.
- 왜: raw 검증을 통과한 데이터를 fixed 16-column processed dataset으로
  변환하고 correction ON/OFF의 실제 XY 성능을 같은 trajectory와
  repetition에서 정량 비교하기 위해서다.
- 어떻게: `experiments/run02_preprocess.py`를 추가했다. Step trajectory는
  첫 vision position jump를 첫 main phase 전환에 맞추고 후속 event
  residual을 검증했다. Circle은 start hold persistent departure를 첫
  `circle_ccw_*` 시작에 맞추고 end hold를 검증했다. `500 ms`를 넘는
  vision sample gap은 보간하지 않았다. 같은 pair의 공통 timestamp에서
  tracking RMSE/MAE/max error를 계산하고 trajectory와 전체를 동일
  가중했다.
- 결과: measured-position/merged/alignment 파일을 각각 24개 생성했고
  comparison report 1개를 생성했다. merged coverage는 `94.6~100%`,
  step 최대 anchor residual은 `232.522 ms`, circle end hold는
  `2.127~2.684 s`였다. `tracking_xy_rmse`는 reverse grid `+11.05%`,
  diamond `+13.67%` 개선됐지만 cross `-7.73%`, circle `-25.41%`
  악화됐다. 전체 trajectory 동일가중 결과는 OFF `6.3582 mm`, ON
  `6.4222 mm`로 `2.10%` 악화되어 전체 개선으로 판단하지 않았다.
- 변경 파일: `docs/plans/2026-06-07_002_run02_preprocess_and_comparison.md`,
  `experiments/run02_preprocess.py`,
  `experiments/test_run02_preprocess.py`, `experiments/README.md`,
  `data/real/derived/measured_position_2026-06-06_run02_*.csv`,
  `data/processed/merged_2026-06-06_run02_*.csv`,
  `data/processed/alignment_2026-06-06_run02_*.json`,
  `experiments/results/run02_comparison_2026-06-07.json`,
  `docs/hardware_experiment_run_02_corrected_comparison.md`,
  `docs/daily_notes/2026-06-07.md`
- 다음: cross와 circle의 phase별 tracking error와 predicted correction
  방향을 분석해 trajectory별 gain 또는 correction 적용 조건을
  재검토한다.

## Entry 003
- 무엇을: Run 02에서 correction ON의 전체 `tracking_xy_rmse`가 악화된
  원인을 추가 진단하고 현재 확인된 문제점과 해석 제한을 기록했다.
- 왜: 기존 comparison 결과만으로 가상센서 모델 자체가 실패했다고
  결론내리기 전에 correction sign, 실제 actuator command 반영 여부,
  run 간 반복 정밀도와 circle 시간 정렬 영향을 분리해야 하기 때문이다.
- 어떻게: ON correction log의 predicted/applied correction과 OFF/ON
  vision tracking error 방향을 비교했다. Arduino firmware의
  `thetaToServoAngle()`과 serial RX의 integer `servo cmd`를 확인했다.
  각 OFF/ON pair의 초기 home vision 평균 위치를 비교하고, circle
  원운동 구간에 추가 time shift를 적용해 RMSE 민감도를 계산했다.
- 결과:
  - 모델 predicted error 방향은 OFF 실제 tracking error와
    `95.6~100%` 같은 방향이었다. correction은 그 반대 방향으로
    적용되므로 correction sign 자체는 정상으로 판단했다.
  - Arduino는 fractional theta를 수신하지만 최종적으로
    `servo.write()`에 전달하기 전에 integer servo angle로 반올림한다.
    ON command event 중 nominal OFF와 실제 integer servo command가
    달라진 비율은 cross `11.1%`, reverse grid `45.5%`, diamond
    `27.1%`, circle `33.0%`였다. 특히 cross에서는 보정 명령 대부분이
    실제 actuator command 변화로 이어지지 않았다.
  - correction이 없는 동일 home servo command에서도 OFF/ON 초기
    vision 위치 차이가 발생했다. pair별 최대 home shift는 cross
    `4.34 mm`, reverse grid `2.80 mm`, diamond `4.39 mm`, circle
    `1.85 mm`였다. 현재 correction 크기보다 백래시, zero drift 또는
    기구 반복 정밀도 영향이 클 수 있다.
  - 기존 circle 단일 departure alignment에서는 ON
    `tracking_xy_rmse`가 `25.41%` 악화됐다. 원운동 구간만 별도로
    shift sweep한 결과 OFF best RMSE는 `4.85 mm`, ON best RMSE는
    `4.22 mm`였고 ON에는 약 `350 ms` 추가 phase shift가 필요했다.
    이 조건에서는 ON이 약 `12.9%` 개선되므로 기존 circle 공식 결과는
    동적 위상 정렬에 민감하다.
  - Circle ON correction은 원운동 기준 평균 tangential correction
    약 `0.341 mm`를 포함했고, 측정 tangential error 평균은 OFF
    `0.048 mm`에서 ON `3.037 mm`로 증가했다. 모델이 정적 위치 오차와
    동적 lag를 구분하지 못하는 가능성이 있다.
  - 초기 home offset을 각 run에서 제거해 비교하면 diamond는 세
    repetition 모두 `10.61~20.55%` 개선됐지만 cross는 두 repetition이
    악화됐고 reverse grid는 `-5.96~+2.57%` 범위로 개선 근거가
    약해졌다.
  - 따라서 현재 결과를 모델 전체 실패 또는 전체 개선으로 확정하지
    않는다. Diamond 개선은 상대적으로 일관되지만 cross는 actuator
    resolution과 반복 정밀도, reverse grid는 baseline drift, circle은
    phase alignment와 동적 특성의 영향을 우선 해결해야 한다.
- 변경 파일: `docs/plans/2026-06-07_003_record_run02_diagnostic_findings.md`,
  `docs/daily_notes/2026-06-07.md`
- 다음: circle angular/phase alignment를 정식 구현하고, 실제 integer
  servo command 기준 correction 반영률과 absolute/baseline-normalized
  metric을 함께 산출한다. 이후 hardware 재실험 전 servo resolution,
  backlash와 zero repeatability를 별도 gate로 측정한다.

## Entry 004
- 무엇을: 추가 하드웨어 수집 없이 기존 Run 02 24개 run에서 home
  normalization, 실제 integer servo command 반영률, step transition/stable
  구간과 circle radial/tangential/phase 지표를 정식 재분석했다.
- 왜: 제한된 실험 시간 안에서 correction model 효과와 초기 zero drift,
  actuator 해상도 및 circle 동적 lag 영향을 분리하고, 재실험 우선순위를
  결정하기 위해서다.
- 어떻게: 각 run의 초기 `home_1`에서 `vision - target` 중앙값을 구해
  absolute와 home-normalized metric을 함께 계산했다. main CSV의 실제
  전송 theta를 Arduino firmware와 같은 mapping으로 integer servo
  command로 변환했다. Step은 phase 시작 후 첫 `1 s`와 stable 구간을
  나눴다. Circle은 fitted center/radius, target-frame radial/tangential
  error, angular phase lag와 `-1000~1000 ms` shift sweep을 계산했다.
- 결과:
  - 기존 12 pair absolute RMSE는 공식 comparison report와 차이 `0`으로
    재현됐다.
  - home-normalized RMSE는 cross `-22.75%`, reverse grid `+3.42%`,
    diamond `+14.96%`, circle `-34.02%`였다. Diamond만 absolute와
    normalized에서 모두 세 repetition 개선됐다.
  - 실제 integer servo command 변화율은 cross `11.1%`, reverse grid
    `45.5%`, diamond `27.1%`, circle `33.0%`였다. Fractional corrected
    theta 기준 이론 변화율은 각각 `33.3%`, `63.6%`, `57.1%`,
    `57.0%`로 실제 전송/정수화 단계에서 보정이 추가 소실됐다.
  - Diamond는 transition/stable의 absolute 및 normalized 네 조건에서
    모두 개선됐다. Cross stable과 normalized 구간은 악화됐고, reverse
    grid는 normalized transition이 악화돼 baseline drift 영향이 남았다.
  - Circle은 radial RMSE가 `3.319 -> 2.843 mm`로 감소했지만 tangential
    RMSE는 `3.593 -> 4.442 mm`, 평균 tangential error는
    `0.048 -> 3.037 mm`로 증가했다. Equivalent median phase lag는 OFF
    `2.2 ms`, ON `361.7 ms`였고 ON 세 repetition의 best shift는 모두
    `350 ms`였다.
  - Circle best-shift RMSE는 OFF `4.791 mm`, ON `4.220 mm`였지만 실제
    동적 lag를 제거한 diagnostic이므로 공식 time-domain metric을
    대체하지 않는다.
- 변경 파일:
  `docs/plans/2026-06-07_004_run02_existing_data_reanalysis.md`,
  `experiments/run02_reanalyze.py`,
  `experiments/test_run02_reanalyze.py`,
  `experiments/results/run02_reanalysis_2026-06-07.json`,
  `docs/hardware_experiment_run_02_corrected_comparison.md`,
  `docs/daily_notes/2026-06-07.md`
- 다음: 하드웨어 시간이 허용되면 동일 방향 접근을 사용한 home
  repeatability 5~10회 측정을 우선하고, 추가 OFF/ON 확인은 현재 가장
  일관된 Diamond 1 pair로 제한한다.

## Entry 005
- 무엇을: `+40 mm`와 `+60 mm` X preload 후 home 반복성을 비교할
  6개 run ID를 확정하고 Computer 2 Codex용 독립 실행 지시서를
  추가했다.
- 왜: +X 이동에서 부하와 진동이 더 크지만 `(+60, 0)`에서 home으로
  복귀할 때 원점에 가까워진다는 관찰을 재현 가능한 최소 실험으로
  확인하고, 이후 Diamond OFF/ON 실험의 공통 접근 방향을 정하기
  위해서다.
- 어떻게: `2026-06-07_home_repeat_xp40_r01-r03`과
  `2026-06-07_home_repeat_xp60_r01-r03`을 확정했다. `xp40`을 먼저
  3회 수행하고 안전 확인 후 `xp60`을 수행하도록 순서를 고정했다.
  Direct theta command, preload hold `2 s`, home stabilization `5 s`,
  Arduino Serial Monitor 설정, reset 주의사항, vision READY/END
  handshake, CSV 필드, 중단 조건과 회수 파일을 한 문서에 기록했다.
- 결과: Computer 2 Codex가 추가 코드나 Git 없이 Serial Monitor와
  Windows vision 담당자 간 동일 run ID로 6회 실험을 수행할 수 있는
  실행 기준이 생겼다. Serial Monitor를 다시 열면 Arduino가 reset되어
  HOME으로 초기화되므로 6개 run 동안 포트를 유지하는 규칙도
  명시했다.
- 변경 파일:
  `docs/plans/2026-06-07_005_home_repeatability_computer2_handoff.md`,
  `experiments/home_repeatability_computer2_codex_handoff.md`,
  `experiments/README.md`, `docs/daily_notes/2026-06-07.md`
- 다음: Computer 2와 Windows vision에서 확정된 6개 run을 수집한 뒤
  마지막 home stabilization 구간의 XY 중앙값, 반복 간 최대 거리와
  진동 메모를 비교해 `xp40` 또는 `xp60` preload를 선택한다.

## Entry 006
- 무엇을: Home repeatability 6개 run을 Windows vision 컴퓨터에서
  기록하기 위한 Vision Codex용 독립 인수인계 문서를 추가했다.
- 왜: Computer 2 명령 실행과 vision raw CSV가 동일 run ID로 정확히
  pairing되고, preload 이동 전 초기 home부터 최종 home stabilization
  `5 s`까지 누락 없이 기록되어야 `xp40/xp60` 반복성을 비교할 수 있기
  때문이다.
- 어떻게: Entry 005에서 확정한
  `2026-06-07_home_repeat_xp40_r01-r03`과
  `2026-06-07_home_repeat_xp60_r01-r03`을 vision 문서에도 고정했다.
  기존 logger와 calibration을 사용하도록 하고 `VISION READY`/`RUN END`
  handshake, `vision_<run_id>.csv` 파일명, 필수 7개 컬럼, timestamp와
  frame ID 단조 증가, marker loss, post-run check, 메모와 반환 파일을
  정의했다.
- 결과: Vision Codex가 Computer 2 문서나 원래 리포 코드에 의존하지
  않고 6개 raw CSV를 같은 계약으로 수집할 수 있다. 실제 home shift를
  제거하지 않도록 run별 원점 재설정, homography 재계산, invalid frame
  보간과 placeholder CSV 생성을 금지했다.
- 변경 파일:
  `docs/plans/2026-06-07_006_home_repeatability_vision_handoff.md`,
  `experiments/home_repeatability_vision_windows_codex_handoff.md`,
  `experiments/README.md`, `docs/daily_notes/2026-06-07.md`
- 다음: Computer 2와 vision 담당자가 두 인수인계 문서의 동일 run ID와
  handshake를 확인한 뒤 xp40 세 run부터 수집한다.

## Entry 007
- 무엇을: 외부 AI용 프로젝트 요약인 `fulltext.md`를 Run 01-main 이전
  상태에서 2026-06-07 Run 02 재분석 및 최소 home 반복성 실험 준비
  상태까지 갱신했다.
- 왜: 기존 fulltext에는 Run 01-main 준비 중, Ridge 미착수, Run 02
  미착수로 남아 있어 현재 코드, 데이터, 모델, 실험 결과와 다음 작업을
  잘못 전달할 위험이 있었기 때문이다.
- 어떻게: Run 01-main 15개와 `5,158` row, `alpha=100` Ridge 모델,
  complete holdout `37.174%` 개선 결과를 추가했다. Run 02 24개 run과
  12 pair 공식 comparison, home-normalized 재분석, integer servo
  command 변화율, Diamond 일관 개선, circle phase lag 진단을 요약했다.
  Stage 7~9 상태와 다음 작업을 xp40/xp60 home repeatability 6개 run,
  preload 선택 및 최소 Diamond 확인 실험 중심으로 교체했다.
- 결과: `fulltext.md`가 현재 구현 및 실험 상태와 일치하며, 전체
  correction이 개선됐다고 과대 해석하지 않고 Diamond 개선, cross
  actuator resolution, reverse grid baseline drift, circle dynamic lag를
  분리해 설명한다. Computer 2와 Vision Codex 인수인계 문서 및 확정
  run ID도 요약본에서 바로 찾을 수 있다.
- 변경 파일:
  `docs/plans/2026-06-07_007_update_fulltext_after_run02.md`,
  `fulltext.md`, `docs/daily_notes/2026-06-07.md`
- 다음: 두 인수인계 문서에 따라 xp40 세 run부터 home repeatability
  데이터를 수집하고, 안전 확인 후 xp60을 수집해 공통 preload를
  선택한다.

## Entry 008
- 무엇을: 추가 하드웨어 실험 없이 기존 Run 02 artifact만 사용해
  발표용 RMSE 요약 Markdown과 CSV를 생성했다.
- 왜: 하드웨어 문제로 추가 실험이 불가능한 상황에서 현재 12개 OFF/ON
  pair의 trajectory별 결과와 해석 한계를 근거로 결론을 도출하기
  위해서다.
- 어떻게:
  `experiments/results/run02_comparison_2026-06-07.json`과
  `experiments/results/run02_reanalysis_2026-06-07.json`의 trajectory
  summary를 사용했다. Official absolute와 home-normalized 각각에 대해
  OFF/ON mean, 표시 평균 기준 improvement percent, improved pair count,
  네 trajectory 동일가중 전체 평균을 정리했다. Circle radial,
  tangential, phase lag와 trajectory별 integer servo command 반영률은
  reanalysis diagnostic을 인용했다.
- 결과: Diamond는 absolute `13.67%`, home-normalized `14.96%` 개선과
  두 기준 모두 `3/3` pair 개선으로 가장 일관됐다. Reverse grid는
  absolute `11.50%`에서 home-normalized `3.42%`로 개선 폭이 감소해
  home offset 의존성이 확인됐다. Cross는 실제 integer servo command
  변화율이 `11.1%`에 그쳐 actuator resolution 제한이 컸다. Circle은
  radial RMSE가 `3.319 -> 2.843 mm`로 감소했지만 tangential RMSE와
  phase lag가 증가해 공식 RMSE가 악화됐다. 전체 동일가중 평균은
  absolute `-1.01%`, home-normalized `-8.53%`로 전체 개선을 주장할 수
  없으며 trajectory별 결론이 필요하다.
- 변경 파일:
  `docs/plans/2026-06-07_008_run02_presentation_summary.md`,
  `experiments/results/run02_presentation_summary_2026-06-07.md`,
  `experiments/results/run02_presentation_summary_2026-06-07.csv`,
  `docs/daily_notes/2026-06-07.md`
- 다음: 발표에서는 전체 평균을 보조 지표로만 제시하고 Diamond의
  일관된 개선, reverse grid의 home-offset 민감도, Cross의 command
  resolution, Circle의 tangential/phase lag 증가를 중심 결론으로
  사용한다.

## Entry 009
- 무엇을: Run 02 발표 요약 Markdown을 한국어로 변환하고, Run 01
  offline Ridge 예상 RMSE 감소를 발표용 Markdown과 CSV로 정리했다.
- 왜: 발표에서 Run 01 모델 평가의 예상 개선과 Run 02 실제 하드웨어
  OFF/ON 결과를 함께 제시하되 두 지표의 의미를 혼동하지 않기 위해서다.
- 어떻게: Run 02의 표 수치와 계산 정의는 유지하면서 제목, 설명, 표
  머리글, trajectory 해석과 결론을 한국어로 변환했다. Run 01은
  `ridge_run01_holdout_2026-06-06.json`의 complete holdout 4개에 대해
  zero-prediction XY RMSE와 frozen Ridge residual XY RMSE를 비교했다.
  Main 3-fold OOF, row-weighted phase 분석과 partial Circle 결과도 별도
  구간으로 정리했다.
- 결과: Run 01 complete holdout 동일가중 macro XY RMSE는
  `4.533 -> 2.848 mm`, 예상 감소율은 `37.174%`였고 네 run 모두
  residual이 감소했다. Trajectory별 예상 감소율은 Static `43.427%`,
  Cross `16.933%`, Square `36.110%`, Grid `45.895%`였다. Partial
  Circle은 `6.665 -> 7.872 mm`로 `18.113%` 악화돼 공식 complete macro
  평균에서 제외했다. 이 값은 offline prediction residual이며 Run 02의
  실제 tracking 개선율과 직접 동일시할 수 없음을 명시했다.
- 변경 파일:
  `docs/plans/2026-06-07_009_korean_run02_and_run01_presentation_summary.md`,
  `experiments/results/run02_presentation_summary_2026-06-07.md`,
  `experiments/results/run01_presentation_summary_2026-06-07.md`,
  `experiments/results/run01_presentation_summary_2026-06-07.csv`,
  `docs/daily_notes/2026-06-07.md`
- 다음: 발표에서는 Run 01의 `37.174%`를 모델의 offline 예상치로 먼저
  제시하고, Run 02에서 actuator 해상도, home drift와 동적 phase lag가
  포함되면서 trajectory별 실제 결과가 달라졌다고 연결한다.

## Entry 010
- 무엇을: 기존 Run 01/02 결과 artifact에서 발표용 그래프 7종을 PNG와
  SVG로 생성했다.
- 왜: 추가 하드웨어 실험 없이 trajectory별 RMSE, 개선율, home
  normalization 영향, Circle error decomposition과 actuator command
  반영률을 PPT에서 직접 설명하기 위해서다.
- 어떻게: 외부 plotting package가 없는 환경에서도 재현할 수 있도록
  Python 표준 라이브러리만 사용하는 SVG/PNG renderer를
  `experiments/generate_presentation_figures.py`에 구현했다. Run 02는
  official OFF/ON RMSE, absolute improvement, absolute 대
  home-normalized improvement, Circle radial/tangential/phase lag,
  integer servo command 변화율을 그렸다. Run 01은 complete holdout의
  zero-prediction 대 Ridge residual RMSE와 offline 예상 감소율을
  그렸다.
- 결과: `experiments/figures/`에 Run 02 5종과 Run 01 2종의 PNG/SVG 총
  14개 파일을 생성했다. 모든 PNG는 `1600x900`, SVG는
  `viewBox="0 0 1600 900"`이며 요청 수치 label을 포함한다. 동일
  스크립트를 재실행한 결과 14개 파일이 byte 단위로 동일해 재현성을
  확인했다.
- 변경 파일:
  `docs/plans/2026-06-07_010_run01_run02_presentation_figures.md`,
  `experiments/generate_presentation_figures.py`,
  `experiments/figures/run02_rmse_by_trajectory.png/.svg`,
  `experiments/figures/run02_improvement_by_trajectory.png/.svg`,
  `experiments/figures/run02_absolute_vs_home_normalized.png/.svg`,
  `experiments/figures/run02_circle_error_decomposition.png/.svg`,
  `experiments/figures/run02_actuator_command_change_rate.png/.svg`,
  `experiments/figures/run01_holdout_rmse_by_trajectory.png/.svg`,
  `experiments/figures/run01_expected_improvement_by_trajectory.png/.svg`,
  `docs/daily_notes/2026-06-07.md`
- 다음: PPT에서는 Run 01 offline 예상 감소를 먼저 제시하고, Run 02의
  trajectory별 실제 결과와 Circle/actuator 진단 figure로 예상치와
  실제 적용 결과의 차이를 설명한다.

## Entry 011
- 무엇을: 기존 Run 02 발표 패키지에서 미완료였던 failure mode table과
  presentation numbers check 문서를 추가했다.
- 왜: 발표에서 trajectory별 조건부 개선과 실패 원인을 일관된 표로
  설명하고, JSON/CSV/figure 사이의 수치 정의와 source 경로를 사전에
  검증하기 위해서다.
- 어떻게: 기존 summary와 figure는 변경하지 않았다. Reanalysis JSON의
  absolute/home-normalized RMSE, improved pair, actuator command 변화율,
  Circle radial/tangential/phase 값을 summary CSV와 자동 대조했다.
  Comparison JSON의 pair-level percent mean과 발표의 mean-based
  improvement percent가 다른 이유도 별도 표로 기록했다. Correction
  sign 방향 일치율 `95.6~100%`는 machine-readable JSON이 아니라 기존
  Entry 003과 `fulltext.md`에 기록된 진단값임을 제한으로 명시했다.
- 결과: Required package file, Run 02 merged CSV `24`개, summary CSV
  data row `10`개와 필수 PNG `5`개의 존재를 확인했다. Summary 핵심
  수치는 source JSON과 허용 오차 내에서 모두 일치했다. 수치 불일치는
  없으며 improvement percent는 계산 정의 두 가지를 혼용하지 않아야
  한다. Source path 누락도 없었다.
- 변경 파일:
  `docs/plans/2026-06-07_011_run02_failure_modes_and_numbers_check.md`,
  `experiments/results/run02_failure_mode_table_2026-06-07.md`,
  `experiments/results/run02_presentation_numbers_check_2026-06-07.md`,
  `docs/daily_notes/2026-06-07.md`
- 다음: 발표에서는 overview RMSE, trajectory별 improvement,
  absolute/home-normalized 비교, actuator resolution, Circle dynamic
  failure 순서로 제시하고 조건부 개선과 한계 분석으로 결론낸다.
