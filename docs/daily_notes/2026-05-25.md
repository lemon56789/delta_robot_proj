# Daily Note - 2026-05-25

## Entry 001
- 무엇을:
  - `IK/FK`의 `theta` 부호 규약과 `FK`의 lower branch 선택을 문서 SoT와 일치하도록 수정했다.
  - `fake_pipeline`의 angle range를 현재 SoT `-45..90 deg`로 갱신하고 2026-05-04 샘플 CSV/JSON을 다시 생성했다.
- 왜:
  - 문서 기준 `+theta = upper arm이 -z 방향으로 내려가는 회전`인데, 구현은 `IK`에서 `theta = -2atan(t)`를 사용하고 `FK` sphere center `z`도 반대 부호를 써서 물리 기하와 어긋나 있었다.
  - 그 결과 `IK -> FK`는 내부적으로만 맞고, `theta=45 deg` 대칭 자세가 문서 기하상 `z≈-376 mm`가 아니라 잘못된 상부 해로 수렴하는 문제가 있었다.
- 어떻게:
  - `kinematics/inverse_kinematics.py`에서 `t -> theta` 복원식을 `theta = 2atan(t)`로 되돌리고 기본 angle range를 `-45..90 deg`로 맞췄다.
  - `kinematics/forward_kinematics.py`에서 elbow/sphere center의 `z` 부호를 `-L sin(theta)`로 맞추고, 초기 추정값이 세 구의 하부 교차해를 우선 잡도록 수정했다.
  - `experiments/fake_pipeline.py`의 `THETA_MIN_DEG`를 `-45.0`으로 바꾸고 샘플 CSV/JSON을 재생성했다.
- 결과:
  - `delta_fk(45, 45, 45)`가 `z=-376.017157... mm`를 반환해 문서 기하와 맞아졌다.
  - 첫 row 목표점 `(0, 4.673020..., -212.0)`에 대해 `delta_ik(...) = (-33.309286..., -35.134779..., -35.134779...)`가 나오고, 해당 각도를 `delta_fk(...)`에 넣으면 원래 목표점을 재현한다.
  - `python kinematics/validate_roundtrip.py` 검증이 대표 샘플들에 대해 `max_position_error_mm=0.000000`으로 통과했다.
- 변경 파일:
  - `docs/plans/2026-05-25_001_fix_ik_fk_theta_sign_consistency.md`
  - `kinematics/inverse_kinematics.py`
  - `kinematics/forward_kinematics.py`
  - `experiments/fake_pipeline.py`
  - `data/fake_pipeline/fake_pipeline_sample_2026-05-04.csv`
  - `data/fake_pipeline/fake_pipeline_sample_2026-05-04.json`
  - `docs/daily_notes/2026-05-25.md`
- 다음:
  - `Simulink/Simscape` 쪽 joint sign, frame orientation, output point가 수정된 Python 기하 기준과 일치하는지 다시 대조한다.
  - 기존 workspace/fake dataset 해석에서 양수 `theta`를 전제로 읽은 부분이 없는지 점검한다.

## Entry 002
- 무엇을:
  - 기존 `fake_pipeline_sample_2026-05-04.csv`의 `target_x/y/z` 전 구간을 유지한 채, 수정된 `IK/FK` 기준으로 재계산한 별도 CSV `fake_pipeline_sample_2026-05-04_recomputed.csv`를 생성했다.
- 왜:
  - 기존 파일은 보존하면서도 동일한 target trajectory에 대해 수정된 운동학 결과를 독립 파일로 비교할 수 있어야 하기 때문이다.
- 어떻게:
  - 원본 CSV에서 `time`, `target_x`, `target_y`, `target_z`를 읽고, 현재 `delta_ik`, `delta_fk`, fake measured angle 모델을 사용해 `theta_cmd`, `theta_meas`, `sim_*`, `error_*`를 다시 계산했다.
  - 계산 결과를 `data/fake_pipeline/fake_pipeline_sample_2026-05-04_recomputed.csv`로 저장했다.
- 결과:
  - 새 CSV는 원본과 동일한 `180` rows를 가지며, 첫 row의 target은 `(0.0, 4.673020107703806, -212.0)`로 유지된다.
  - 첫 row 재계산 결과는 `theta_cmd=(-33.309286..., -35.134779..., -35.134779...)`, `sim=(-1.398886e-14, 4.673020..., -212.000000...)`로 기록되었다.
- 변경 파일:
  - `docs/plans/2026-05-25_002_generate_recomputed_fake_pipeline_csv.md`
  - `data/fake_pipeline/fake_pipeline_sample_2026-05-04_recomputed.csv`
  - `docs/daily_notes/2026-05-25.md`
- 다음:
  - 원본 CSV, recomputed CSV, Simscape CSV의 `theta_*`, `sim_*`, `error_*`를 나란히 비교해 Python/Simscape 불일치 원인을 분리한다.

## Entry 003
- 무엇을:
  - 현재 수정된 운동학 기준에서 `theta_cmd`가 전 구간 양수로 유지되는 fake pipeline 케이스 `fake_pipeline_sample_2026-05-25_positive_theta.csv`를 생성했다.
- 왜:
  - 음수 `theta` branch뿐 아니라 양수 `theta` branch에서의 CSV 거동도 별도 기준 파일로 확인할 필요가 있었기 때문이다.
- 어떻게:
  - 현재 fake pipeline과 같은 `x/y` 진동 패턴을 유지하되, `z` trajectory를 `-340 + 8 cos(phase) mm`로 두어 target `z` 범위를 `-348..-332 mm`로 설정했다.
  - 각 row에 대해 현재 `delta_ik`, `delta_fk`, fake measured angle 모델을 적용해 CSV를 생성했다.
- 결과:
  - 생성 파일은 `180` rows이며, `theta1_cmd/theta2_cmd/theta3_cmd`의 최소값이 각각 `24.297...`, `23.552...`, `24.658... deg`로 전 구간 양수다.
  - 첫 row는 `target=(0.0, 4.673020..., -332.0)`, `theta_cmd=(25.857707..., 24.678232..., 24.678232...)`, `sim=(-2.841344e-14, 4.673020..., -332.0)`로 기록되었다.
- 변경 파일:
  - `docs/plans/2026-05-25_003_generate_positive_theta_fake_pipeline_case.md`
  - `data/fake_pipeline/fake_pipeline_sample_2026-05-25_positive_theta.csv`
  - `docs/daily_notes/2026-05-25.md`
- 다음:
  - 양수 `theta` 케이스와 Simscape 결과를 비교해, 양/음 branch별 좌표계/부호 해석 차이가 남아 있는지 확인한다.

## Entry 004
- 무엇을:
  - 가상센서 baseline 모델 구현 전에 필요한 선행 작업 범위를 정리한 계획서 `docs/plans/2026-05-25_004_pre_virtual_sensor_simscape_alignment_plan.md`를 추가했다.
- 왜:
  - 현재 `Simscape`의 `sim_*` 계산이 구현되었더라도, Python 운동학과 같은 좌표계/부호/출력점/시간축 의미로 정렬되지 않으면 이후 `virtual_sensor` 학습 입력이 오염될 수 있기 때문이다.
  - 로드맵상 `Stage 4`가 아직 `부분 완료` 상태이므로, 모델 구현 전에 simulation path를 먼저 닫는 순서를 명확히 남길 필요가 있었기 때문이다.
- 어떻게:
  - `Stage 4`와 `Stage 8` 사이 경계를 기준으로, 선행 작업을 `Simscape-Python CSV 비교`, `simulation output 의미 고정`, `sampling/logging/post-alignment 규칙 구체화` 세 묶음으로 정리했다.
  - 비교 기준 데이터로 `recomputed` 케이스와 `positive_theta` 케이스를 명시해 다음 작업이 바로 비교/정렬 검증으로 이어지게 했다.
- 결과:
  - 다음 구현 우선순위가 `virtual_sensor` 모델 추가가 아니라 `Simscape-Python 정렬 검증`임이 계획서 기준으로 고정되었다.
  - 후속 작업을 비교 스크립트, 문서 보강, alignment 규칙 정리로 분해할 기준이 생겼다.
- 변경 파일:
  - `docs/plans/2026-05-25_004_pre_virtual_sensor_simscape_alignment_plan.md`
  - `docs/daily_notes/2026-05-25.md`
- 다음:
  - `Simscape` 결과 CSV와 Python 기준 CSV를 나란히 비교하는 실제 작업 계획서를 더 작은 단위로 작성한다.
  - 비교 결과를 바탕으로 `simulation path`와 `alignment` 문서의 빈 항목을 채울지 판단한다.

## Entry 005
- 무엇을:
  - `recomputed` 및 `positive_theta` 기준 Python/Simscape CSV 비교 결과를 바탕으로 `lag`와 `error_*` 의미를 SoT 문서에 반영했다.
- 왜:
  - 현재 `virtual_sensor` 모델 구현 전에 `Simscape` 결과가 어떤 시간축과 어떤 correction field 의미를 가지는지 먼저 고정해야 학습 입력 해석이 흔들리지 않기 때문이다.
  - 특히 현재 `Simscape` CSV의 `error_*`가 SoT 의미와 다르므로, 이를 문서로 분리해 두지 않으면 이후 계약 필드로 오해할 위험이 있었다.
- 어떻게:
  - `recomputed`/`positive_theta`와 대응 `Simscape` CSV를 비교해 현재 comparison path에서 `sim_*`가 provisional `1 sample = 20 ms` lag를 가진다는 점을 정리했다.
  - `docs/system_data_flow.md`의 simulation path, alignment policy, correction field notes를 갱신해 `error_*`의 SoT 의미를 `measured_position - sim_position`으로 고정했다.
  - `docs/roadmap.md` Stage 4 current status에 현재 비교 결과와 후속 `Simulink/Simscape` 수정 필요 사항을 추가했다.
- 결과:
  - 현재 fake pipeline comparison 기준에서 사용할 provisional lag가 `20 ms`로 기록되었다.
  - `error_x`, `error_y`, `error_z`는 계속 SoT 기준 correction field로 유지되고, 현재 `Simscape` CSV의 `target_position - sim_position` 값은 임시 diagnostic 값으로만 해석하게 되었다.
  - 하드웨어 또는 estimator 경로에서 `measured_position`을 확보하면 `Simulink/Simscape` export의 `error_*` 계산을 나중에 수정해야 한다는 점이 문서에 남았다.
- 변경 파일:
  - `docs/plans/2026-05-25_005_record_simscape_lag_and_error_semantics.md`
  - `docs/system_data_flow.md`
  - `docs/roadmap.md`
  - `docs/daily_notes/2026-05-25.md`
- 다음:
  - 현재 provisional `20 ms` lag를 실제 hardware logging path에서도 유지할 수 있는지 후속 비교에서 재검증한다.
  - `measured_position` 확보 후 `Simulink/Simscape` CSV의 `error_*`를 `measured_position - sim_position` 기준으로 수정한다.
