# Daily Note - 2026-05-26

## Entry 001
- 무엇을:
  - `2026-05-25` 기준 `IK/FK` 정리 이후 남아 있던 요약 문서 불일치를 점검하고, `fulltext.md`와 `docs/roadmap.md`를 현재 SoT에 맞게 갱신했다.
- 왜:
  - 핵심 SoT 문서들은 이미 `theta` 부호, angle range, `Simscape` lag, `error_*` 의미를 반영했지만, 요약 문서와 상태 라벨 일부는 이전 해석을 남겨 두고 있었기 때문이다.
  - 특히 `fulltext.md`가 아직 `theta` 부호 보정 서술과 `virtual_sensor` 선행 우선순위를 유지하면 현재 작업 방향을 잘못 읽을 수 있었다.
- 어떻게:
  - `fulltext.md`의 갱신일, alignment/correction 요약, `IK` 현재 코드 상태, 현재 구현 기준 핵심 상태, 바로 다음 작업 우선순위를 현재 `docs/*` SoT 기준으로 다시 정리했다.
  - `docs/roadmap.md`의 `Current Status Snapshot` 기준일 라벨을 상세 상태 반영 범위와 맞췄다.
- 결과:
  - `fulltext.md`가 `theta = 2atan(t)`, provisional `20 ms` lag, `error_* = measured_position - sim_position`, `Simscape-Python alignment` 선행 우선순위를 반영하게 되었다.
  - `docs/roadmap.md` 상단 snapshot 날짜가 `2026-05-25` 기준으로 정렬되어 문서 내부 일관성이 개선되었다.
- 다음:
  - `Simscape` comparison path와 실제 hardware logging path의 lag 차이를 후속 비교에서 다시 검증한다.
  - `measured_position` 확보 후 `Simulink/Simscape` export의 `error_*`를 SoT 의미에 맞게 수정한다.
