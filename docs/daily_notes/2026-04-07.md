# Daily Note - 2026-04-07

## Entry 001
- 무엇을:
  - `docs/plans/2026-04-07_001_ik_root_selection_rule.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에 IK `+/-` 해 선택 규칙을 추가했다.
- 왜:
  - `2atan(t)` 기반 해를 문서에 연결했더라도, 실제로 어느 branch를 선택할지 기준이 없으면 구현 시 해석이 다시 갈릴 수 있기 때문이다.
- 어떻게:
  - 판별식 기반 reject, 모터 각도 범위 검사, downward-working branch 검사, `previous_theta_i` 기반 연속성 선택 규칙을 순서대로 문서에 반영했다.
  - 초기 시점에는 `previous_theta_i`가 없다는 점도 별도 규칙으로 정리했다.
- 결과:
  - IK 해 선택 규칙이 문서 수준에서 정리되어 후속 구현과 테스트의 기준점이 생겼다.
- 변경 파일:
  - `docs/plans/2026-04-07_001_ik_root_selection_rule.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-07.md`
- 다음:
  - `theta_min`, `theta_max` 수치와 downward-working branch 판정 조건을 더 구체화하거나, arm 2와 arm 3 대칭 확장을 정리한다.

## Entry 002
- 무엇을:
  - `docs/plans/2026-04-07_002_provisional_theta_range_0_90.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`의 해 선택 규칙에 임시 각도 범위 `0 deg <= theta_i <= 90 deg`를 반영했다.
- 왜:
  - 현재 단계에서는 기구 수치가 모두 확정되지 않았더라도, branch 선택용 1차 working range가 있어야 구현과 테스트 기준을 세울 수 있기 때문이다.
- 어떻게:
  - 해 선택 규칙의 각도 범위 검사 항목을 일반적인 `theta_min`, `theta_max` 표기 대신 현재 임시 working range `0 deg <= theta_i <= 90 deg`로 바꾸고, 추후 갱신 가능하다고 명시했다.
- 결과:
  - IK 해 선택 규칙이 당장 사용할 수 있는 형태로 더 구체화되었다.
- 변경 파일:
  - `docs/plans/2026-04-07_002_provisional_theta_range_0_90.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-07.md`
- 다음:
  - arm 2와 arm 3 대칭 확장을 정리하거나, 필요하면 downward-working branch 판정을 더 엄밀한 문장으로 다듬는다.

## Entry 003
- 무엇을:
  - `docs/plans/2026-04-07_003_expand_arm2_arm3_by_symmetry.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에 arm 2와 arm 3의 대칭 확장과 일반화된 `E_i`, `F_i`, `G_i`를 추가했다.
- 왜:
  - arm 1만 식이 닫혀 있으면 실제 3축 델타 로봇 IK로 쓰기 어렵고, 나머지 두 arm도 같은 좌표계 기준으로 정리되어야 구현 패턴이 완성되기 때문이다.
- 어떻게:
  - 각 arm의 outward 방향 단위벡터 `e_1`, `e_2`, `e_3`를 정의하고, 이를 이용해 `B_i`, `P_i`, `J_i`를 일반화했다.
  - `p_i = r · e_i`를 도입해 일반화된 `E_i`, `F_i`, `G_i`를 정리한 뒤 arm 2, arm 3 식을 각각 풀어 적었다.
- 결과:
  - 3개 arm 전체의 IK 구조가 같은 문서 안에서 연결되었고, arm 1 식이 일반식의 특수한 경우라는 점도 분명해졌다.
- 변경 파일:
  - `docs/plans/2026-04-07_003_expand_arm2_arm3_by_symmetry.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-07.md`
- 다음:
  - 일반화된 `E_i`, `F_i`, `G_i`에 반각 치환을 일괄 적용하는 정리나, 구현용 함수 시그니처 초안으로 이어간다.
