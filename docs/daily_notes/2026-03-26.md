# Daily Note - 2026-03-26

## Entry 001
- 무엇을:
  - `docs/system_data_flow.md` 파일을 생성하고, 사용자가 직접 채울 수 있는 빈 입력 템플릿을 추가했다.
  - 작업 절차 기록을 위해 계획서 `docs/plans/2026-03-26_001_system_data_flow_template.md`를 작성했다.
- 왜:
  - 시스템 데이터 흐름 문서를 작성하기 전에 단위, 시간축, 인터페이스, 보정 지점을 누락 없이 수집할 표준 입력 양식이 필요해서.
- 어떻게:
  - 대화에서 확정한 템플릿 구조를 기준으로 섹션/필드 항목을 빈 상태로 정리해 신규 Markdown 파일로 생성했다.
  - AGENTS 작업 규칙에 맞게 계획서와 변경 기록 문서를 함께 추가했다.
- 결과:
  - 팀원이 동일한 템플릿에 값을 채워 `system_data_flow` 본문으로 전환할 수 있는 준비 상태가 됐다.
- 변경 파일:
  - `docs/plans/2026-03-26_001_system_data_flow_template.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - 사용자가 템플릿 항목을 채우면 `docs/system_data_flow.md`를 최종 데이터 흐름 문서 포맷으로 정리한다.

## Entry 002
- 무엇을:
  - `docs/system_data_flow.md`의 `Meta` 항목에 초안 버전과 현재 SoT 문서를 반영했다.
  - `Units & Coordinate Frame` 항목에 `deg`, `base center (O)`, workspace direction `-Z` 기준을 반영했다.
- 왜:
  - 데이터 흐름 문서의 후속 섹션(IK, CSV, simulation, correction)이 동일한 단위와 좌표계 기준 위에서 작성되어야 하기 때문이다.
- 어떻게:
  - 사용자와 대화에서 확정한 기준값을 직접 문서에 반영하고, 아직 정해지지 않은 축 방향은 `TBD`로 유지했다.
- 결과:
  - 각도 단위와 원점 정의, 작업공간 방향 기준이 문서에 고정되었다.
  - `+X`, `+Y`는 후속 팀 합의가 필요한 미정 항목으로 분리되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_002_system_data_flow_meta_units.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Timestamp Policy`를 확정해 실물-시뮬레이션 정렬 기준을 먼저 고정한다.

## Entry 003
- 무엇을:
  - `docs/system_data_flow.md`의 좌표축 정의에서 `+X`, `+Y`, `+Z`의 방향을 팀 합의 기준으로 확정했다.
  - base/platform geometry와 링크 길이 관련 기호 정의(`sB`, `sP`, `uB`, `uP`, `wB`, `wP`, `L`, `l`, `B1~B3`, `P1~P3`)를 문서에 반영했다.
- 왜:
  - IK, simulation, geometry 문서가 같은 좌표축과 기호 체계를 공유해야 이후 구현과 검증에서 해석 차이가 생기지 않기 때문이다.
- 어떻게:
  - 사용자 제공 정의를 기준으로 좌표축 방향을 문서 문장으로 정리하고, 기호 설명을 `Units & Coordinate Frame` 비고에 통합했다.
- 결과:
  - 기존 `TBD` 항목이 제거되었고, 좌표계와 주요 기하 기호가 문서에서 직접 참조 가능한 상태가 되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_003_system_data_flow_coordinate_definitions.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Timestamp Policy`와 `IK Interface Contract`를 같은 좌표계 기준으로 이어서 확정한다.

## Entry 004
- 무엇을:
  - `docs/system_data_flow.md`의 좌표축 표기를 대소문자 혼용 없이 `x`, `y`, `z` 기준으로 통일했다.
- 왜:
  - 좌표축 표기 규칙을 문서 전체에서 일관되게 유지해 해석 혼선을 줄이기 위해.
- 어떻게:
  - `Units & Coordinate Frame` 섹션의 축 방향과 workspace direction 표기를 소문자 기준으로 수정했다.
- 결과:
  - 문서 내 좌표축 표기 규칙이 `x, y, z`로 고정되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_004_system_data_flow_axis_notation.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Timestamp Policy`를 확정해 실물 데이터와 시뮬레이션 데이터의 시간 정렬 기준을 정의한다.

## Entry 005
- 무엇을:
  - `docs/system_data_flow.md`의 `Timestamp Policy` 섹션에 시간 기록 정책을 반영했다.
  - timestamp source를 `both`로, 누락 row 처리 규칙을 `invalid` 표기로 확정했다.
- 왜:
  - 실물 경로와 PC logger 경로를 이후 비교 및 정렬할 수 있도록 시간 기록 기준을 먼저 고정해야 하기 때문이다.
- 어떻게:
  - `time` 필드명, `boot_ms` 포맷, `ms` 해상도, `post-alignment` 정렬 방식을 문서에 채우고 누락 row 처리 규칙을 명시했다.
- 결과:
  - Arduino와 PC logger가 각각 timestamp를 남기고, 후처리에서 정렬하는 구조가 문서 기준으로 고정되었다.
  - timestamp 누락 row는 삭제가 아니라 `invalid` 표기로 관리하는 정책이 명확해졌다.
- 변경 파일:
  - `docs/plans/2026-03-26_005_system_data_flow_timestamp_policy.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `IK Interface Contract`를 채워 입력/출력 필드명과 순서를 고정한다.

## Entry 006
- 무엇을:
  - `docs/system_data_flow.md`의 `IK Interface Contract` 섹션에 입력/출력 필드명과 실패 처리 정책을 반영했다.
  - 출력 필드명을 `theta1_cmd`, `theta2_cmd`, `theta3_cmd`로 확정했다.
- 왜:
  - 역기구학 결과를 controller, logger, virtual sensor가 동일한 이름으로 참조해야 데이터 흐름 문서가 실제 인터페이스 기준으로 동작하기 때문이다.
- 어떻게:
  - 입력 필드를 `target_x`, `target_y`, `target_z`로, 출력 필드를 `theta1_cmd`, `theta2_cmd`, `theta3_cmd`로 채우고 실패 동작을 `reject`로 명시했다.
  - notes에 좌표계와 단위 기준도 함께 추가했다.
- 결과:
  - IK 블록의 입출력 명세와 실패 정책이 문서에 고정되었다.
  - 향후 CSV 계약에서 `motor*_cmd`와의 정합성 검토가 필요하다는 위험도 분리되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_006_system_data_flow_ik_contract.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Real Measurement Definition`에서 `theta_meas`의 실제 취득 방식과 샘플링 기준을 확정한다.

## Entry 007
- 무엇을:
  - `docs/system_data_flow.md`의 `Simulation Path Definition` 섹션에 시뮬레이션 기준 경로를 반영했다.
  - primary simulation source를 `Simscape`로 고정하고, `RecurDyn`는 secondary reference 용도로 notes에 분리했다.
- 왜:
  - 데이터 흐름 문서에서 기준 시뮬레이터가 둘이면 비교 기준과 책임이 모호해지기 때문이다.
- 어떻게:
  - simulation input을 `target_x`, `target_y`, `target_z`로, output을 `sim_x`, `sim_y`, `sim_z`로 채우고 기준 source를 `Simscape`로 명시했다.
- 결과:
  - 현재 문서의 simulation path는 `Simscape` 중심 구조로 고정되었다.
  - `RecurDyn`는 후속 cross-check 용도의 보조 reference로만 남겨졌다.
- 변경 파일:
  - `docs/plans/2026-03-26_007_system_data_flow_simulation_path.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Real-Sim Alignment Policy`를 정해 실물 데이터와 시뮬레이션 데이터를 어떻게 시간축에서 맞출지 정의한다.

## Entry 008
- 무엇을:
  - `docs/system_data_flow.md`의 `Real-Sim Alignment Policy` 섹션에 시간 정렬 기준을 반영했다.
  - alignment reference clock을 `PC logger`로, resampling method를 `linear interpolation`으로 확정했다.
- 왜:
  - 실물 데이터와 simulation 데이터를 동일한 시간축에서 비교하려면 기준 clock과 정렬 방식을 먼저 고정해야 하기 때문이다.
- 어떻게:
  - delay compensation을 `post-alignment`로 정의하고, `invalid` row 제외 정책과 serial delay 한계를 notes에 추가했다.
- 결과:
  - 현재 문서 기준에서 real-sim 정렬은 PC logger 시간축 기반 후처리 방식으로 고정되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_008_system_data_flow_alignment_policy.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Correction Injection Point`에서 보정값이 target position에 들어갈지 motor command에 들어갈지 정책을 확정한다.

## Entry 009
- 무엇을:
  - `docs/system_data_flow.md`의 `Correction Injection Point` 섹션에 보정 주입 정책을 반영했다.
  - correction target을 `target_position`으로 확정하고 correction field 초안(`corr_x`, `corr_y`, `corr_z`)을 채웠다.
- 왜:
  - virtual sensor의 보정값이 제어 흐름 어디에 적용되는지 먼저 고정해야 이후 controller와 CSV 계약이 흔들리지 않기 때문이다.
- 어떻게:
  - fallback 정책을 `use uncorrected target position`으로 추가하고, 나머지 clamp 수치는 비워 두었다.
- 결과:
  - 현재 문서 기준에서 correction은 motor command가 아니라 target position에 적용되는 구조로 고정되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_009_system_data_flow_correction_point.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `CSV Contract (Final)`에서 실제 필드명과 기존 SoT와의 차이를 정리한다.

## Entry 010
- 무엇을:
  - `docs/system_data_flow.md`의 correction field 명칭을 `corr_*`에서 `error_*`로 변경했다.
  - `CSV Contract (Final)` 섹션에 `theta_*`와 `error_*` 기준의 고정 컬럼 순서 초안을 반영했다.
- 왜:
  - 명령/측정 각도는 `motor`보다 `theta`가 IK 문맥에 더 정확하고, 보정 관련 위치 필드는 `error`가 팀 내부 해석에 더 자연스럽기 때문이다.
- 어떻게:
  - `Correction Injection Point`의 field와 notes를 수정하고, CSV header required, fixed column order, contract change detail을 문서에 채웠다.
- 결과:
  - 현재 문서 기준 CSV 명명 규칙이 `theta1_cmd ~ theta3_meas`, `error_x ~ error_z`로 정리되었다.
  - 기존 AGENTS 예시와 차이가 있는 부분은 `contract_change`로 명시되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_010_system_data_flow_csv_naming.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Owner Mapping`, `Validation Criteria`, `Open Questions`를 채워 문서의 운영 정보와 미정 항목을 마무리한다.

## Entry 011
- 무엇을:
  - `docs/system_data_flow.md`의 `Owner Mapping` 섹션에 기존 팀 역할 기준을 반영했다.
  - block별 owner를 trajectory, IK, controller, hardware, logger, simulation, virtual sensor, feedback controller 기준으로 채웠다.
- 왜:
  - 데이터 흐름 문서가 단순 인터페이스 정의를 넘어서 실제 작업 책임 분담표 역할도 해야 하기 때문이다.
- 어떻게:
  - `docs/member_profiles/`에 정의된 역할 설명을 기준으로 `S`, `L`, `Y`, `T`, `Team` 의미를 문서에 명시하고 block별 owner를 매핑했다.
- 결과:
  - 문서에서 각 데이터 흐름 블록의 주 담당자와 협업 범위가 바로 확인 가능한 상태가 되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_011_system_data_flow_owner_mapping.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - `Validation Criteria`와 `Open Questions`를 채워 문서를 1차 완성 상태로 마무리한다.

## Entry 012
- 무엇을:
  - `docs/system_data_flow.md`의 `Validation Criteria`와 `Open Questions` 섹션을 채워 문서를 1차 완성 상태로 마무리했다.
- 왜:
  - 데이터 흐름 문서가 실제 운영 문서로 쓰이려면 검증 기준과 미정 항목까지 명시되어 있어야 하기 때문이다.
- 어떻게:
  - 검증 지표는 `RMSE_x`, `RMSE_y`, `RMSE_z`, `max_error` 중심의 최소 기준으로 정리하고, 재현성 체크리스트에 실행 커맨드, 사용 데이터, 파라미터 및 코드 버전 기록 항목을 추가했다.
  - 미정 항목은 `theta_meas` 취득 방식, simulation sampling rate, correction clamp, CSV path/optional columns로 정리했다.
- 결과:
  - `docs/system_data_flow.md`가 메타, 좌표계, 시간 정책, IK, simulation, alignment, correction, CSV, owner, validation, open questions를 포함한 1차 기준 문서가 되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_012_system_data_flow_finalize_v0_1.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - 팀 합의가 필요한 미정 항목을 채운 뒤 `v1.0`으로 승격하고, 필요하면 템플릿 제목을 실제 문서 제목으로 정리한다.

## Entry 013
- 무엇을:
  - `docs/system_data_flow.md`의 제목에서 `Input Template` 표현을 제거하고 실제 문서 제목으로 정리했다.
  - 문서 상단에 목적 설명 2문장을 추가했다.
- 왜:
  - 현재 문서는 더 이상 입력 양식이 아니라 프로젝트의 기준 데이터 흐름 문서이기 때문이다.
- 어떻게:
  - 제목을 `System Data Flow`로 변경하고, 문서 목적과 사용 원칙을 상단 본문으로 추가했다.
- 결과:
  - 문서가 템플릿이 아니라 실제 운영 기준 문서로 바로 읽히는 상태가 되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_013_system_data_flow_title_cleanup.md`
  - `docs/system_data_flow.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - 미정 항목이 채워지면 `v0.1-draft`를 `v1.0`으로 승격하고 필요 시 문장을 최종 문서 톤으로 한 번 더 정리한다.

## Entry 014
- 무엇을:
  - `README.md`의 시스템 구조와 진행 단계를 Mermaid 다이어그램으로 시각화했다.
  - 작업 승인 이후 문서 변경 절차를 기록하기 위해 계획서 `docs/plans/2026-03-26_014_readme_process_diagram.md`를 작성했다.
- 왜:
  - 프로젝트 개요 문서에서 데이터 흐름과 전체 진행 순서를 텍스트만으로 읽는 것보다 시각적으로 바로 이해할 수 있게 만들기 위해.
- 어떻게:
  - 기존 README의 시스템 구조 설명을 기준으로 feedback loop가 보이는 Mermaid flowchart를 추가했다.
  - 기존 진행 단계 1~5의 내용을 유지한 채 단계별 핵심 작업을 포함한 Mermaid flowchart를 추가했다.
- 결과:
  - README에서 프로젝트의 시스템 흐름과 수행 단계가 한눈에 보이는 형태로 정리되었다.
- 변경 파일:
  - `docs/plans/2026-03-26_014_readme_process_diagram.md`
  - `README.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - 필요하면 README의 폴더 구조와 팀 역할도 같은 스타일의 다이어그램 또는 표로 추가 정리한다.

## Entry 015
- 무엇을:
  - `README.md`의 진행 단계 다이어그램을 PowerPoint 스타일에 가까운 단계별 박스 묶음 구조로 재작성했다.
  - 각 단계에 세부 작업 체크박스와 단계 완료 체크리스트를 추가했다.
- 왜:
  - 단순 흐름선보다 단계별 도형과 체크 항목이 있어야 프로젝트가 어디까지 진행됐는지 README에서 바로 확인할 수 있기 때문이다.
- 어떻게:
  - Mermaid `subgraph`를 사용해 각 단계를 카드처럼 묶고, 내부에 `Status`와 작업 체크 항목을 배치했다.
  - 다이어그램 아래에는 단계 완료 여부를 빠르게 갱신할 수 있는 Markdown 체크박스 목록을 추가했다.
- 결과:
  - README에서 프로젝트 로드맵과 진행 현황 표시 지점을 동시에 확인할 수 있게 되었다.
  - 실제 완료 상태는 추후 진행 상황에 맞춰 `[x]`로 갱신하면 된다.
- 변경 파일:
  - `docs/plans/2026-03-26_015_readme_stage_diagram_refine.md`
  - `README.md`
  - `docs/daily_notes/2026-03-26.md`
- 다음:
  - 실제 프로젝트 진행 상태가 정리되면 각 단계 `Status`와 체크박스를 현재 상태에 맞게 업데이트한다.
