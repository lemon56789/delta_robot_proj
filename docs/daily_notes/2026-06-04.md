# Daily Note - 2026-06-04

## Entry 001
- 무엇을: hardware base config 정리 계획서를 작성하고, Y가 제공한 power/Arduino/measurement 내용을 Markdown 구조와 한글 설명 중심으로 정리했다.
- 왜: 다중 라인 설명이 리스트 항목 밖으로 빠져 문서 구조가 깨져 보였고, 영어 설명과 일부 오타/표기 충돌이 남아 Run 00 준비 문서로 바로 쓰기 어려웠기 때문이다.
- 어떻게: `docs/hardware_experiment_base_config.md`를 섹션별 bullet 구조로 재정리하고, 전압 정보를 공칭 7.4 V와 최대 8.4 V 기준으로 정리했다. firmware 경로는 아직 repo에 추가되지 않은 예정 경로로 표시했고, `STOP` command는 현재 미구현이며 Run 00에서는 물리 전원 차단을 우선한다고 기록했다. `theta*_meas`는 SoT 기준 `theta_i` convention을 따라야 하며, servo command 기준 0점은 `center_cmd_i`로 기록한다고 정리했다.
- 결과: base config가 Markdown 구조에 맞게 정리되었고, Run 00 문서는 offset 값 입력 이후 별도 작성하기로 유지했다.
- 변경 파일: `docs/plans/2026-06-04_001_cleanup_hardware_base_config.md`, `docs/hardware_experiment_base_config.md`, `docs/daily_notes/2026-06-04.md`
- 다음: `sign_i`, `fine_offset_i`, firmware 파일 추가 여부를 확인한 뒤 Run 00 commissioning 문서를 실제 절차로 채운다.

## Entry 002
- 무엇을: `theta_i = 0 deg`에 대응하는 servo 기준값을 `center_cmd_i`로 기록하고, 추가 fine offset이 없다는 현재 판단을 반영했다.
- 왜: `84/86/88 deg`는 SoT 기준 0도와 servo command 기준 0점 사이의 기준 command 값이며, `theta*_meas` offset이나 fine calibration offset으로 부르면 이후 데이터 필드 의미와 혼동될 수 있기 때문이다.
- 어떻게: Arduino 변환식을 `servo_angle_i = center_cmd_i + sign_i * theta_cmd_i + fine_offset_i`로 정리하고, `center_cmd_1=84 deg`, `center_cmd_2=86 deg`, `center_cmd_3=88 deg`, `sign_i=+1`, `fine_offset_i=0 deg`를 measurement calibration table에 기록했다.
- 결과: 현재 하드웨어 기준에서는 `+90 deg` command가 실제 `+90 deg` 이동으로 해석되며, Run 00에서는 arm별 저속 확인으로 `sign_i=+1`과 추가 fine offset 없음이 유지되는지만 검증하면 된다.
- 변경 파일: `docs/hardware_experiment_base_config.md`, `docs/daily_notes/2026-06-04.md`
- 다음: Run 00 문서에 center command 기준 zeroing, `±3 deg -> ±5 deg` 조립 상태 소각도 확인, vision 위치 확인 절차를 실제 실행 순서로 작성한다.

## Entry 003
- 무엇을: 최근 하드웨어 기준 정리 내용을 외부 AI 분석용 요약본 `fulltext.md`에 반영했다.
- 왜: `fulltext.md`가 2026-05-28 상태에 머물러 있어 `wB=46 mm`, `H=285 mm`, Simscape 재확인, 하드웨어 base config, Run 00 준비 기준, `center_cmd_i` 기록 방식이 빠져 있었기 때문이다.
- 어떻게: 갱신일을 2026-06-04로 올리고, 하드웨어 참고 치수, 전원/안전, Arduino command mapping, Run 00 목적, vision 위치 확인, 최신 roadmap 상태와 다음 작업 우선순위를 요약했다.
- 결과: `fulltext.md`가 현재 Run 00 준비 직전 상태를 반영하며, 상세 SoT는 기존처럼 `docs/*`를 우선하도록 유지했다.
- 변경 파일: `docs/plans/2026-06-04_002_update_fulltext_after_hardware_base_config.md`, `fulltext.md`, `docs/daily_notes/2026-06-04.md`
- 다음: Run 00 commissioning 문서를 실제 실행 절차로 작성한다.

## Entry 004
- 무엇을: Run 00 commissioning 문서를 A/B/C gate 구조로 재작성했다.
- 왜: Run 00은 위치 정확도 평가가 아니라 조립 안정성, 방향성, 로그/비전 준비 상태를 확인하는 gate 실험이어야 하므로 단계별 목적과 통과 조건을 분리해야 하기 때문이다.
- 어떻게: A를 assembled joint small-angle check로 두고 `±3 deg -> ±5 deg` 단일축 확인을 기록했다. B는 IK 기반 `±3 mm -> ±5 mm` x/y 방향 이동으로 방향성과 대략적인 평행 이동만 확인하도록 했다. C는 ArUco marker 검출, `vision_x/y` 좌표 방향, static noise, 같은 `run_id` 여부를 확인하도록 정리했다. base config의 Run 00 theta test range와 `fulltext.md`의 Run 00 요약도 `±3 deg -> ±5 deg` 기준으로 맞췄다.
- 결과: Run 00이 Run 01 baseline 데이터 수집 전 gate 문서로 정리되었고, A가 실패하면 B로 가지 않고 B가 실패하면 C로 가지 않는 조건이 명시되었다.
- 변경 파일: `docs/plans/2026-06-04_003_write_run_00_commissioning_abc_gate.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/hardware_experiment_base_config.md`, `fulltext.md`, `docs/daily_notes/2026-06-04.md`
- 다음: 실제 실험 전 `z0`, firmware path/status, logger 저장 경로, vision calibration file을 채운다.

## Entry 005
- 무엇을: Run 00 문서에 실험 전 확정/보류 상태를 반영했다.
- 왜: `z0`, firmware path/status, vision calibration file은 아직 확정 전이고, 로그 저장 경로는 기본 예시 경로로 사용할 수 있기 때문이다.
- 어떻게: `z0`는 A 단계 수행 후 현재 안전 home pose 기준 z로 기록하도록 남기고, firmware path/status는 Y 확인 전까지 pending으로 표시했다. main log와 vision log는 각각 `data/real/raw/main_<run_id>.csv`, `data/vision/raw/vision_<run_id>.csv`로 기록했다. vision calibration/homography file은 다음 정보 수신 전까지 pending으로 표시했다.
- 결과: Run 00 문서에서 지금 확정된 값과 보류된 값이 구분되었다.
- 변경 파일: `docs/plans/2026-06-04_004_update_run_00_pending_values.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/daily_notes/2026-06-04.md`
- 다음: Y에게 firmware 정보를 확인하고, vision calibration/homography file 정보를 받으면 Run 00 문서에 반영한다.

## Entry 006
- 무엇을: Windows 환경에서 C-pre 비전 카메라 체크를 수행하기 위한 권장 방식을 `fulltext.md`에 반영했다.
- 왜: WSL에서 webcam이 `/dev/video*`로 자동 노출되지 않을 수 있으므로, repo는 WSL을 SoT로 유지하고 camera I/O만 Windows Python에서 수행하는 구성이 더 안전하기 때문이다.
- 어떻게: Windows Python은 OpenCV camera open, ArUco ID detection, platform marker pixel CSV 저장까지만 수행하도록 정리했다. C-pre pixel CSV는 `vision_x/y` 대신 `marker_px/marker_py`를 쓰고, `data/vision/raw/vision_pixel_<run_id>.csv`로 저장하도록 기록했다. WSL repo 접근 경로 예시는 `\\wsl$\<distro>\home\lemon56789\delta_robot\data\vision\raw`로 추가했다.
- 결과: Windows 쪽 Codex가 repo를 별도 복제하지 않고 C-pre 비전 확인만 수행할 수 있는 기준이 생겼다.
- 변경 파일: `docs/plans/2026-06-04_005_update_fulltext_windows_vision_cpre.md`, `fulltext.md`, `docs/daily_notes/2026-06-04.md`
- 다음: Windows 환경에서 camera open, ArUco marker 검출, pixel CSV 저장을 순서대로 확인한다.
