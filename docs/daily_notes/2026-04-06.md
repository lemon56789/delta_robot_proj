# Daily Note - 2026-04-06

## Entry 001
- 무엇을:
  - `docs/plans/2026-04-06_001_vision_tracking_ground_truth.md` 계획서를 작성했다.
  - `docs/vision_tracking.md` 문서를 신규 작성해 외부 비전 시스템의 역할과 범위를 정리했다.
- 왜:
  - 프로젝트의 최종 운영 목표는 비전 없는 가상센싱 기반 보정이지만, 학습 및 검증 단계에서는 외부 ground-truth 측정계가 필요했기 때문이다.
  - 기존 시스템 계약과 충돌 없이 비전 시스템의 역할을 분리 정의할 문서가 필요했다.
- 어떻게:
  - 비전 시스템을 운영 센서가 아닌 `학습/검증용 외부 측정계`로 명시했다.
  - 측정 범위를 `XY ground-truth`로 한정하고, 단일 top-view 웹캠 구성에서는 `Z`를 직접 제공하지 않는다고 정리했다.
  - 메인 CSV 계약은 유지하고 비전 데이터는 별도 로그 또는 후처리 병합 대상으로 문서화했다.
- 결과:
  - 비전 시스템이 현재 프로젝트의 운영 경로를 바꾸지 않으면서도 학습 라벨과 검증 기준을 제공하는 보조 측정계로 정리되었다.
  - 좌표계, calibration, timestamp alignment, limitation 항목이 함께 정의되어 후속 구현 논의의 기준점이 생겼다.
- 변경 파일:
  - `docs/plans/2026-04-06_001_vision_tracking_ground_truth.md`
  - `docs/vision_tracking.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - 필요하면 `docs/system_data_flow.md`에서 외부 ground-truth 측정계 참조 위치를 최소 범위로 추가 검토한다.
  - vision log의 저장 경로와 후처리 병합 규칙을 후속 문서나 계획서로 구체화한다.

## Entry 002
- 무엇을:
  - `docs/plans/2026-04-06_002_vision_log_storage_policy.md` 계획서를 작성했다.
  - `docs/vision_tracking.md`에 비전 로그 저장 경로와 파일명 규칙을 추가했다.
- 왜:
  - 외부 비전 측정계를 문서화했더라도 실제 raw 로그를 어디에 어떻게 저장할지 정해지지 않으면 후속 구현과 실험 기록이 일관되지 않기 때문이다.
  - 현재 단계에서는 데이터 구조를 과도하게 확정하기보다 raw 원본을 먼저 보존하는 정책이 더 안전하기 때문이다.
- 어떻게:
  - 비전 로그를 메인 CSV와 분리하고 `data/vision/raw/` 경로에 저장하는 기준을 문서에 반영했다.
  - `run_id` 기반 파일명 규칙과 최소 CSV 필드를 정리하고, 메인 로그와의 결합은 후처리에서 수행한다고 명시했다.
- 결과:
  - 비전 로그 저장 위치의 초기 기준이 `data/vision/raw/`로 고정되었다.
  - 아직 가공 데이터 구조를 확정하지 않은 상태에서도 raw 데이터 수집을 바로 시작할 수 있는 문서 기준이 생겼다.
- 변경 파일:
  - `docs/plans/2026-04-06_002_vision_log_storage_policy.md`
  - `docs/vision_tracking.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - 필요하면 `data/vision/raw/` 디렉터리 자체를 생성하고 README 또는 data 규칙 문서에 반영한다.
  - 후속 계획서에서 processed 데이터와 병합 결과 저장 구조를 별도로 정의한다.

## Entry 003
- 무엇을:
  - `docs/plans/2026-04-06_003_readme_vision_tracking_note.md` 계획서를 작성했다.
  - `README.md`에 외부 비전 기반 `XY ground-truth` 측정계 관련 최소 설명 문구를 추가했다.
- 왜:
  - 비전 시스템의 상세 내용은 `docs/vision_tracking.md`에 두되, README 수준에서도 해당 장치가 학습 및 검증 단계의 외부 측정계라는 점은 간단히 드러낼 필요가 있었기 때문이다.
- 어떻게:
  - `4단계: 가상센싱` 설명 아래에 초기 학습 및 검증 단계에서의 외부 비전 사용과 운영 단계에서의 제거 원칙을 한 문장으로 반영했다.
- 결과:
  - README를 읽는 사람도 비전 시스템이 상시 운영 센서가 아니라 초기 ground-truth 확보용 장치라는 점을 바로 이해할 수 있게 되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_003_readme_vision_tracking_note.md`
  - `README.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - 필요하면 `docs/system_data_flow.md`에도 외부 ground-truth 측정계 참조를 최소 범위로 연결한다.

## Entry 004
- 무엇을:
  - `docs/plans/2026-04-06_004_vision_raw_directory_and_repo_structure.md` 계획서를 작성했다.
  - `data/vision/raw/` 디렉터리 기준을 문서와 리포 구조에 반영했다.
- 왜:
  - 비전 raw 로그 저장 정책이 문서에만 있고 실제 리포 구조에 경로가 없으면 이후 수집 코드와 실험 기록이 일관되게 쌓이지 않기 때문이다.
- 어떻게:
  - 비전 raw 경로를 `data/vision/raw/`로 고정하고, README의 폴더 구조와 `docs/vision_tracking.md`의 저장 경로 설명에 같은 경로를 반영했다.
- 결과:
  - 비전 raw 로그 저장 경로가 실제 리포 구조와 문서 기준에서 동일하게 정렬되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_004_vision_raw_directory_and_repo_structure.md`
  - `README.md`
  - `docs/vision_tracking.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - `data/vision/raw/` 아래 샘플 파일명 규칙과 향후 processed 데이터 경로를 별도 정책으로 이어서 정의한다.

## Entry 005
- 무엇을:
  - `docs/plans/2026-04-06_005_reference_directory_for_ik_papers.md` 계획서를 작성했다.
  - `docs/references/README.md`를 추가해 참고 논문 보관 경로 기준을 만들었다.
- 왜:
  - IK 관련 논문을 프로젝트 채택안으로 고정하지 않고도 리포 안에서 reference로 관리할 위치가 필요했기 때문이다.
- 어떻게:
  - `docs/references/`를 참고 자료 전용 경로로 정의하고, 해당 자료가 Source of Truth나 공식 채택안을 의미하지 않는다고 명시했다.
  - 파일명은 `year_author_short-title` 형태를 권장해 검색성을 확보하도록 정리했다.
- 결과:
  - IK 관련 논문과 기타 외부 자료를 프로젝트 기준 문서와 분리해 보관할 수 있는 기준 경로가 생겼다.
- 변경 파일:
  - `docs/plans/2026-04-06_005_reference_directory_for_ik_papers.md`
  - `docs/references/README.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - 실제 IK 참고 논문 파일을 `docs/references/`에 추가하고, 필요하면 해당 논문 제목만 후속 문서에서 참조한다.

## Entry 006
- 무엇을:
  - `docs/plans/2026-04-06_006_ik_structure_note_and_variable_mapping.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`를 신규 작성해 `2atan(t)` 기반 IK 구조 메모와 프로젝트 변수 매핑을 정리했다.
- 왜:
  - IK 구현 전에 reference 기반 수식 구조와 현재 프로젝트 좌표계 및 기구 변수 사이의 연결을 먼저 맞춰둘 필요가 있었기 때문이다.
- 어떻게:
  - 길이 제약식, `E_i cos(theta_i) + F_i sin(theta_i) + G_i = 0` 형태, 반각 치환, `theta_i = 2 atan(t_i)` 복원 흐름을 단계별로 정리했다.
  - `B1~B3`, `P1~P3`, `L`, `l`, `target_x/y/z`, `theta1~theta3`가 논문식 arm index 구조와 어떻게 연결되는지 문서로 묶었다.
  - 아직 확정되지 않은 `E_i`, `F_i`, `G_i` 최종 식과 해 선택 규칙은 미정 항목으로 분리했다.
- 결과:
  - `2atan(t)` 기반 IK를 프로젝트 변수 체계로 해석하기 위한 최소 공통 메모가 생겼다.
  - 후속 구현은 이제 `theta` 기준 정의와 `E/F/G` 유도 단계로 바로 이어갈 수 있게 되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_006_ik_structure_note_and_variable_mapping.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - `theta_i`의 기준 자세와 부호를 먼저 정의하고, arm 1 기준으로 `E_1`, `F_1`, `G_1`을 유도한다.

## Entry 007
- 무엇을:
  - `docs/plans/2026-04-06_007_theta_definition_and_joint_notation.md` 계획서를 작성했다.
  - `docs/system_data_flow.md`와 `docs/ik_structure_note.md`에 `theta_i` 정의와 `J_i` 보조기호 설명을 반영했다.
- 왜:
  - `theta_i`의 기준 자세와 양의 방향이 정해져야 `J_i(theta_i)` 좌표식과 후속 `E/F/G` 유도가 의미를 갖기 때문이다.
- 어떻게:
  - `theta_i = 0 deg`를 upper arm이 `base plane`에 놓인 자세로, `theta_i = +90 deg`를 upper arm이 `-z` 방향과 평행한 자세로 문서에 명시했다.
  - `J_i`를 upper arm 끝점이자 parallelogram link와 연결되는 elbow joint로 정의하고, arm 1 기준 메모 좌표식 `J_1(theta_1) = (0, -wB + L cos(theta_1), -L sin(theta_1))`를 추가했다.
- 결과:
  - IK 각도 정의가 SoT notes 수준에서 더 분명해졌고, arm 1 기준 좌표 전개를 시작할 수 있는 최소 출발점이 생겼다.
- 변경 파일:
  - `docs/plans/2026-04-06_007_theta_definition_and_joint_notation.md`
  - `docs/system_data_flow.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - `P_1`의 프로젝트 좌표식을 정하고, `|P_1 - J_1|^2 = l^2`에서 `E_1`, `F_1`, `G_1`을 유도한다.

## Entry 008
- 무엇을:
  - `docs/plans/2026-04-06_008_theta_outward_direction_fix.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`의 arm 1 방향 해석과 `J_1(theta_1)` 좌표식을 수정했다.
- 왜:
  - 기존 메모에서는 `theta_1 = 0 deg`가 원점 `O` 방향을 향하는 것으로 적혀 있었지만, 실제 합의 기준은 그 반대방향이어야 했기 때문이다.
- 어떻게:
  - arm 1의 base plane 수평 기준을 `outward = -y`로 수정하고, `J_1(theta_1)`를 `J_1(theta_1) = (0, -wB - L cos(theta_1), -L sin(theta_1))`로 바로잡았다.
- 결과:
  - `theta_1 = 0 deg`일 때 arm 1 upper arm이 원점 반대방향을 향하고, `theta_1 = +90 deg`일 때 `-z` 방향과 평행해지는 정의가 문서 식과 일치하게 되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_008_theta_outward_direction_fix.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - 수정된 `J_1(theta_1)`를 기준으로 `P_1` 좌표식과 `|P_1 - J_1|^2 = l^2` 전개를 이어간다.

## Entry 009
- 무엇을:
  - `docs/plans/2026-04-06_009_p1_coordinate_note.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에 `P_1` 좌표식과 arm 1 길이 제약식 출발점을 추가했다.
- 왜:
  - `J_1(theta_1)`만 있어서는 arm 1 IK 전개를 닫을 수 없고, platform 연결점 `P_1`의 프로젝트 좌표가 함께 있어야 `|P_1 - J_1|^2 = l^2`를 전개할 수 있기 때문이다.
- 어떻게:
  - platform center `(x, y, z)` 기준에서 `P_1`을 `-y` 방향 vertex로 두고 `P_1 = (x, y - uP, z)`로 정리했다.
  - 참고 수준으로 `P_2`, `P_3` 대칭 좌표도 함께 적고, `P_1`과 `J_1(theta_1)`를 바로 연결한 길이 제약식 형태를 메모에 추가했다.
- 결과:
  - arm 1에 대해 `P_1`과 `J_1`이 같은 좌표계에서 연결되었고, 다음 단계인 `E_1`, `F_1`, `G_1` 전개 준비가 되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_009_p1_coordinate_note.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - `|(x, y - uP, z) - (0, -wB - L cos(theta_1), -L sin(theta_1))|^2 = l^2`를 전개해 arm 1의 `E_1`, `F_1`, `G_1` 형태를 도출한다.

## Entry 010
- 무엇을:
  - `docs/plans/2026-04-06_010_clarify_sb_sp_side_length.md` 계획서를 작성했다.
  - `docs/system_data_flow.md`와 `docs/ik_structure_note.md`에서 `sB`, `sP` 의미를 더 명확히 정리했다.
- 왜:
  - `sB`, `sP`가 각각 base/platform 삼각형 자체를 뜻하는 것처럼 읽힐 수 있어서, 실제로는 각 정삼각형의 변 길이임을 직접 명시할 필요가 있었기 때문이다.
- 어떻게:
  - `sB`는 base triangle side length, `sP`는 platform triangle side length라고 문서에 직접 써 넣고, `uB/uP`, `wB/wP`와 함께 대응 관계가 보이도록 정리했다.
- 결과:
  - 기구 변수 해석에서 `sB`, `sP`의 의미가 더 분명해졌다.
- 변경 파일:
  - `docs/plans/2026-04-06_010_clarify_sb_sp_side_length.md`
  - `docs/system_data_flow.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - arm 1 길이 제약식을 전개해 `E_1`, `F_1`, `G_1` 형태로 정리한다.

## Entry 011
- 무엇을:
  - `docs/plans/2026-04-06_011_unify_b_wb_p_up_notation.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에서 `B_i`는 `wB`, `P_i`는 `uP` 기준으로만 표현하도록 통일했다.
- 왜:
  - `sB/sP`와 `wB/uP`를 섞어 쓰면 점 좌표식이 직관적으로 읽히지 않고, 전개 과정에서 혼동이 커지기 때문이다.
- 어떻게:
  - `B_i`는 base side center라는 점에 맞춰 `wB` 기준으로 두고, `P_i`는 platform vertex라는 점에 맞춰 `uP` 기준으로 정리했다.
  - `P_2`, `P_3` 좌표는 `sP/2`, `wP` 표현 대신 `uP`만 쓰는 대칭식으로 바꿨다.
- 결과:
  - IK 메모 문서에서 base/platform 점 좌표식이 더 일관된 형태로 정리되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_011_unify_b_wb_p_up_notation.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - `B_i`, `P_i`, `J_i`를 같은 기준으로 두고 arm 1 길이 제약식을 전개해 `E_1`, `F_1`, `G_1` 형태를 도출한다.

## Entry 012
- 무엇을:
  - `docs/plans/2026-04-06_012_expand_arm1_efg.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에 arm 1 길이 제약식 전개와 `E_1`, `F_1`, `G_1` 도출 과정을 추가했다.
- 왜:
  - `2atan(t)` 기반 IK를 실제 프로젝트 변수로 쓰려면 arm 1에서 `E cos(theta) + F sin(theta) + G = 0` 형태가 실제로 나오는지 먼저 닫아야 하기 때문이다.
- 어떻게:
  - `P_1 = (x, y - uP, z)`와 `J_1(theta_1) = (0, -wB - L cos(theta_1), -L sin(theta_1))`를 사용해 `|P_1 - J_1|^2 = l^2`를 전개했다.
  - 중간 변수 `A_1 = y - uP + wB`를 두고 정리해 `E_1 = 2L(y - uP + wB)`, `F_1 = 2Lz`, `G_1 = x^2 + (y - uP + wB)^2 + z^2 + L^2 - l^2`를 얻었다.
- 결과:
  - arm 1에 대해 `E_1 cos(theta_1) + F_1 sin(theta_1) + G_1 = 0` 형태가 프로젝트 변수 기준으로 정리되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_012_expand_arm1_efg.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - arm 1 식에 `t = tan(theta_1 / 2)` 치환을 적용하고, 이후 arm 2와 arm 3로 대칭 확장한다.

## Entry 013
- 무엇을:
  - `docs/plans/2026-04-06_013_arm1_half_angle_form.md` 계획서를 작성했다.
  - `docs/ik_structure_note.md`에 arm 1 반각 치환과 `t_1`, `theta_1` 복원식을 추가했다.
- 왜:
  - 현재 프로젝트가 참고하는 `2atan(t)` 구조를 실제 arm 1 식까지 연결해 둬야 후속 대칭 확장과 구현 준비가 쉬워지기 때문이다.
- 어떻게:
  - `t_1 = tan(theta_1 / 2)`를 두고 `cos(theta_1)`, `sin(theta_1)`를 `t_1`로 치환한 뒤 2차식으로 정리했다.
  - 최종적으로 `t_1 = (-F_1 ± sqrt(E_1^2 + F_1^2 - G_1^2)) / (G_1 - E_1)`와 `theta_1 = 2 atan(t_1)`를 문서에 반영했다.
- 결과:
  - arm 1에 대해 `2atan(t)` 기반 해 구조가 프로젝트 변수 기준으로 문서에 연결되었다.
- 변경 파일:
  - `docs/plans/2026-04-06_013_arm1_half_angle_form.md`
  - `docs/ik_structure_note.md`
  - `docs/daily_notes/2026-04-06.md`
- 다음:
  - arm 2와 arm 3를 대칭 확장하거나, 먼저 `±` 해 선택 규칙과 reject 조건을 정리한다.
