# Daily Note - 2026-06-06

## Entry 001
- 무엇을: `data/real/raw`에 남아 있던 Run 01-pre square dry-run 산출물 삭제 작업을 기록했다.
- 왜: `square_pm20`과 `square_pm40`의 main CSV/JSON/TXT가 `"dry_run": true`인 검증용 파일인데 실제 run 데이터와 이름이 비슷해 혼동될 수 있기 때문이다.
- 어떻게: 삭제 대상은 JSON metadata의 `"dry_run": true` 여부로만 선정했다. 실제 run으로 확인된 static/cross real raw와 vision raw 파일은 삭제하지 않는다.
- 결과: 삭제 실행 후 해당 dry-run 파일이 목록에서 사라졌는지 확인한다.
- 변경 파일: `docs/plans/2026-06-06_001_remove_dry_run_real_raw_files.md`, `docs/daily_notes/2026-06-06.md`, `data/real/raw/main_2026-06-05_run01_pre_square_pm20_r01.csv`, `data/real/raw/main_2026-06-05_run01_pre_square_pm20_r01.json`, `data/real/raw/serial_2026-06-05_run01_pre_square_pm20_r01.txt`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.csv`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`, `data/real/raw/serial_2026-06-05_run01_pre_square_pm40_r01.txt`
- 다음: 실제 `square_pm40` main CSV/JSON/TXT를 다시 받아오면 `"dry_run": false`와 serial RX 응답 포함 여부를 먼저 확인한다.

## Entry 002
- 무엇을: Run 01-pre `cross_pm40`과 `square_pm40` main metadata의 trajectory 값을 정정하고, Arduino/controller position gain `1.25` 적용 범위를 run별로 기록했다.
- 왜: 새로 받은 real/raw JSON은 실제 run이지만 `cross_pm40`은 `"trajectory": "cross_pm10_pre"`, `square_pm40`은 `"trajectory": "square_pm10_pre"`로 잘못 적혀 있었다. 또한 `cross_pm40`은 gain `1.25` 적용 전 데이터이고, `square_pm40`만 gain `1.25` 적용 후 데이터라 같은 hardware calibration condition으로 해석하면 안 되기 때문이다.
- 어떻게: `data/real/raw/main_2026-06-05_run01_pre_cross_pm40_r01.json`의 trajectory 값을 `cross_pm40_pre`로, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`의 trajectory 값을 `square_pm40_pre`로 정정했다. Run 01 문서에는 `cross_pm40`은 gain 적용 전, `square_pm40`은 gain 적용 후 데이터라고 명시했다. CSV 구조와 raw row 값은 수정하지 않았다.
- 결과: cross/square metadata label이 실제 `±40 mm` target과 일치한다. Run 01-pre acceptance 판단에서는 cross의 큰 추종 오차를 gain 적용 전 하드웨어 조건으로 분리해서 해석해야 한다.
- 변경 파일: `docs/plans/2026-06-06_002_correct_square_metadata_and_gain_scope.md`, `data/real/raw/main_2026-06-05_run01_pre_cross_pm40_r01.json`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: static/cross/square 각 run이 Run 01-pre 합격 기준을 만족하는지 main/vision raw 기준으로 확인한다.

## Entry 003
- 무엇을: Run 01-pre static/cross/square raw log가 기본 합격 기준을 만족하는지 확인했다.
- 왜: Run 01-main으로 넘어가기 전에 main log, vision log, run_id, timestamp, marker/valid ratio, theta command/measurement field가 기본 기준을 만족하는지 판단해야 하기 때문이다.
- 어떻게: `data/real/raw/main_<run_id>.csv`, `data/real/raw/main_<run_id>.json`, `data/vision/raw/vision_<run_id>.csv`를 읽어 row 수, timestamp 단조 증가, run_id 일치, vision column/value 존재, marker_detected ratio, valid ratio, theta field 존재 여부를 계산했다.
- 결과: `static_center`는 marker/valid ratio `100.00%`로 기본 합격. `cross_pm40`은 marker/valid ratio `92.98%`, `square_pm40`은 `93.21%`로 `>=95%` 기준에는 미달한다. valid/detected row에는 `vision_x/y`가 모두 기록되어 있다. cross의 위치 오차는 gain 적용 전 데이터 조건으로 별도 해석한다.
- 변경 파일: `docs/daily_notes/2026-06-06.md`
- 다음: cross/square를 Run 01-pre pass로 인정하려면 marker/valid ratio 기준을 완화하는 명시적 rejection/acceptance note가 필요하고, 엄격 기준을 유지하면 rerun이 필요하다.

## Entry 004
- 무엇을: Run 01-pre static/cross/square에 대해 angle-derived measured position CSV와 processed merged dataset을 생성했다.
- 왜: Simscape output 생성 이후 단계인 angle-derived measured position 생성, merged dataset 생성, `error_x/y/z` label 생성 확인까지 end-to-end pipeline validation을 진행하기 위해서다.
- 어떻게: `experiments/run01_preprocess.py`를 추가해 main CSV, vision CSV, Simscape CSV를 읽고, `theta*_meas` 기반 FK로 `measured_position_<run_id>.csv`를 생성했다. vision `vision_time`은 각 vision 로그 시작 시각을 기준으로 상대 ms로 바꾼 뒤 main 첫 timestamp에 맞춰 provisional post-alignment했다. merged CSV에서는 fixed 16-column contract를 유지하고 `error_x = measured_x_vision - sim_x`, `error_y = measured_y_vision - sim_y`, `error_z = measured_z_est - sim_z`를 계산했다.
- 결과: `data/real/derived/measured_position_2026-06-05_run01_pre_static_center_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_square_pm40_r01.csv`와 `data/processed/merged_2026-06-05_run01_pre_static_center_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_square_pm40_r01.csv`가 생성되었다. merged row 수는 static `299`, cross `450`, square `350`이고, `virtual_sensor/check_dataset.py` 검증에서 모두 `has_nan=False`를 확인했다.
- 변경 파일: `docs/plans/2026-06-06_003_run01_pre_alignment_merge.md`, `experiments/run01_preprocess.py`, `data/real/derived/measured_position_2026-06-05_run01_pre_static_center_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_square_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_static_center_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_square_pm40_r01.csv`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-pre acceptance 판단에서는 cross/square marker valid ratio 미달과 `theta*_meas = command_echo_no_encoder` 한계를 별도 note로 남긴 뒤, strict 기준 유지 시 rerun 여부를 결정한다.

## Entry 005
- 무엇을: Stage 8 초기 virtual sensor 학습 방법을 PyTorch neural network가 아니라 linear regression/Ridge regression baseline으로 문서화했다.
- 왜: 현재 남은 시간과 데이터 규모를 고려하면 신경망보다 설명 가능하고 구현 부담이 낮은 regression baseline이 먼저 필요하기 때문이다.
- 어떻게: Run 01 문서에 초기 모델 정책을 추가하고, roadmap과 `virtual_sensor/README.md`에 첫 모델은 linear/Ridge, PyTorch MLP는 future extension이라고 기록했다. `fulltext.md`와 `README.md`의 PyTorch 표기도 future optional로 낮췄다. Ridge alpha와 feature 선택은 validation split에서만 결정하고 Run 01-holdout은 fitting/tuning에 쓰지 않는다고 명시했다.
- 결과: Stage 8 첫 구현 방향이 `theta*_cmd`, `theta*_meas`, `sim_x/y/z` feature에서 processed `error_x/y/z`를 예측하는 linear/Ridge baseline으로 고정되었다. `error_x/y`는 주 평가 대상, `error_z`는 angle-derived Z 기반 보조/diagnostic target으로 해석한다.
- 변경 파일: `docs/plans/2026-06-06_004_linear_ridge_virtual_sensor_baseline.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/roadmap.md`, `virtual_sensor/README.md`, `README.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main processed dataset이 확보되면 linear/Ridge 학습 스크립트와 RMSE/MAE/max error 평가 리포트 생성을 계획한다.

## Entry 006
- 무엇을: Run 01-main과 Run 01-holdout의 실행 순서와 기존 코드 재사용 범위를 보고용으로 정리했다.
- 왜: Run 01-pre pipeline validation이 완료되었고, 다음 실험에서 어떤 순서로 main/holdout 데이터를 수집하고 학습/평가로 연결할지 팀에 바로 공유해야 하기 때문이다.
- 어떻게: 현재 `experiments/run01_main_logger.py`는 pre trajectory만 지원하므로 먼저 main/holdout trajectory를 추가해야 한다고 정리했다. 이후 main/vision raw 수집, Simscape output 생성, `experiments/run01_preprocess.py`를 이용한 measured/merged CSV 생성, `virtual_sensor/check_dataset.py` 검증, linear/Ridge 학습, holdout 평가 순서로 기록했다.
- 결과: 다음 진행 순서는 아래와 같다.
  1. `run01_main_logger.py`에 `static_center_hold`, `cross_pm20`, `square_pm20`, `circle_r40`, `grid_3x3_pm40`, `cross_pm15_holdout`, `square_pm15_holdout`, `circle_r40_holdout` trajectory 지원을 추가한다.
  2. Run 01-main을 correction off, 동일 gain/camera/marker/calibration 조건으로 수집한다.
  3. 각 `run_id`에 대해 같은 target/command sequence 기준 Simscape CSV를 `data/simulation/raw/simscape_<run_id>.csv`로 생성한다.
  4. `python experiments/run01_preprocess.py --run-id <run_id>`로 `data/real/derived/measured_position_<run_id>.csv`와 `data/processed/merged_<run_id>.csv`를 생성한다.
  5. `python virtual_sensor/check_dataset.py data/processed/merged_<run_id>.csv`로 16컬럼, NaN 없음, timestamp 단조 증가, marker valid ratio를 확인한다.
  6. Run 01-main processed CSV를 training/validation으로 나누고, validation에서만 Ridge alpha와 feature 선택을 결정한다.
  7. Run 01-holdout도 같은 수집/Simscape/merge/check 절차로 처리하되, fitting/tuning에는 절대 사용하지 않는다.
  8. 최종 보고에서는 Run 01-holdout merged CSV로 linear/Ridge 모델의 일반화 성능을 평가한다.
- 변경 파일: `docs/plans/2026-06-06_005_run01_main_holdout_execution_order.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main/holdout trajectory 지원 추가 계획서를 작성하고 승인 후 `experiments/run01_main_logger.py`를 확장한다.

## Entry 007
- 무엇을: Run 01-pre 완료와 linear/Ridge baseline 결정 이후 최신 진행 상태를 주요 문서와 `fulltext.md`에 반영했다.
- 왜: 문서 일부가 아직 2026-06-01/2026-06-04 기준 상태, Run 00 준비 상태, processed 경로 미정 상태로 남아 있어 현재 보고 및 후속 작업 판단과 맞지 않았기 때문이다.
- 어떻게: `docs/roadmap.md`의 current status snapshot과 immediate priorities를 2026-06-06 기준으로 갱신했다. `docs/measured_data_structure.md`에는 `data/real/raw`, `data/real/derived`, `data/simulation/raw`, `data/processed` 경로와 `experiments/run01_preprocess.py` 현 구현 상태를 반영했다. Run 01 문서에는 Run 01-pre pipeline validation 완료 상태와 limitations를 추가했다. `experiments/README.md`, `data/README.md`, `fulltext.md`에는 Run 01-pre 산출물, processed dataset naming, 다음 Run 01-main/holdout 진행 순서를 정리했다.
- 결과: 문서 기준 현재 상태가 Run 00 통과, Run 01-pre pipeline validation 완료, Run 01-main 준비 중, 첫 virtual sensor model은 linear/Ridge baseline으로 정렬되었다. Run 01-pre artifact는 final training dataset이 아니라 pipeline validation용이라는 한계도 함께 남겼다.
- 변경 파일: `docs/plans/2026-06-06_006_update_docs_after_run01_pre.md`, `docs/roadmap.md`, `docs/measured_data_structure.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `experiments/README.md`, `data/README.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main/holdout trajectory 지원 추가 계획서를 작성하고 승인 후 logger를 확장한다.

## Entry 008
- 무엇을: 실제 수행 내용을 기준으로 팀원 역할 문서를 갱신했다.
- 왜: 기존 문서에는 S가 동역학/진동 분석, T가 기구 설계, N이 구조/응력 해석 중심으로 기록되어 있었지만, 실제 진행에서는 S/T/N이 하드웨어 제작/조립을 수행했고 S는 팀장 역할, N은 프로파일 주문제작 요청, Y는 Arduino 제어와 함께 회로 구성/배선 및 전원/구동계 연결을 담당했기 때문이다. 동역학/진동/구조/응력 해석은 기획 단계 역할로는 맞지만 시간상 실제 주 산출물로 과장하면 안 되기 때문이다.
- 어떻게: `README.md`에는 짧은 팀원 역할 표를 반영했다. `docs/member_profiles/S.md`, `T.md`, `N.md`, `Y.md`와 `docs/member_profiles/README.md`에는 상세 역할을 반영했다. `docs/system_data_flow.md`의 owner mapping, `docs/hardware_experiment_base_config.md`의 하드웨어/전원/안전 담당자, Run 00/Run 01 protocol의 responsible members, `fulltext.md`의 팀 역할 요약도 같은 기준으로 맞췄다.
- 결과: 역할 문서가 S=팀장/하드웨어 제작, L=시스템 통합/가상센싱/데이터/비전, Y=Arduino 제어/회로/배선/전원 연결, T=하드웨어 제작/기구 설계, N=하드웨어 제작/프로파일 주문제작/기획 단계 구조해석으로 정렬되었다. 기존 해석 역할은 삭제하지 않고 planning-stage scope로 유지했다.
- 변경 파일: `docs/plans/2026-06-06_007_update_team_roles_after_hardware_build.md`, `README.md`, `docs/member_profiles/README.md`, `docs/member_profiles/S.md`, `docs/member_profiles/T.md`, `docs/member_profiles/N.md`, `docs/member_profiles/Y.md`, `docs/system_data_flow.md`, `docs/hardware_experiment_base_config.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 발표 자료 또는 보고서 작성 시 README의 짧은 역할 표를 기본으로 쓰고, 상세 역할 설명은 `fulltext.md`와 `docs/member_profiles/`를 참조한다.

## Entry 009
- 무엇을: `experiments/run01_main_logger.py`에 Run 01-main/holdout trajectory 지원을 추가하고, circle/grid 정의를 최신 실험 지시에 맞게 수정했다.
- 왜: Run 01-main 수집을 시작하려면 pre 전용 logger를 main/holdout trajectory까지 실행할 수 있어야 하며, circle은 반지름 `40 mm` 반시계 방향 원, grid는 `40 mm` 간격 3x3 경로로 확정되었기 때문이다.
- 어떻게: logger에 `static_center_hold`, `cross_pm20`, `square_pm20`, `circle_r40`, `grid_3x3_pm40`, `cross_pm15_holdout`, `square_pm15_holdout`, `circle_r40_holdout` 선택지를 추가했다. `circle_r40`은 home -> `(40, 0)` -> 반시계 방향 `r=40 mm` 원 -> home 순서로 setpoint를 생성하며, 전체 회전 시간은 `--circle-duration-s`로 지정해 metadata에 남기게 했다. `grid_3x3_pm40`은 home -> `(-40,-40)` -> `(0,-40)` -> `(40,-40)` -> `(-40,0)` -> `(0,0)` -> `(40,0)` -> `(-40,40)` -> `(0,40)` -> `(40,40)` -> home 순서로 구현했다.
- 결과: Run 01-main/holdout logger dry-run과 Python compile 검증을 진행할 수 있는 상태가 되었다. Arduino protocol, raw main CSV field, processed CSV contract는 변경하지 않았다.
- 변경 파일: `docs/plans/2026-06-06_008_extend_run01_main_logger_trajectories.md`, `experiments/run01_main_logger.py`, `experiments/README.md`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: dry-run으로 대표 trajectory row 생성과 IK theta range를 확인하고, 실제 Run 01-main 수집 시 `circle-duration-s` 값을 metadata에 기록한다.
