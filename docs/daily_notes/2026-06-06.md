# Daily Note - 2026-06-06

## Entry 001
- 무엇을: `data/real/raw`에 남아 있던 Run 01-pre square dry-run 산출물 삭제 작업을 기록했다.
- 왜: `square_pm20`과 `square_pm40`의 main CSV/JSON/TXT가 `"dry_run": true`인 검증용 파일인데 실제 run 데이터와 이름이 비슷해 혼동될 수 있기 때문이다.
- 어떻게: 삭제 대상은 JSON metadata의 `"dry_run": true` 여부로만 선정했다. 실제 run으로 확인된 static/cross real raw와 vision raw 파일은 삭제하지 않는다.
- 결과: 삭제 실행 후 해당 dry-run 파일이 목록에서 사라졌는지 확인한다.
- 변경 파일: `docs/plans/2026-06-06_001_remove_dry_run_real_raw_files.md`, `docs/daily_notes/2026-06-06.md`, `data/real/raw/main_2026-06-05_run01_pre_square_pm20_r01.csv`, `data/real/raw/main_2026-06-05_run01_pre_square_pm20_r01.json`, `data/real/raw/serial_2026-06-05_run01_pre_square_pm20_r01.txt`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.csv`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`, `data/real/raw/serial_2026-06-05_run01_pre_square_pm40_r01.txt`
- 다음: 실제 `square_pm40` main CSV/JSON/TXT를 다시 받아오면 `"dry_run": false`와 serial RX 응답 포함 여부를 먼저 확인한다.

## Entry 002
- 무엇을: Run 01-pre `cross_pm40`과 `square_pm40` main metadata의 trajectory 값을 정정하고, Arduino/controller position gain `1.25` 적용 범위를 run별로 기록했다.
- 왜: 새로 받은 real/raw JSON은 실제 run이지만 `cross_pm40`은 `"trajectory": "cross_pm10_pre"`, `square_pm40`은 `"trajectory": "square_pm10_pre"`로 잘못 적혀 있었다. 또한 `cross_pm40`은 gain `1.25` 적용 전 데이터이고, `square_pm40`만 gain `1.25` 적용 후 데이터라 같은 hardware calibration condition으로 해석하면 안 되기 때문이다.
- 어떻게: `data/real/raw/main_2026-06-05_run01_pre_cross_pm40_r01.json`의 trajectory 값을 `cross_pm40_pre`로, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`의 trajectory 값을 `square_pm40_pre`로 정정했다. Run 01 문서에는 `cross_pm40`은 gain 적용 전, `square_pm40`은 gain 적용 후 데이터라고 명시했다. CSV 구조와 raw row 값은 수정하지 않았다.
- 결과: cross/square metadata label이 실제 `±40 mm` target과 일치한다. Run 01-pre acceptance 판단에서는 cross의 큰 추종 오차를 gain 적용 전 하드웨어 조건으로 분리해서 해석해야 한다.
- 변경 파일: `docs/plans/2026-06-06_002_correct_square_metadata_and_gain_scope.md`, `data/real/raw/main_2026-06-05_run01_pre_cross_pm40_r01.json`, `data/real/raw/main_2026-06-05_run01_pre_square_pm40_r01.json`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: static/cross/square 각 run이 Run 01-pre 합격 기준을 만족하는지 main/vision raw 기준으로 확인한다.

## Entry 003
- 무엇을: Run 01-pre static/cross/square raw log가 기본 합격 기준을 만족하는지 확인했다.
- 왜: Run 01-main으로 넘어가기 전에 main log, vision log, run_id, timestamp, marker/valid ratio, theta command/measurement field가 기본 기준을 만족하는지 판단해야 하기 때문이다.
- 어떻게: `data/real/raw/main_<run_id>.csv`, `data/real/raw/main_<run_id>.json`, `data/vision/raw/vision_<run_id>.csv`를 읽어 row 수, timestamp 단조 증가, run_id 일치, vision column/value 존재, marker_detected ratio, valid ratio, theta field 존재 여부를 계산했다.
- 결과: `static_center`는 marker/valid ratio `100.00%`로 기본 합격. `cross_pm40`은 marker/valid ratio `92.98%`, `square_pm40`은 `93.21%`로 `>=95%` 기준에는 미달한다. valid/detected row에는 `vision_x/y`가 모두 기록되어 있다. cross의 위치 오차는 gain 적용 전 데이터 조건으로 별도 해석한다.
- 변경 파일: `docs/daily_notes/2026-06-06.md`
- 다음: cross/square를 Run 01-pre pass로 인정하려면 marker/valid ratio 기준을 완화하는 명시적 rejection/acceptance note가 필요하고, 엄격 기준을 유지하면 rerun이 필요하다.

## Entry 004
- 무엇을: Run 01-pre static/cross/square에 대해 angle-derived measured position CSV와 processed merged dataset을 생성했다.
- 왜: Simscape output 생성 이후 단계인 angle-derived measured position 생성, merged dataset 생성, `error_x/y/z` label 생성 확인까지 end-to-end pipeline validation을 진행하기 위해서다.
- 어떻게: `experiments/run01_preprocess.py`를 추가해 main CSV, vision CSV, Simscape CSV를 읽고, `theta*_meas` 기반 FK로 `measured_position_<run_id>.csv`를 생성했다. vision `vision_time`은 각 vision 로그 시작 시각을 기준으로 상대 ms로 바꾼 뒤 main 첫 timestamp에 맞춰 provisional post-alignment했다. merged CSV에서는 fixed 16-column contract를 유지하고 `error_x = measured_x_vision - sim_x`, `error_y = measured_y_vision - sim_y`, `error_z = measured_z_est - sim_z`를 계산했다.
- 결과: `data/real/derived/measured_position_2026-06-05_run01_pre_static_center_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_square_pm40_r01.csv`와 `data/processed/merged_2026-06-05_run01_pre_static_center_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_square_pm40_r01.csv`가 생성되었다. merged row 수는 static `299`, cross `450`, square `350`이고, `virtual_sensor/check_dataset.py` 검증에서 모두 `has_nan=False`를 확인했다.
- 변경 파일: `docs/plans/2026-06-06_003_run01_pre_alignment_merge.md`, `experiments/run01_preprocess.py`, `data/real/derived/measured_position_2026-06-05_run01_pre_static_center_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/real/derived/measured_position_2026-06-05_run01_pre_square_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_static_center_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_cross_pm40_r01.csv`, `data/processed/merged_2026-06-05_run01_pre_square_pm40_r01.csv`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-pre acceptance 판단에서는 cross/square marker valid ratio 미달과 `theta*_meas = command_echo_no_encoder` 한계를 별도 note로 남긴 뒤, strict 기준 유지 시 rerun 여부를 결정한다.

## Entry 005
- 무엇을: Stage 8 초기 virtual sensor 학습 방법을 PyTorch neural network가 아니라 linear regression/Ridge regression baseline으로 문서화했다.
- 왜: 현재 남은 시간과 데이터 규모를 고려하면 신경망보다 설명 가능하고 구현 부담이 낮은 regression baseline이 먼저 필요하기 때문이다.
- 어떻게: Run 01 문서에 초기 모델 정책을 추가하고, roadmap과 `virtual_sensor/README.md`에 첫 모델은 linear/Ridge, PyTorch MLP는 future extension이라고 기록했다. `fulltext.md`와 `README.md`의 PyTorch 표기도 future optional로 낮췄다. Ridge alpha와 feature 선택은 validation split에서만 결정하고 Run 01-holdout은 fitting/tuning에 쓰지 않는다고 명시했다.
- 결과: Stage 8 첫 구현 방향이 `theta*_cmd`, `theta*_meas`, `sim_x/y/z` feature에서 processed `error_x/y/z`를 예측하는 linear/Ridge baseline으로 고정되었다. `error_x/y`는 주 평가 대상, `error_z`는 angle-derived Z 기반 보조/diagnostic target으로 해석한다.
- 변경 파일: `docs/plans/2026-06-06_004_linear_ridge_virtual_sensor_baseline.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/roadmap.md`, `virtual_sensor/README.md`, `README.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main processed dataset이 확보되면 linear/Ridge 학습 스크립트와 RMSE/MAE/max error 평가 리포트 생성을 계획한다.

## Entry 006
- 무엇을: Run 01-main과 Run 01-holdout의 실행 순서와 기존 코드 재사용 범위를 보고용으로 정리했다.
- 왜: Run 01-pre pipeline validation이 완료되었고, 다음 실험에서 어떤 순서로 main/holdout 데이터를 수집하고 학습/평가로 연결할지 팀에 바로 공유해야 하기 때문이다.
- 어떻게: 현재 `experiments/run01_main_logger.py`는 pre trajectory만 지원하므로 먼저 main/holdout trajectory를 추가해야 한다고 정리했다. 이후 main/vision raw 수집, Simscape output 생성, `experiments/run01_preprocess.py`를 이용한 measured/merged CSV 생성, `virtual_sensor/check_dataset.py` 검증, linear/Ridge 학습, holdout 평가 순서로 기록했다.
- 결과: 다음 진행 순서는 아래와 같다.
  1. `run01_main_logger.py`에 `static_center_hold`, `cross_pm20`, `square_pm20`, `circle_r40`, `grid_3x3_pm40`, `cross_pm15_holdout`, `square_pm15_holdout`, `circle_r40_holdout` trajectory 지원을 추가한다.
  2. Run 01-main을 correction off, 동일 gain/camera/marker/calibration 조건으로 수집한다.
  3. 각 `run_id`에 대해 같은 target/command sequence 기준 Simscape CSV를 `data/simulation/raw/simscape_<run_id>.csv`로 생성한다.
  4. `python experiments/run01_preprocess.py --run-id <run_id>`로 `data/real/derived/measured_position_<run_id>.csv`와 `data/processed/merged_<run_id>.csv`를 생성한다.
  5. `python virtual_sensor/check_dataset.py data/processed/merged_<run_id>.csv`로 16컬럼, NaN 없음, timestamp 단조 증가, marker valid ratio를 확인한다.
  6. Run 01-main processed CSV를 training/validation으로 나누고, validation에서만 Ridge alpha와 feature 선택을 결정한다.
  7. Run 01-holdout도 같은 수집/Simscape/merge/check 절차로 처리하되, fitting/tuning에는 절대 사용하지 않는다.
  8. 최종 보고에서는 Run 01-holdout merged CSV로 linear/Ridge 모델의 일반화 성능을 평가한다.
- 변경 파일: `docs/plans/2026-06-06_005_run01_main_holdout_execution_order.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main/holdout trajectory 지원 추가 계획서를 작성하고 승인 후 `experiments/run01_main_logger.py`를 확장한다.

## Entry 007
- 무엇을: Run 01-pre 완료와 linear/Ridge baseline 결정 이후 최신 진행 상태를 주요 문서와 `fulltext.md`에 반영했다.
- 왜: 문서 일부가 아직 2026-06-01/2026-06-04 기준 상태, Run 00 준비 상태, processed 경로 미정 상태로 남아 있어 현재 보고 및 후속 작업 판단과 맞지 않았기 때문이다.
- 어떻게: `docs/roadmap.md`의 current status snapshot과 immediate priorities를 2026-06-06 기준으로 갱신했다. `docs/measured_data_structure.md`에는 `data/real/raw`, `data/real/derived`, `data/simulation/raw`, `data/processed` 경로와 `experiments/run01_preprocess.py` 현 구현 상태를 반영했다. Run 01 문서에는 Run 01-pre pipeline validation 완료 상태와 limitations를 추가했다. `experiments/README.md`, `data/README.md`, `fulltext.md`에는 Run 01-pre 산출물, processed dataset naming, 다음 Run 01-main/holdout 진행 순서를 정리했다.
- 결과: 문서 기준 현재 상태가 Run 00 통과, Run 01-pre pipeline validation 완료, Run 01-main 준비 중, 첫 virtual sensor model은 linear/Ridge baseline으로 정렬되었다. Run 01-pre artifact는 final training dataset이 아니라 pipeline validation용이라는 한계도 함께 남겼다.
- 변경 파일: `docs/plans/2026-06-06_006_update_docs_after_run01_pre.md`, `docs/roadmap.md`, `docs/measured_data_structure.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `experiments/README.md`, `data/README.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main/holdout trajectory 지원 추가 계획서를 작성하고 승인 후 logger를 확장한다.

## Entry 008
- 무엇을: 실제 수행 내용을 기준으로 팀원 역할 문서를 갱신했다.
- 왜: 기존 문서에는 S가 동역학/진동 분석, T가 기구 설계, N이 구조/응력 해석 중심으로 기록되어 있었지만, 실제 진행에서는 S/T/N이 하드웨어 제작/조립을 수행했고 S는 팀장 역할, N은 프로파일 주문제작 요청, Y는 Arduino 제어와 함께 회로 구성/배선 및 전원/구동계 연결을 담당했기 때문이다. 동역학/진동/구조/응력 해석은 기획 단계 역할로는 맞지만 시간상 실제 주 산출물로 과장하면 안 되기 때문이다.
- 어떻게: `README.md`에는 짧은 팀원 역할 표를 반영했다. `docs/member_profiles/S.md`, `T.md`, `N.md`, `Y.md`와 `docs/member_profiles/README.md`에는 상세 역할을 반영했다. `docs/system_data_flow.md`의 owner mapping, `docs/hardware_experiment_base_config.md`의 하드웨어/전원/안전 담당자, Run 00/Run 01 protocol의 responsible members, `fulltext.md`의 팀 역할 요약도 같은 기준으로 맞췄다.
- 결과: 역할 문서가 S=팀장/하드웨어 제작, L=시스템 통합/가상센싱/데이터/비전, Y=Arduino 제어/회로/배선/전원 연결, T=하드웨어 제작/기구 설계, N=하드웨어 제작/프로파일 주문제작/기획 단계 구조해석으로 정렬되었다. 기존 해석 역할은 삭제하지 않고 planning-stage scope로 유지했다.
- 변경 파일: `docs/plans/2026-06-06_007_update_team_roles_after_hardware_build.md`, `README.md`, `docs/member_profiles/README.md`, `docs/member_profiles/S.md`, `docs/member_profiles/T.md`, `docs/member_profiles/N.md`, `docs/member_profiles/Y.md`, `docs/system_data_flow.md`, `docs/hardware_experiment_base_config.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `fulltext.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 발표 자료 또는 보고서 작성 시 README의 짧은 역할 표를 기본으로 쓰고, 상세 역할 설명은 `fulltext.md`와 `docs/member_profiles/`를 참조한다.

## Entry 009
- 무엇을: `experiments/run01_main_logger.py`에 Run 01-main/holdout trajectory 지원을 추가하고, circle/grid 정의를 최신 실험 지시에 맞게 수정했다.
- 왜: Run 01-main 수집을 시작하려면 pre 전용 logger를 main/holdout trajectory까지 실행할 수 있어야 하며, circle은 반지름 `40 mm` 반시계 방향 원, grid는 `40 mm` 간격 3x3 경로로 확정되었기 때문이다.
- 어떻게: logger에 `static_center_hold`, `cross_pm20`, `square_pm20`, `circle_r40`, `grid_3x3_pm40`, `cross_pm15_holdout`, `square_pm15_holdout`, `circle_r40_holdout` 선택지를 추가했다. `circle_r40`은 home -> `(40, 0)` -> 반시계 방향 `r=40 mm` 원 -> home 순서로 setpoint를 생성하며, 전체 회전 시간은 `--circle-duration-s`로 지정해 metadata에 남기게 했다. `grid_3x3_pm40`은 home -> `(-40,-40)` -> `(0,-40)` -> `(40,-40)` -> `(-40,0)` -> `(0,0)` -> `(40,0)` -> `(-40,40)` -> `(0,40)` -> `(40,40)` -> home 순서로 구현했다.
- 결과: Run 01-main/holdout logger dry-run과 Python compile 검증을 진행할 수 있는 상태가 되었다. Arduino protocol, raw main CSV field, processed CSV contract는 변경하지 않았다.
- 변경 파일: `docs/plans/2026-06-06_008_extend_run01_main_logger_trajectories.md`, `experiments/run01_main_logger.py`, `experiments/README.md`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: dry-run으로 대표 trajectory row 생성과 IK theta range를 확인하고, 실제 Run 01-main 수집 시 `circle-duration-s` 값을 metadata에 기록한다.

## Entry 010
- 무엇을: Run 01 vision valid-row acceptance threshold를 `95%`에서 `90%`로 낮추고 trajectory coverage 조건을 추가했다.
- 왜: 정지 시간을 늘려 valid ratio만 높이면 학습 데이터가 static row에 편향될 수 있으며, 실제 학습 품질에는 전체 비율보다 이동 구간의 관측 coverage가 더 중요하기 때문이다.
- 어떻게: Run 01-pre/main/holdout 공통 최소 valid-row ratio를 `>=90%`로 변경했다. marker 연속 미검출은 계속 `<1 s`로 제한하고, 특정 corner, 이동 방향, grid row, circle 연속 구간에 누락이 집중되면 ratio가 `90%` 이상이어도 재실행하도록 규정했다. valid ratio를 높이기 위해 정지 시간만 늘리는 방법은 사용하지 않도록 명시했다.
- 결과: 기존 Run 01-pre cross `92.98%`, square `93.21%`는 ratio 기준을 만족한다. 다만 gain 조건 차이와 `theta*_meas=command_echo_no_encoder` 한계 때문에 Run 01-pre 데이터는 계속 pipeline validation용으로만 해석한다.
- 변경 파일: `docs/plans/2026-06-06_009_lower_run01_vision_valid_ratio.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main 실행 후 valid ratio, longest continuous marker loss, trajectory segment coverage를 함께 기록한다.

## Entry 011
- 무엇을: 실제 Run 01-main 수집 범위를 `pm40`으로 정정하고, Run 01-holdout cross/square 범위를 `pm30`으로 변경했다.
- 왜: 실제 학습용 수집은 `cross_pm20`/`square_pm20` 초안이 아니라 `cross_pm40`/`square_pm40`으로 3회씩 진행되었으며, holdout은 학습과 다른 amplitude인 `pm30`에서 평가하기로 결정했기 때문이다.
- 어떻게: `experiments/run01_main_logger.py`에 `cross_pm40`, `square_pm40`, `static_center_holdout`, `cross_pm30_holdout`, `square_pm30_holdout`, `grid_3x3_pm40_holdout`을 추가했다. 기존 pm20 main과 pm15 holdout 선택지는 호환성을 위해 유지했다. Run 01 프로토콜과 컴퓨터 2 실행 안내는 main `pm40`, holdout `pm30` 기준으로 갱신했다.
- 결과: Run 01-main은 static/cross pm40/square pm40/circle r40/grid pm40 각 3회, 총 15회 완료 상태로 기록했다. 현재 repository에는 같은 run_id의 vision raw CSV 15개가 들어왔고, Arduino/main CSV·JSON·serial 파일은 아직 이전 전이다. Holdout은 static, cross pm30, square pm30, circle r40, grid pm40을 각 1회 실행하는 구성으로 정리했다.
- 변경 파일: `docs/plans/2026-06-06_010_align_main_pm40_holdout_pm30.md`, `experiments/run01_main_logger.py`, `experiments/README.md`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 하드웨어가 있는 동안 5개 holdout을 `r01`로 수집하고, main/vision에 동일한 run_id를 사용한다.

## Entry 012
- 무엇을: Run 01-main 15개 run의 main/vision/Simscape 시간 정렬 방식을 trajectory-aware 방식으로 변경하고 measured-position, merged dataset, alignment sidecar를 생성했다.
- 왜: Run 01-main vision log는 main log보다 시작·종료 여유 구간이 길어 Run 01-pre의 첫 timestamp 기준 정렬을 그대로 사용하면 실제 이동 구간이 어긋나기 때문이다.
- 어떻게: cross/square/grid는 시작·끝 home을 anchor에서 제외하고 내부 phase boundary를 main phase에 piecewise linear mapping했다. circle은 초기 home 위치에서 반경 `10 mm`를 넘는 첫·마지막 vision sample을 검출해 main 원운동 시간축에 mapping했다. static은 전체 vision duration을 main duration에 mapping했다. 현재 Simscape export 파일명 `run01_simscape_*`도 resolver에서 인식하고, Simscape/main timestamp가 완전히 같은지 검증했다.
- 결과: Run 01-main 15개 모두 `measured_position_<run_id>.csv`, `merged_<run_id>.csv`, `alignment_<run_id>.json`을 생성했다. merged dataset은 총 `5,137` rows이며 모두 fixed 16-column contract, timestamp 단조 증가, `has_nan=False`를 만족했다. cross/square/grid의 phase median은 target 방향과 일치했고, circle 정렬 후 vision 반경 중앙값은 세 반복에서 약 `41.05-41.50 mm`로 일관적이었다.
- 변경 파일: `docs/plans/2026-06-06_011_run01_main_phase_alignment.md`, `experiments/run01_preprocess.py`, `docs/measured_data_structure.md`, `data/real/derived/measured_position_2026-06-06_run01_main_*.csv`, `data/processed/merged_2026-06-06_run01_main_*.csv`, `data/processed/alignment_2026-06-06_run01_main_*.json`, `docs/daily_notes/2026-06-06.md`
- 다음: Run 01-main merged dataset의 train/validation split을 확정하고 linear/Ridge baseline 학습을 진행한다. Run 01-holdout은 fitting/tuning에서 제외한다.

## Entry 013
- 무엇을: 초기 Linear/Ridge 가상센서의 validation 방식을 run-level 3-fold cross-validation으로 확정했다.
- 왜: 각 trajectory가 `r01-r03`으로 3회 반복되어 있어 반복 실행 간 변동을 모두 validation에 반영할 수 있고, 한 개 반복만 고정 validation으로 두는 것보다 alpha 선택의 안정성을 높일 수 있기 때문이다.
- 어떻게: Fold 1은 모든 trajectory의 `r01`, Fold 2는 `r02`, Fold 3은 `r03`을 validation으로 사용하고 나머지 두 반복을 training으로 사용하도록 정의했다. 동일 run의 인접 row는 train/validation으로 무작위 분할하지 않는다. alpha는 세 fold의 run/trajectory 단위 XY RMSE 평균으로 선택하고 XY MAE/max error를 함께 기록한다. 설정 확정 후 Run 01-main 전체로 최종 모델 하나를 재학습하며, 세 fold 모델 중 하나를 그대로 최종 모델로 선택하지 않는다.
- 결과: Ridge tuning, 최종 재학습, holdout 평가의 경계가 명확해졌다. Run 01-holdout은 fitting, cross-validation, alpha 및 feature 선택에 사용하지 않는다.
- 변경 파일: `docs/plans/2026-06-06_012_run_level_three_fold_ridge_validation.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `virtual_sensor/README.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 이 정책을 따르는 Ridge 학습·평가 스크립트를 구현하고 fold별/평균 XY RMSE, MAE, max error를 저장한다.

## Entry 014
- 무엇을: 다음 context에서 수행할 Ridge 가상센서 구현·학습·holdout 평가의 통합 계획서를 작성했다.
- 왜: 코드 변경 전에 15개 main dataset의 run-level 3-fold CV, alpha 선택, 전체 재학습, 모델 저장, holdout 격리와 partial circle 처리까지 실행 범위를 고정하기 위해서다.
- 어떻게: `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`에 입력 데이터, fold 구성, 표준화 누수 방지, metric 집계, alpha tie-break, 최종 `.npz` 모델, JSON 보고서, holdout 전처리·평가 및 검증 조건을 기록했다.
- 결과: 이번 작업에서는 계획서와 변경 기록만 작성했으며 가상센서 코드 구현이나 학습 실행은 시작하지 않았다.
- 변경 파일: `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 새 context에서 계획서 승인 내용을 기준으로 Ridge 구현부터 최종 holdout 평가까지 진행한다.

## Entry 015
- 무엇을: NumPy 기반 Ridge 가상센서, Run 01-main run-level 3-fold CV, 최종 모델 저장, frozen holdout 평가 코드를 구현하고 main 학습을 실행했다.
- 왜: 15개 Run 01-main dataset으로 누수 없는 baseline alpha를 선택하고, 전체 main data로 재학습한 portable model을 확보하기 위해서다.
- 어떻게: fold별 training data에서만 feature mean/scale을 계산하고 `r01`, `r02`, `r03`을 차례로 validation에 사용했다. alpha grid `[0, 1e-4, 1e-3, 1e-2, 1e-1, 1, 10, 100, 1000]`을 비교했으며, 각 run의 metric을 동일 가중 평균해 alpha를 선택했다. `alpha=0`은 command/measured feature 중복으로 특이행렬이 되므로 NumPy least-squares로 계산하고, 양의 alpha는 정규화 normal equation으로 계산했다. 최종 모델은 15개 run, 5,137 row 전체로 재학습했다. partial holdout circle은 관측된 약 `348.6 deg`만 main 원운동 구간에 대응시키고 미관측 마지막 구간과 home return을 제외하도록 정렬했다.
- 결과: `alpha=1`이 선택되었고 15개 validation run의 macro 평균은 XY RMSE `10.007230 mm`, XY MAE `5.826378 mm`, XY max error `48.584248 mm`였다. fold별 XY RMSE는 `r01=10.423035 mm`, `r02=8.859494 mm`, `r03=10.739160 mm`였다. 반복 실행에서 alpha, 전체 CV metric, coefficient가 완전히 동일했고 모델 저장 후 prediction 최대 차이는 `0.0`이었다. holdout용 Simscape CSV 5개가 없고 현재 환경에 MATLAB이 없어 holdout merged CSV와 holdout 결과 JSON은 생성하지 못했다. 누락 입력을 대체하는 dummy simulation은 만들지 않았다.
- 변경 파일: `virtual_sensor/ridge_model.py`, `virtual_sensor/train_ridge.py`, `virtual_sensor/evaluate_ridge.py`, `experiments/run01_preprocess.py`, `virtual_sensor/README.md`, `virtual_sensor/models/ridge_run01_main_2026-06-06.npz`, `experiments/results/ridge_run01_main_cv_2026-06-06.json`, `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`, `docs/daily_notes/2026-06-06.md`
- 다음: `data/simulation/raw/simscape_2026-06-06_run01_holdout_<trajectory>_r01.csv` 5개를 MATLAB/Simscape에서 생성한 뒤 holdout 전처리 5회와 `python3 virtual_sensor/evaluate_ridge.py`를 실행한다.

## Entry 016
- 무엇을: circle을 제외한 complete holdout 4개를 전처리하고 frozen Ridge 모델의 예비 holdout 평가를 실행했다.
- 왜: circle Simscape 파일을 기다리는 동안 static, cross pm30, square pm30, grid pm40에서 모델의 일반화 성능과 보정 가능성을 먼저 확인하기 위해서다.
- 어떻게: evaluator가 complete 4개만 있으면 `preliminary_complete_only`, circle까지 있으면 `final_with_partial_circle`로 동작하도록 확장했다. 네 Simscape CSV와 main CSV의 time axis 일치를 확인한 뒤 기존 trajectory-aware alignment로 measured-position, merged, alignment 파일을 생성했다. alpha, scaler, coefficient는 변경하지 않고 기존 `ridge_run01_main_2026-06-06.npz`를 그대로 사용했다. 보정 전 비교값은 error prediction을 모두 0으로 둔 baseline으로 계산했다.
- 결과: merged row는 static `300`, cross `350`, square `250`, grid `450`, 합계 `1,350`이며 모두 fixed 16-column schema, finite value, strictly increasing timestamp, `has_nan=False`를 만족했다. 4-run macro XY RMSE는 보정 전 `11.945516 mm`, 모델 적용 후 `11.033113 mm`로 `7.638%` 개선됐다. run별 XY RMSE 개선율은 static `29.662%`, cross pm30 `5.196%`, square pm30 `2.928%`, grid pm40 `6.997%`였다. 현재 보고서는 circle 미포함 예비 결과이며 모델 재학습이나 alpha 재선택에는 사용하지 않았다.
- 변경 파일: `virtual_sensor/evaluate_ridge.py`, `experiments/run01_preprocess.py`, `data/real/derived/measured_position_2026-06-06_run01_holdout_{static_center_hold,cross_pm30,square_pm30,grid_3x3_pm40}_r01.csv`, `data/processed/merged_2026-06-06_run01_holdout_{static_center_hold,cross_pm30,square_pm30,grid_3x3_pm40}_r01.csv`, `data/processed/alignment_2026-06-06_run01_holdout_{static_center_hold,cross_pm30,square_pm30,grid_3x3_pm40}_r01.json`, `experiments/results/ridge_run01_holdout_2026-06-06.json`, `virtual_sensor/README.md`, `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`, `docs/daily_notes/2026-06-06.md`
- 다음: circle holdout Simscape CSV를 가져와 partial observed interval 전처리를 수행하고 같은 evaluator를 재실행해 `final_with_partial_circle` 보고서를 생성한다.

## Entry 017
- 무엇을: frozen Ridge 모델의 phase별 오차와 transition 영향을 진단하는 계획 014를 작성했다.
- 왜: holdout 4개에서 macro XY RMSE 개선이 `7.638%`에 그쳤고 grid/square 최대 오차가 커서, 신경망 도입 전에 정렬·settling 문제와 지속적인 모델 오차를 분리해야 하기 때문이다.
- 어떻게: main은 selected alpha의 out-of-fold prediction, holdout은 frozen final model prediction을 사용한다. merged timestamp를 raw main phase에 정확히 연결하고 phase별 및 phase 시작 후 `0-0.5 s`, `0.5-1.5 s`, `1.5 s+` 구간별 baseline/Ridge metric과 최악 row를 기록하도록 범위를 정했다.
- 결과: 이번 분석에서는 모델, alpha, feature, CSV contract, 제어기를 변경하지 않고 JSON/Markdown 진단 결과만 생성한다.
- 변경 파일: `docs/plans/2026-06-06_014_phase_error_analysis.md`, `docs/daily_notes/2026-06-06.md`
- 다음: phase 분석 도구를 구현하고 main OOF 및 complete holdout 4개에 실행한다.

## Entry 018
- 무엇을: Run 01-main OOF와 complete holdout 4개에 대해 phase별, phase-age별, phase-edge별 Ridge 오차 분석을 실행했다.
- 왜: holdout 전체 개선율이 낮고 grid/square 최대 오차가 큰 원인이 모델 용량 부족인지 alignment transition contamination인지 분리하기 위해서다.
- 어떻게: main 15개는 selected `alpha=1`의 repetition별 OOF model로 예측하고, holdout 4개는 frozen final model로 예측했다. merged timestamp를 raw main phase에 정확히 매핑하고 phase 시작 후 구간과 phase 첫/내부/마지막 `0.5 s`를 분리했다. worst residual row에는 time, phase, phase age, target, true error, prediction을 기록했다.
- 결과: holdout row-weighted XY RMSE는 zero prediction `13.282 mm`, Ridge `12.462 mm`로 전체 `6.175%` 개선됐다. phase 내부 구간은 `6.068 -> 5.097 mm`로 `16.003%` 개선됐지만 마지막 `0.5 s`는 `41.872 -> 40.304 mm`였다. cross/grid/square 모두 `phase_end_alignment_sensitive`로 분류됐다. grid 최악 row는 `x_plus_y_center` 종료 직전이며 target `(40, 0)`에서 vision-derived 위치가 다음 phase 방향인 약 `(-41, 45)`로 이동했다. square도 `q2` 종료 직전에 target `(-30, 30)`인데 vision-derived 위치가 다음 `q3` 방향인 약 `(-32, -31)`이었다. raw vision segment의 마지막 sample이 이미 다음 setpoint 이동을 포함하는데 현재 alignment가 이를 현재 main phase 끝에 mapping한 것이 확인됐다.
- 변경 파일: `docs/plans/2026-06-06_014_phase_error_analysis.md`, `virtual_sensor/analyze_phase_errors.py`, `experiments/results/ridge_run01_phase_analysis_2026-06-06.json`, `experiments/results/ridge_run01_phase_analysis_2026-06-06.md`, `virtual_sensor/README.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 신경망이나 feature 변경 전에 phase-boundary alignment 정책을 수정하고 main/holdout processed dataset을 재생성한 뒤 Ridge CV와 holdout 평가를 다시 실행한다.

## Entry 019
- 무엇을: phase-boundary realignment, processed dataset 재생성, Ridge 재학습과 holdout 재평가를 수행하는 계획 015를 작성했다.
- 왜: plan 014에서 vision 이동이 이전 phase의 마지막 약 `0.15-0.43 s`에 포함되고 Simscape는 command 전환 후 대체로 약 `110 ms`에 새 자세 근처로 도달하는 것이 확인됐기 때문이다.
- 어떻게: vision stable position에서 `5 mm` 이상 이탈하는 시점을 다음 main phase 시작에 mapping하고, 새 vision phase의 첫 valid sample을 해당 Simscape target의 `3 mm` 이내 도달 시점에 mapping한다. static/circle 정책은 유지하고 main 15개 및 complete holdout 4개를 재생성한 뒤 기존 CV/holdout/phase 분석을 다시 실행하도록 범위를 고정했다.
- 결과: processed label과 모델/report는 교체되지만 raw log, fixed 16-column contract, controller interface는 변경하지 않는다.
- 변경 파일: `docs/plans/2026-06-06_015_phase_boundary_realignment_retrain.md`, `docs/daily_notes/2026-06-06.md`
- 다음: event-anchor alignment를 구현하고 anchor ordering 및 대표 run 위치 정합성을 검증한다.

## Entry 020
- 무엇을: cross/square/grid의 phase-boundary alignment를 event 기반으로 수정하고 main 15개와 complete holdout 4개를 재전처리한 뒤 Ridge를 재학습·재평가했다.
- 왜: 기존 piecewise phase-start mapping이 vision phase 끝에 포함된 다음 setpoint 이동을 현재 main phase 끝으로 퍼뜨려 grid/square에서 `60-90 mm` 수준의 가짜 오차를 만들었기 때문이다.
- 어떻게: vision stable 위치에서 `5 mm` 이탈을 departure로 검출하고, departure 직전 stable sample을 현재 main phase 끝에, departure를 다음 command 시작에, 새 phase 첫 valid sample을 Simscape target `3 mm` 이내 도달 시점에 mapping했다. phase 내부에 departure가 없으면 마지막 valid timestamp를 boundary edge로 사용했다. 또한 epoch timestamp에서 `math.isclose` 상대오차가 sub-second anchor를 동일 시각으로 처리하던 문제를 절대오차 `1e-9 ms` 비교로 수정했다. main 15개와 holdout 4개를 재생성하고 기존 3-fold CV, final retraining, holdout 평가, phase 분석을 같은 명령으로 재실행했다.
- 결과: main dataset은 총 `5,158` row이며 fixed schema, finite value, strictly increasing timestamp를 만족했다. selected alpha는 `1 -> 100`, main OOF macro XY RMSE는 `10.007230 -> 4.886987 mm`로 감소했다. holdout macro는 zero prediction `4.533414 mm`, Ridge `2.848173 mm`, 개선율 `37.174%`로 기존 `7.638%`보다 높아졌다. run별 개선율은 static `43.427%`, cross `16.933%`, square `36.110%`, grid `45.895%`다. phase 마지막 `0.5 s` Ridge RMSE는 `40.304 -> 2.444 mm`로 감소했다. 다만 기존 holdout을 alignment 결함 진단에 사용했으므로 이 결과는 retrospective이며 fresh Run 02 data에서 독립 검증해야 한다.
- 변경 파일: `docs/plans/2026-06-06_015_phase_boundary_realignment_retrain.md`, `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`, `docs/plans/2026-06-06_014_phase_error_analysis.md`, `experiments/run01_preprocess.py`, `data/real/derived/measured_position_2026-06-06_run01_main_*.csv`, `data/real/derived/measured_position_2026-06-06_run01_holdout_*.csv`, `data/processed/merged_2026-06-06_run01_main_*.csv`, `data/processed/merged_2026-06-06_run01_holdout_*.csv`, `data/processed/alignment_2026-06-06_run01_main_*.json`, `data/processed/alignment_2026-06-06_run01_holdout_*.json`, `virtual_sensor/models/ridge_run01_main_2026-06-06.npz`, `experiments/results/ridge_run01_main_cv_2026-06-06.json`, `experiments/results/ridge_run01_holdout_2026-06-06.json`, `experiments/results/ridge_run01_phase_analysis_2026-06-06.json`, `experiments/results/ridge_run01_phase_analysis_2026-06-06.md`, `virtual_sensor/README.md`, `docs/measured_data_structure.md`, `docs/daily_notes/2026-06-06.md`
- 다음: circle holdout을 추가해 partial diagnostic을 완료하고, Run 02에서는 conservative correction gain/clamp를 적용한 fresh run으로 독립 성능을 검증한다.

## Entry 021
- 무엇을: 가상센싱 진행 과정과 결과를 한 번에 볼 수 있는 통합 문서를 작성했다.
- 왜: 계획서, Daily Note, README, JSON 결과가 여러 위치에 나뉘어 있어 모델 선택부터 alignment 문제 발견, 재학습, 최종 결과까지 따라가기 어려웠기 때문이다.
- 어떻게: `docs/virtual_sensing_progress.md`에 목적, 데이터 구성, Ridge 선택, 최초 `7.638%` 결과, phase 분석, event-anchor realignment, 재학습 후 `37.174%` 결과, 현재 모델, 제한사항, Run 02 다음 단계를 순서대로 정리했다. `docs/README.md`와 `virtual_sensor/README.md`에서 바로 접근할 수 있도록 링크했다.
- 결과: 가상센싱 진행 상황은 통합 문서를 시작점으로 읽고, 필요한 경우 계획 013-015와 machine-readable 결과로 내려갈 수 있다.
- 변경 파일: `docs/plans/2026-06-06_016_virtual_sensing_progress_summary.md`, `docs/virtual_sensing_progress.md`, `docs/README.md`, `virtual_sensor/README.md`, `docs/daily_notes/2026-06-06.md`
- 다음: circle holdout과 Run 02 결과가 나오면 같은 통합 문서의 결과 및 다음 단계 섹션을 갱신한다.

## Entry 022
- 무엇을: 최종 궤적과 Simscape 파일 확정 전에 Run 02 공통 보정 엔진과 offline 안전 검증을 준비하는 계획 017을 작성했다.
- 왜: Arduino 담당자가 부재하고 궤적 선택도 보류 중이지만, 모델 추론, XY gain/clamp, fallback, corrected-target IK, correction log는 궤적과 독립적으로 먼저 구현하고 검증할 수 있기 때문이다.
- 어떻게: frozen Ridge 모델을 사용하는 공통 엔진, serial-free generic CSV replay, main/holdout 분리 안전성 보고서, 표준 라이브러리 단위 테스트를 범위로 정했다. holdout label로 gain/clamp를 최적화하지 않고 main prediction 분포와 IK 안전성만으로 후보를 검토하도록 제한했다.
- 결과: 이번 계획은 Arduino firmware, serial 전송, trajectory 정의, Simscape 생성, fixed 16-column contract를 변경하지 않는다. 구현은 사용자 승인 후 시작한다.
- 변경 파일: `docs/plans/2026-06-06_017_run02_correction_engine_offline_validation.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 계획 017 승인 후 공통 보정 엔진, offline validator, 테스트를 구현하고 기존 Run 01-main/holdout으로 검증한다.

## Entry 023
- 무엇을: Run 02 공통 correction engine, 단위 테스트, serial-free offline validator를 구현하고 기존 Run 01-main/holdout에서 gain/clamp 후보를 검증했다.
- 왜: 최종 trajectory와 새 Simscape 파일이 확정되기 전에도 모델 추론부터 XY correction, clamp, corrected-target IK, fallback까지의 공통 계산과 수치 안전성을 먼저 확인할 수 있기 때문이다.
- 어떻게: frozen Ridge artifact의 feature 순서를 강제하고 `target_xy - gain * predicted_error_xy`를 적용했다. XY correction은 vector norm으로 clamp하고 Z correction은 0으로 유지했다. feature/추론/IK 오류 시 nominal target/theta로 fallback하도록 했다. gain `0.25/0.5/1.0`과 clamp `2/4/6 mm`의 9개 조합을 main 5,158행과 complete holdout 1,356행에서 replay했다. validator는 holdout `error_*` label을 읽지 않았다.
- 결과: 단위 테스트 6개가 통과했다. 9개 후보 모두 IK fallback, correction limit violation, theta limit violation이 0이었다. corrected theta 전체 범위는 약 `-11.467 deg`에서 `16.501 deg`였다. main-only 초기 hardware gate 후보는 gain `0.25`, XY clamp `2 mm`이며 main clamp는 `4/5,158`행(`0.077549%`), holdout clamp는 `0/1,356`행이었다. 이 값은 offline 성능 최적값이 아니라 첫 물리 실험용 잠정 안전 후보다.
- 변경 파일: `virtual_sensor/correction_engine.py`, `virtual_sensor/test_correction_engine.py`, `experiments/run02_offline_validate.py`, `experiments/results/run02_correction_offline_validation_2026-06-06.json`, `virtual_sensor/README.md`, `experiments/README.md`, `docs/hardware_experiment_run_02_corrected_comparison.md`, `docs/virtual_sensing_progress.md`, `docs/plans/2026-06-06_017_run02_correction_engine_offline_validation.md`, `docs/daily_notes/2026-06-06.md`
- 다음: trajectory와 대응 Simscape 파일을 확정한 뒤 serial-enabled Run 02 logger 계획을 승인받아 static/small-motion hardware gate부터 진행한다.

## Entry 024
- 무엇을: 사용 여부가 아직 확정되지 않은 Run 02 24회 trajectory 후보를 문서화하는 계획 018을 작성했다.
- 왜: cross, reverse grid, diamond, circle의 순서와 방향 및 OFF/ON 반복 구성을 대화 기록에만 남기지 않고 나중에 다시 검토할 수 있어야 하기 때문이다.
- 어떻게: cross와 circle은 기존 방향을 유지하고, grid는 기존 3x3 waypoint의 정확한 역순, diamond는 `(35,0)`에서 시작하는 반시계 순서로 정의했다. 네 trajectory 각각 correction OFF/ON 3회씩인 총 24회 구성을 optional candidate로 기록하도록 범위를 정했다.
- 결과: 이번 단계에서는 계획서만 작성했으며 Run 02 본문, logger 코드, Simscape 파일은 아직 변경하지 않았다.
- 변경 파일: `docs/plans/2026-06-06_018_document_run02_24_run_trajectory_candidate.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 계획 018 승인 후 Run 02 protocol에 optional 24-run matrix와 정확한 waypoint를 기록한다.

## Entry 025
- 무엇을: Run 02 protocol에 optional 24회 trajectory 후보와 각 경로의 정확한 waypoint 및 방향을 기록했다.
- 왜: 해당 구성을 최종적으로 사용하지 않더라도 이후 Run 02 설계에서 동일한 후보를 정확히 재현하고 비교할 수 있어야 하기 때문이다.
- 어떻게: cross `+/-30 mm`는 기존 holdout 순서, reverse grid `+/-40 mm`는 기존 3x3 목록의 정확한 역순, diamond `+/-35 mm`는 `(35,0)` 시작 반시계 순서, circle `r=40 mm`는 기존 `(40,0)` 시작 반시계 정의로 작성했다. 각 trajectory는 correction OFF/ON 상태별 3회로 총 24회다.
- 결과: 24회 후보가 Run 02 본문에 저장되었고 optional/not finalized 상태도 명시됐다. duration, hold time, circle point count, OFF/ON 순서, run naming은 구현 전에 확정해야 한다.
- 변경 파일: `docs/hardware_experiment_run_02_corrected_comparison.md`, `docs/plans/2026-06-06_018_document_run02_24_run_trajectory_candidate.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 실제 Run 02 구성을 결정할 때 이 후보 또는 시간 절약형 구성을 선택하고, 선택된 trajectory의 Simscape 입력과 serial-enabled logger 구현 계획을 작성한다.

## Entry 026
- 무엇을: 새 Simscape circle 파일로 Run 01 holdout circle을 전처리하고, 기존 frozen Ridge 모델을 5개 holdout 파일에 다시 평가했다.
- 왜: 기존 평가는 circle Simscape가 없어 complete holdout 4개만 포함했으며, partial circle diagnostic을 추가한 최종 holdout 보고서가 필요했기 때문이다.
- 어떻게: `python3 experiments/run01_preprocess.py --run-id 2026-06-06_run01_holdout_circle_r40_r01`로 measured-position, merged, alignment 파일을 생성했다. vision valid `283/298`행 중 관측 원운동 구간을 `partial_circle_observed_angular_coverage`로 정렬해 merged `350`행을 만들었다. fixed 16-column schema, finite value, timestamp 단조 증가를 확인한 뒤 `python3 virtual_sensor/evaluate_ridge.py`를 실행했다. 모델은 `alpha=100`인 기존 `ridge_run01_main_2026-06-06.npz`를 그대로 사용했고 재학습이나 tuning은 수행하지 않았다.
- 결과: complete 4-run macro XY RMSE는 기존과 동일하게 `4.533414 -> 2.848173 mm`, `37.174%` 개선이었다. partial circle은 `6.664867 -> 7.872045 mm`로 `18.113%` 악화됐고 model XY max error는 `40.447808 mm`였다. 첫 `0.5 s`를 제외해도 `5.688084 -> 7.045994 mm`, `23.873%` 악화되어 초기 누락 전이만의 문제는 아니었다. 5개 파일 전체 `1,706`행의 참고용 row-weighted XY RMSE는 `5.090411 -> 4.371378 mm`, `14.125%` 개선이었다. circle은 coverage가 partial이므로 complete macro에는 포함하지 않았고 holdout 격리는 유지됐다.
- 변경 파일: `data/real/derived/measured_position_2026-06-06_run01_holdout_circle_r40_r01.csv`, `data/processed/merged_2026-06-06_run01_holdout_circle_r40_r01.csv`, `data/processed/alignment_2026-06-06_run01_holdout_circle_r40_r01.json`, `experiments/results/ridge_run01_holdout_2026-06-06.json`, `docs/plans/2026-06-06_013_ridge_virtual_sensor_training.md`, `docs/daily_notes/2026-06-06.md`
- 다음: circle 결과를 이용해 현재 모델을 재조정하지 않고, fresh Run 02 circle OFF/ON 비교에서 악화가 재현되는지 독립적으로 확인한다.

## Entry 027
- 무엇을: Run 02를 cross, reverse grid, diamond, circle의 4종 trajectory와 correction OFF/ON, 상태별 3회 반복인 총 24회 구성으로 확정하고 serial logger를 구현했다.
- 왜: Run 01을 종료하고 fresh Run 02 데이터에서 동일 trajectory의 OFF/ON pair를 직접 비교해야 하며, correction ON에는 frozen model 입력용 nominal Simscape time axis와 별도 correction log가 필요하기 때문이다.
- 어떻게: run ID를 `YYYY-MM-DD_run02_<trajectory>_<off|on>_r01..r03`으로 고정하고 repetition별로 각 trajectory OFF 직후 ON을 실행하도록 했다. 공통 Z는 `-263.27731514697575 mm`, waypoint hold는 `5 s`, circle은 `72` points/`30 s`와 `(40,0)` 시작·끝 `2 s` hold를 사용한다. `experiments/run02_logger.py`는 deterministic `0.1 s` time axis, exact Simscape axis 검증, gain `0.25`, XY clamp `2 mm`, Z-off correction, fallback, correction auxiliary log를 구현한다. Arduino smooth move가 blocking인 점을 반영해 serial command는 phase 전환 또는 `0.5 s`마다 갱신하고 `command_sent`, 실제 elapsed time, schedule lag를 기록한다.
- 결과: manifest는 24개 고유 run ID와 trajectory별 nominal Simscape 파일 4개를 생성한다. 단위 테스트 11개가 통과했다. 네 trajectory의 OFF/ON dry-run에서 fallback과 clamp는 모두 0이었고 최대 applied correction은 reverse grid의 `1.661925 mm`, corrected theta 전체 범위는 약 `-10.968..16.187 deg`였다. row 수는 cross 450, reverse grid 550, diamond 350, circle 500이다.
- 변경 파일: `docs/plans/2026-06-06_019_finalize_run02_24_runs.md`, `experiments/run02_logger.py`, `experiments/test_run02_logger.py`, `experiments/run02_logger_computer2_powershell.md`, `experiments/README.md`, `docs/hardware_experiment_run_02_corrected_comparison.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 실제 실행 날짜로 manifest를 생성하고 네 nominal Simscape CSV를 준비한 뒤 r01 cross OFF/ON pair부터 hardware gate를 진행한다.

## Entry 028
- 무엇을: Git 저장소 구조가 없는 Computer 2의 Codex를 위한 독립형 Run 02 인수인계 문서를 추가했다.
- 왜: 전달 대상 컴퓨터에서 필수 폴더와 의존 파일을 누락하지 않고, Simscape 준비부터 24회 실험 및 결과 회수까지 동일 조건으로 수행하게 하기 위해서다.
- 어떻게: 필수 파일과 SHA-256, 생성할 Windows 폴더, Python 검증, OFF/ON dry-run, Simscape 시간축 계약, 실제 실행 순서, 중단 조건과 회수 파일을 한 문서에 정리했다.
- 결과: Computer 2가 Git 없이 `C:\delta_robot_run02` 실행 환경을 재구성하고 사전 검증 후 실험을 진행할 수 있는 전달 문서가 준비됐다.
- 변경 파일: `experiments/run02_computer2_codex_handoff.md`, `experiments/README.md`, `docs/plans/2026-06-06_020_computer2_codex_run02_handoff.md`, `docs/daily_notes/2026-06-06.md`
- 다음: 필수 파일과 두 PowerShell 문서를 Computer 2로 전달하고, 해시 및 네 개의 ON dry-run 검증을 완료한 뒤 실제 실험을 시작한다.
