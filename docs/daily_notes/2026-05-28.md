# Daily Note - 2026-05-28

## Entry 001
- 무엇을:
  - 현재 `docs/roadmap.md` 기준 프로젝트 진행 상태를 점검하고, 다음 우선작업 3개를 오늘 기준 메모로 정리했다.
- 왜:
  - Stage 1은 완료되었고, Stage 2, 3, 4, 6이 각각 다른 수준으로 진행 중이라 다음 작업 순서를 명확히 남겨둘 필요가 있었다.
  - 특히 Stage 8 가상센서 학습으로 바로 넘어가기 전에 Stage 4 simulation/data path를 먼저 닫아야 한다는 점을 기록해 둘 필요가 있었다.
- 어떻게:
  - `docs/roadmap.md`의 `Current Status Snapshot`과 Stage별 `Current Status`를 기준으로 현재 완료/진행 상태를 다시 확인했다.
  - 그 기준으로 다음 작업을 세 우선순위로 압축했다.
- 결과:
  - 다음 우선작업은 아래 세 가지로 정리되었다.
  - 1) Stage 4 마무리: `Simulink/Simscape` export와 `error_*` 의미를 SoT 기준으로 정렬한다.
  - 2) Stage 4 보강: 기준 모델 `simscape_delta_robot_trajectory.slx`와 실행 절차, 입력/출력 경로를 문서 기준으로 더 명확히 고정한다.
  - 3) Stage 5~7 준비: real log, vision ground-truth, alignment 규칙을 구체화해 실데이터 수집으로 이어질 준비를 한다.
- 다음:
  - `run_fake_pipeline_simscape.m`의 `error_*` 처리와 SoT 의미 차이를 줄이는 구체 작업 계획을 별도 계획서로 분리한다.
  - `simulation/simulink/model_notes.md`를 바탕으로 실행 절차 문서를 더 구체화할지 검토한다.
  - vision/raw log와 main log alignment 규칙을 문서 단위로 먼저 정리할지 확인한다.

## Entry 002
- 무엇을:
  - `run_fake_pipeline_simscape.m`의 `error_*` 처리 방향과 `measured_position` 데이터 확보 방안을 별도 계획서로 정리했다.
- 왜:
  - 현재 Simscape export의 `error_*`는 `target_position - sim_position` diagnostic 값이지만, SoT 기준 `error_*`는 `measured_position - sim_position` correction field이기 때문이다.
  - Stage 8 가상센서 학습으로 넘어가기 전에 `measured_position`의 출처와 한계를 명확히 해야 label 의미가 흔들리지 않기 때문이다.
- 어떻게:
  - 기존 SoT 문서의 `error_*` 의미, vision tracking 문서의 `vision_x/y` 출력, 현재 Simscape export 동작을 기준으로 계획서를 작성했다.
  - `measured_position` 확보 경로를 vision 기반 XY, 별도 측정이 필요한 Z, theta 기반 estimator-derived position으로 분리했다.
- 결과:
  - `docs/plans/2026-05-28_002_simscape_error_semantics_and_measured_position_plan.md`가 추가되었다.
  - 초기 `measured_x/y`는 vision raw log를 post-alignment해 확보하고, `measured_z`는 별도 Z ground-truth 없이 angle-derived estimate로 분리해 다루는 방향이 정리되었다.
- 다음:
  - `run_fake_pipeline_simscape.m` 수정 전에 실제 measured data 구조를 먼저 정의한다.
  - `error_*`는 measured/sim/vision 로그가 정렬된 processed dataset 단계에서 생성한다.

## Entry 003
- 무엇을:
  - `measured_z` 확보 방침을 사용자 기준에 맞춰 계획서에 반영했다.
- 왜:
  - 현재 프로젝트에서는 별도 Z ground-truth를 추가하지 않고, 초기에는 XY가 주 평가축이기 때문이다.
- 어떻게:
  - `measured_z`를 `theta*_meas` 기반 FK 또는 estimator-derived 값으로 사용한다고 정리했다.
  - `error_z`는 계산하되 외부 ground-truth 기반 label이 아니라 보조 label 또는 diagnostic 성격으로 해석하도록 적었다.
- 결과:
  - `measured_x/y`는 vision 기반 ground-truth를 우선하고, `measured_z`는 angle-derived estimate로 분리하는 기준이 생겼다.
- 다음:
  - 후속 구현 시 Z 계산 경로가 어떤 FK 함수와 어떤 `theta*_meas` source를 쓰는지 실험 메타데이터에 기록한다.

## Entry 004
- 무엇을:
  - `error_*` 생성 시점을 raw Simscape export가 아니라 measured data 정렬 이후 processed dataset 단계로 문서화했다.
- 왜:
  - 실제 measured data 구조가 정해지기 전에 `error_*` 계산 구현을 먼저 고정하면 label 의미와 데이터 병합 규칙이 다시 흔들릴 수 있기 때문이다.
- 어떻게:
  - `docs/system_data_flow.md`의 simulation path와 correction field notes에 processed/merged dataset 단계에서 contract-compliant `error_*`를 생성한다는 기준을 추가했다.
  - 기존 계획서의 후속 우선순위를 `run_fake_pipeline_simscape.m` 수정에서 measured data 구조 정의로 변경했다.
- 결과:
  - 우선작업 1번은 코드 수정 완료가 아니라 `error_*` 정책 문서화 완료 상태로 정리되었다.
  - 다음 우선작업은 measured data 구조 정의로 전환되었다.
- 다음:
  - real main log, vision raw log, angle-derived measured position, processed merged dataset의 필드 구조를 문서로 고정한다.

## Entry 005
- 무엇을:
  - measured data 구조 정의 작업을 위한 계획서를 추가했다.
- 왜:
  - 실제 measured data 구조가 정해져야 `error_*`를 processed merged dataset 단계에서 일관되게 계산할 수 있기 때문이다.
- 어떻게:
  - raw main log, vision raw log, angle-derived measured position, processed merged dataset을 네 계층으로 나누어 필드 후보와 역할을 정리했다.
  - `error_x/y`는 vision 기반 XY에서, `error_z`는 angle-derived `measured_z_est`에서 계산하는 기준을 계획서에 적었다.
- 결과:
  - `docs/plans/2026-05-28_003_measured_data_structure_definition.md`가 추가되었다.
  - 다음 문서화 작업의 기준 범위가 real log, vision log, estimator-derived position, merged dataset으로 분리되었다.
- 다음:
  - 계획서 기준으로 measured data 구조를 SoT 문서에 반영할지 검토한다.

## Entry 006
- 무엇을:
  - measured data 구조를 별도 SoT 문서로 추가하고 관련 문서에서 참조하도록 정리했다.
- 왜:
  - `error_*`를 processed merged dataset 단계에서 생성하려면 real main log, vision raw log, angle-derived measured position, final merged dataset의 역할과 필드가 먼저 고정되어야 하기 때문이다.
- 어떻게:
  - `docs/measured_data_structure.md`를 추가해 네 데이터 계층, 최소 필드, `error_*` 생성식, alignment 정책을 정리했다.
  - `docs/system_data_flow.md`에 measured data 구조 문서 참조와 processed dataset 기준을 반영했다.
  - `docs/vision_tracking.md`의 vision raw log 필드에 `run_id`와 `valid`를 추가해 measured data 구조와 맞췄다.
- 결과:
  - 초기 `error_x/y`는 vision 기반 XY에서, `error_z`는 angle-derived `measured_z_est`에서 processed merge 단계에 생성하는 구조가 문서화되었다.
  - raw log와 final CSV contract가 분리되었다.
- 다음:
  - processed merged dataset의 저장 경로와 파일명 규칙을 정한다.
  - 실제 `theta*_meas` 출처와 `estimator_method` 기록 규칙을 정한다.

## Entry 007
- 무엇을:
  - measured data 구조와 `error_*` 생성 정책을 `fulltext.md` 요약본에 반영했다.
- 왜:
  - 외부 AI 분석용 요약본이 2026-05-28 기준 SoT 변경사항을 반영하지 않으면 raw log와 final processed CSV contract, `error_*` 생성 시점을 잘못 이해할 수 있기 때문이다.
- 어떻게:
  - `fulltext.md`의 갱신일을 `2026-05-28`로 변경했다.
  - 데이터 계약 섹션에 real main log, vision raw log, angle-derived measured position, processed merged dataset 계층을 추가했다.
  - `error_x/y`는 vision 기반 XY에서, `error_z`는 `measured_z_est`에서 processed merge 단계에 생성한다고 정리했다.
  - 비전 raw log 최소 필드에 `run_id`, `valid`를 반영하고, 다음 작업 우선순위를 measured data 구조 후속 항목으로 갱신했다.
- 결과:
  - `fulltext.md`가 `docs/system_data_flow.md`, `docs/measured_data_structure.md`, `docs/vision_tracking.md`의 현재 기준과 맞춰졌다.
- 다음:
  - processed merged dataset의 저장 경로와 파일명 규칙을 정한다.
  - 실제 `theta*_meas` 출처와 `estimator_method` 기록 규칙을 정한다.

## Entry 008
- 무엇을:
  - 하드웨어 실험 절차 작성을 위한 스펙 수집용 템플릿 문서를 추가했다.
- 왜:
  - 실제 하드웨어 실험 절차를 확정하려면 모터, 드라이버, Arduino 제어기, USB 카메라, 로그 경로, 안전 조건 등 하드웨어 담당자가 채워야 할 정보가 먼저 필요하기 때문이다.
- 어떻게:
  - `docs/hardware_experiment_procedure.md`를 신규 작성했다.
  - 하드웨어 구성, Arduino 제어기, 비전 측정계, 데이터 로깅, 동기화, trajectory, pre-run checklist, run procedure, stop condition, post-run validation 섹션을 추가했다.
  - 미확정 스펙은 빈칸으로 두어 팀원이 실제 장비 기준으로 채울 수 있게 했다.
- 결과:
  - 하드웨어 실험 절차서를 만들기 위한 입력 템플릿이 생겼다.
- 다음:
  - 하드웨어/제어/비전 담당자가 빈칸을 채운 뒤, 실제 실행 가능한 실험 프로토콜로 정리한다.

## Entry 009
- 무엇을:
  - `docs/hardware_experiment_procedure.md`의 각 입력 항목에 한국어 작성 안내를 추가했다.
- 왜:
  - 하드웨어 담당자가 각 빈칸에 어떤 스펙, 설정값, 확인 결과를 넣어야 하는지 바로 알 수 있어야 하기 때문이다.
- 어떻게:
  - 각 항목의 콜론 뒤에 `(작성: ...)` 형식의 짧은 설명을 추가했다.
  - 실제 스펙 값은 입력하지 않고 템플릿 성격을 유지했다.
- 결과:
  - 하드웨어 구성, Arduino 제어기, 비전 측정계, 로그 구조, 동기화, trajectory, 체크리스트, 실행 절차, 중지 조건, 사후 검증 항목별 작성 기준이 문서에 들어갔다.
- 다음:
  - 팀원이 실제 장비 스펙과 설정값을 채운 뒤 실행 절차를 확정한다.

## Entry 010
- 무엇을:
  - 하드웨어 실험 문서를 고정 설정과 실험별 run protocol로 분리했다.
- 왜:
  - 실험 종류에 따라 바뀌는 항목은 scope, trajectory, correction on/off, validation이고, 하드웨어/제어/비전/로그 기본 설정은 대부분 공통이기 때문이다.
- 어떻게:
  - `docs/hardware_experiment_procedure.md`를 인덱스 문서로 정리했다.
  - 공통 설정은 `docs/hardware_experiment_base_config.md`로 분리했다.
  - 커미셔닝, baseline 데이터 수집, 보정 비교, 최종 시연을 각각 `docs/hardware_experiment_run_00_commissioning.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/hardware_experiment_run_02_corrected_comparison.md`, `docs/hardware_experiment_run_03_final_demo.md`로 분리했다.
  - `fulltext.md`에 하드웨어 실험 문서 구조를 요약 반영했다.
- 결과:
  - 고정값은 base config에서 한 번만 관리하고, 실험별 변동값은 각 run protocol에서 관리하는 구조가 생겼다.
- 다음:
  - 하드웨어/제어/비전 담당자가 base config의 빈칸을 먼저 채운다.
  - 이후 실험 목적에 맞춰 각 run protocol의 trajectory, correction 설정, validation 기준을 채운다.

## Entry 011
- 무엇을:
  - 하드웨어 실험 인덱스 문서명을 `docs/hardware_experiment_procedure.md`에서 `docs/hardware_experiment_index.md`로 변경했다.
- 왜:
  - 해당 문서는 실제 실험 절차서 본문이 아니라 base config와 run protocol 문서를 안내하는 인덱스 역할이기 때문이다.
- 어떻게:
  - 기존 인덱스 내용을 새 파일명 `docs/hardware_experiment_index.md`로 옮기고, 이전 `docs/hardware_experiment_procedure.md`는 제거했다.
  - `fulltext.md`의 문서 목록 참조를 새 파일명으로 갱신했다.
- 결과:
  - 하드웨어 실험 문서 구조에서 인덱스와 실험별 procedure/run protocol의 역할이 더 명확해졌다.
- 다음:
  - 팀원이 `docs/hardware_experiment_base_config.md`부터 채우고, 각 실험 목적에 맞는 `docs/hardware_experiment_run_*.md`를 이어서 채운다.
