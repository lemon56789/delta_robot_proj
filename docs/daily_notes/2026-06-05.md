# Daily Note - 2026-06-05

## Entry 001
- 무엇을: Run 00의 A/B gate 통과 상태를 문서에 기록했다.
- 왜: 조립 상태 소각도 joint check(A)와 Cartesian direction / parallel motion check(B)가 통과된 상태를 다음 C 단계 진행 전 gate 상태로 남겨야 하기 때문이다.
- 어떻게: `docs/hardware_experiment_run_00_commissioning.md`의 post-run validation에서 A/B를 사용자 보고 기준 pass로 표시하고, C와 Run 01 readiness는 아직 pending/no로 유지했다.
- 결과: Run 00은 A/B 통과, C 미완료 상태로 구분되었다. Run 01 baseline 데이터 수집 준비 완료로는 아직 보지 않는다.
- 변경 파일: `docs/plans/2026-06-05_001_record_run_00_ab_pass.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/daily_notes/2026-06-05.md`
- 다음: homography 적용 vision logger로 C 단계의 `vision_x/y` 방향 일치와 marker 검출 안정성을 확인한다.

## Entry 002
- 무엇을: 가상센싱 진행 전 남은 gate와 다음 작업 흐름을 보고용으로 정리했다.
- 왜: 현재 Run 00 A/B는 통과했지만 C와 Run 01 baseline 데이터 수집이 끝나지 않았으므로, 가상센싱 착수 전 필요한 조건을 명확히 해야 하기 때문이다.
- 현재 상태:
  - Run 00-A: pass
  - Run 00-B: pass
  - Run 00-C: pending
  - Run 01 baseline data collection: not started
- 가상센싱 전 최소 gate:
  1. Run 00-C 완료
  2. Run 01 baseline data collection 완료
  3. Run 01 main/vision/Simscape data post-alignment 및 merge 완료
  4. processed merged dataset validation 완료
  5. virtual sensor baseline model 학습/검증 시작
- Run 00-C에서 확인할 것:
  - homography 적용 vision logger 실행
  - `vision_x/y`가 `base_frame` 기준 mm 좌표로 기록되는지 확인
  - `+x` 이동에서 `vision_x` 증가 방향 확인
  - `+y` 이동에서 `vision_y` 증가 방향 확인
  - marker가 `1 s` 이상 사라지지 않는지 확인
  - main log와 vision log의 `run_id` 일치 확인
- Run 01에서 해야 할 것:
  - 보정 없이 baseline trajectory 실행
  - main log 저장
  - vision raw log 저장
  - Simscape output 준비
  - `theta*_meas` 또는 `theta*_meas_est` 기반 measured position 준비
  - main/vision/sim 데이터를 post-alignment
  - processed merged dataset 생성
  - `error_x/y/z` label 생성
- 결과: 가상센싱은 Run 01 실험 완료만으로 시작하지 않고, 학습 가능한 processed merged dataset이 생성되고 검증된 뒤 시작해야 한다고 정리했다.
- 변경 파일: `docs/plans/2026-06-05_002_record_virtual_sensing_prerequisites.md`, `docs/daily_notes/2026-06-05.md`
- 다음: Run 00-C를 완료한 뒤 Run 01 baseline data collection 문서와 실행 조건을 확정한다.

## Entry 003
- 무엇을: 6/8 발표 전 성공 기준과 날짜별 to-do list를 정리했다.
- 왜: 남은 일정이 공격적이므로 Run 02 향상 확인과 Run 03 demo를 필수 성공 기준으로 두면 발표 준비 리스크가 커지기 때문이다.
- 성공 기준:
  - Must-have:
    - Run 00-C 통과
    - Run 01 baseline dataset 확보
    - Run 01 main/vision/Simscape data alignment 및 processed merged dataset 생성
    - processed merged dataset validation 완료
    - virtual sensing 최소 모델 1개 학습
    - Run 01 baseline 또는 model prediction 결과 그래프 확보
  - Nice-to-have:
    - Run 02 corrected 실험에서 실제 향상 확인
    - Run 01 vs Run 02 정량 비교 그래프 확보
    - Run 03 end-effector demo 정상동작 확인
- To-do list:
  - 2026-06-05:
    - Run 00-C 완료
    - Run 01 문서 최소 작성
    - Run 01 trajectory 확정
    - Run 01 baseline 데이터 수집
    - Run 01 alignment/merge
    - processed merged dataset 생성 및 검증
    - virtual sensing 최소 baseline model 학습
  - 2026-06-06:
    - Run 02 문서 및 프리셋 작성
    - correction 적용
    - Run 02 corrected 데이터 수집
    - Run 01 vs Run 02 비교
    - 향상이 있으면 Run 03 준비
    - 향상이 없으면 모델 재시도는 최대 1회만 수행
    - 재시도 후에도 향상이 없으면 발표 스토리를 pipeline 구축, baseline 분석, 한계와 개선 계획 중심으로 전환
  - 2026-06-07:
    - platform 교체
    - Run 03 demo-only 문서 작성
    - Run 03 정상동작 확인
    - 발표용 영상/사진 확보
    - 결과 그래프 정리
    - PPT 작성
- 주의:
  - Run 03은 platform이 marker platform에서 end-effector platform으로 바뀌므로 demo-only로 분리한다.
  - Run 03 결과를 Run 01/02 정량 비교와 같은 조건으로 해석하지 않는다.
  - 2026-06-07에는 새 모델/새 분석보다 발표 자료 고정과 시연 안정화에 집중한다.
- 결과: 발표 전 필수 목표는 실제 하드웨어 데이터 기반 pipeline을 한 번 닫는 것으로 정리했고, Run 02 향상과 Run 03 demo는 가능하면 수행하는 확장 목표로 분리했다.
- 변경 파일: `docs/plans/2026-06-05_003_record_presentation_success_criteria_todo.md`, `docs/daily_notes/2026-06-05.md`
- 다음: Run 00-C 완료 후 Run 01 baseline dataset 확보를 최우선으로 진행한다.

## Entry 004
- 무엇을: Run 00-C vision readiness stopcheck 결과를 Run 00 문서에 반영했다.
- 왜: `vision_stopcheck_2026-06-04_run00_c_stopcheck_001.csv`가 실제 코드 기반 stop-and-check 이동 결과이며, marker detection, homography 적용 `vision_x/y`, `+x/+y` 방향 증가를 확인했기 때문이다.
- 어떻게: `docs/hardware_experiment_run_00_commissioning.md`의 C result를 pass로 기록하고, 근거 파일과 calibration 파일 경로를 남겼다. 파일/run_id 날짜가 2026-06-04로 남아 있지만 2026-06-05 검토 기준 Run 00-C 근거로 사용한다고 provenance note를 추가했다. main log run_id match는 Run 00-C stopcheck에서 확인되지 않았으므로 Run 01 조건으로 분리했다.
- 결과: Run 00은 A/B/C gate 통과 상태로 정리되었다. Run 01 vision-based baseline data collection으로 넘어갈 수 있지만, Run 01부터는 main log와 vision log를 같은 `run_id`로 저장하고 timestamp/required fields를 post-run validation에서 확인해야 한다.
- 변경 파일: `docs/plans/2026-06-05_004_record_run_00_c_stopcheck_pass.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/daily_notes/2026-06-05.md`
- 다음: Run 01 baseline data collection 문서의 run_id, trajectory, required logs, validation 조건을 실제 실행 가능한 수준으로 확정한다.

## Entry 005
- 무엇을: Run 01 baseline data collection 문서를 실제 실행 프로토콜로 구체화했다.
- 왜: Run 01은 단순한 동작 확인이 아니라 correction off baseline 데이터 수집, processed merged dataset 생성, virtual sensor 학습/검증/holdout split 확보까지 포함해야 하기 때문이다.
- 어떻게: `docs/hardware_experiment_run_01_baseline_data_collection.md`를 `Run 01-pre`, `Run 01-main`, `Run 01-holdout` 구조로 재작성했다. pre 단계는 static/cross `±10 mm`/square `±10 mm` pipeline validation으로 두고, main 단계는 static/cross `±20 mm`/square `±20 mm`/circle/grid 3x3을 3회 반복하는 학습용 baseline으로 정의했다. holdout 단계는 Run 02 comparison용 trajectory로 분리하고 학습에 넣지 않는 규칙을 명시했다.
- 결과: Run 01 실행 전 필요한 trajectory plan, required logs, run procedure, stop conditions, post-run validation, `error_x/y/z` 해석 제한이 문서에 고정되었다. Run 01-pre가 통과하지 않으면 Run 01-main으로 넘어가지 않는 gate도 명시되었다.
- 변경 파일: `docs/plans/2026-06-05_005_run_01_baseline_protocol.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: Run 01-pre용 실제 run_id와 logger/Simscape/merge 실행 커맨드를 확정하고, main/vision log가 같은 `run_id`로 생성되는지 확인한다.

## Entry 006
- 무엇을: Markdown 문서의 AGENTS.md 언어/작성 기준 일치 여부를 점검하고, 오늘 작성한 Run 01 관련 문서를 한국어 중심으로 정리했다.
- 왜: AGENTS.md와 기존 SoT 문서는 한국어 설명을 기본으로 하며, 직전 Run 01 문서에는 영어 설명 문장과 `yes/no` 판정 표기가 과하게 섞여 있었기 때문이다.
- 어떻게: 전체 Markdown에서 `(작성: ...)`, `TBD`, `yes/no`, 영어 절차 표현을 검색했다. 실행 근거가 있는 Run 01 문서와 관련 계획서는 한국어 중심으로 정리했고, CSV 필드명, 파일명, `run_id`, `train/validation/holdout` 같은 계약명은 유지했다. Run 02/03 문서의 `(작성: ...)` placeholder는 아직 실제 실행 근거가 없으므로 이번에 임의로 채우지 않았다.
- 결과: Run 01 문서는 한국어 중심의 실행 프로토콜로 정리되었고, 데이터 계약 의미는 유지되었다. Run 02/03 템플릿은 후속 실행 계획 수립 시 별도 승인 후 채워야 한다.
- 변경 파일: `docs/plans/2026-06-05_006_markdown_language_consistency.md`, `docs/plans/2026-06-05_005_run_01_baseline_protocol.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: Run 01-pre 실행에 필요한 실제 logger command와 저장 경로를 확인하고, 필요한 경우 `data/real/raw`, `data/real/derived`, `data/simulation/raw`, `data/processed`, `experiments/run_metadata` 디렉터리 생성 계획을 별도로 작성한다.

## Entry 007
- 무엇을: 컴퓨터 2(Y)에서 Arduino serial을 통해 Run 01-pre main log를 저장할 PC-side logger를 추가했다.
- 왜: Arduino는 파일을 직접 저장하지 않고 Serial Monitor에 사람이 읽는 echo만 출력하므로, Run 01-pre의 main log는 Arduino가 연결된 컴퓨터 2에서 Python logger가 serial command를 보내며 CSV로 저장해야 하기 때문이다.
- 어떻게: `experiments/run01_main_logger.py`를 추가했다. script는 `static_center_pre`, `cross_pm10_pre`, `square_pm10_pre`를 지원하고, Arduino에 `ALL theta1 theta2 theta3` command를 보낸다. 컴퓨터 2에 repo 전체가 없어도 쓸 수 있도록 필요한 IK 계산을 script 안에 포함했다. `--dry-run` 모드로 serial 없이 CSV와 metadata를 검증할 수 있게 했다.
- 결과: Run 01-pre에서 vision log는 컴퓨터 1(L), main log는 컴퓨터 2(Y)에서 같은 `run_id`로 생성하는 흐름이 가능해졌다. 현재 `theta*_meas`는 encoder 측정값이 아니라 `command_echo_no_encoder`이며, `time`은 Arduino `millis()`가 아니라 PC logger 기준 `pc_elapsed_ms`로 기록된다.
- 변경 파일: `docs/plans/2026-06-05_007_run01_pc_main_logger.md`, `experiments/run01_main_logger.py`, `experiments/README.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서 Arduino IDE Serial Monitor를 닫고 `pyserial` 설치 여부와 COM port를 확인한 뒤, `static_center_pre`부터 실제 main log를 생성한다.

## Entry 008
- 무엇을: 컴퓨터 2(Y)에 전달할 Run 01-pre main logger PowerShell 실행 안내 파일을 작성했다.
- 왜: 컴퓨터 2는 bash가 아니라 PowerShell 환경에서 Arduino IDE와 serial port를 사용하므로, 실행 명령과 주의사항을 PowerShell 기준으로 전달해야 하기 때문이다.
- 어떻게: `experiments/run01_main_logger_computer2_powershell.md`에 전달받을 파일, `pyserial` 설치, dry-run, `static_center_pre`, `cross_pm10_pre`, `square_pm10_pre` 실행 명령, 생성 파일 전달 위치, COM port/Serial Monitor 주의사항을 정리했다.
- 결과: Y는 repo 전체 없이 `run01_main_logger.py`와 PowerShell 안내 파일만 받아 Run 01-pre main CSV를 생성할 수 있다.
- 변경 파일: `docs/plans/2026-06-05_008_run01_main_logger_computer2_handoff.md`, `experiments/run01_main_logger_computer2_powershell.md`, `experiments/README.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서 실제 COM port를 확인한 뒤 `static_center_pre` main logger와 컴퓨터 1 vision logger를 같은 `run_id`로 동시에 실행한다.

## Entry 009
- 무엇을: Run 01 main log 보관 경로 `data/real/raw/`를 GitHub에 반영할 수 있게 추가했다.
- 왜: 컴퓨터 2(Y)에서 생성한 `main_<run_id>.csv`, metadata JSON, serial text log를 컴퓨터 1 repo에 모아야 하며, GitHub는 빈 디렉터리를 추적하지 않기 때문이다.
- 어떻게: `data/real/raw/.gitkeep`을 추가해 경로를 추적 가능하게 만들었다. 실제 main CSV는 아직 생성하지 않았다.
- 결과: Run 01-pre 실행 후 main log 파일을 `data/real/raw/` 아래에 복사해 보관할 수 있다.
- 변경 파일: `docs/plans/2026-06-05_009_add_real_raw_directory.md`, `data/real/raw/.gitkeep`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서 생성된 `main_<run_id>.csv`, `main_<run_id>.json`, `serial_<run_id>.txt`를 이 경로로 복사한 뒤 main/vision log 검증을 수행한다.

## Entry 010
- 무엇을: Run 01-pre cross/square trajectory 기준을 Arduino 쪽 실제 이동 경로인 `±40 mm`에 맞췄다.
- 왜: 기존 문서와 main logger는 `±10 mm` 기준이었지만, Arduino 쪽 cross 이동 경로가 `40 mm` 기준이면 main log의 `target_x/y`와 실제 vision 이동이 불일치하기 때문이다.
- 어떻게: `experiments/run01_main_logger.py`에 `cross_pm40_pre`, `square_pm40_pre`를 추가하고, 기존 `cross_pm10_pre`, `square_pm10_pre`는 호환용 alias로 남겼다. Run 01 문서와 컴퓨터 2 PowerShell 안내의 run_id, 출력 파일명, target range를 `pm40` 기준으로 갱신했다.
- 결과: static center는 기존 결과를 유지하고, Run 01-pre cross는 `2026-06-05_run01_pre_cross_pm40_r01` 기준으로 main/vision log를 생성해야 한다.
- 변경 파일: `docs/plans/2026-06-05_010_align_run01_pre_to_40mm.md`, `experiments/run01_main_logger.py`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 1 vision logger도 같은 `pm40` run_id를 쓰도록 맞춘 뒤 cross `±40 mm`를 실행한다. square 조건은 이후 Entry 011에서 `pm20` 기준으로 조정했다.

## Entry 011
- 무엇을: Run 01-pre에서 관측된 command 대비 실제 이동 부족과 Arduino/controller position gain `1.25` 적용 판단을 기록하고, 다음 square 실행을 `square_pm20_pre`로 조정했다.
- 왜: `40 mm` command에서 실제 vision 이동이 약 `30 mm`로 작게 나타났지만, 다른 거리에서는 같은 비율로 오차가 나지 않아 한 점 기준 `1.33` gain으로 정밀 보정하면 다른 구간을 악화시킬 수 있기 때문이다. square는 `±20, ±20 mm` 코너 위치로 진행하기로 했다.
- 어떻게: Run 01 문서에 `1.25` gain을 Run 01-pre용 coarse hardware compensation으로 정의하고, correction off 원칙과 metadata 기록 의무를 유지했다. `experiments/run01_main_logger.py`에 `square_pm20_pre`를 추가하고 컴퓨터 2 PowerShell 안내의 square 명령을 `pm20` 기준으로 바꿨다.
- 결과: 다음 square 실행 기준은 `2026-06-05_run01_pre_square_pm20_r01`이며, Arduino/controller gain `1.25` 적용 여부와 값은 run metadata 또는 별도 run note에 기록해야 한다.
- 변경 파일: `docs/plans/2026-06-05_011_record_run01_pre_gain_and_square_pm20.md`, `experiments/run01_main_logger.py`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서는 `square_pm20_pre` main logger를 실행하고, 컴퓨터 1 vision logger도 같은 `run_id`를 사용한다.

## Entry 012
- 무엇을: Run 01-pre square 실행 조건을 `square_pm40_pre`로 되돌리고, square corner 순서를 `home -> 3사분면 -> 4사분면 -> 2사분면 -> 1사분면 -> home`으로 수정했다.
- 왜: 실제 진행할 square가 `pm20`이 아니라 `pm40`이며, Arduino/vision 양쪽이 같은 target order와 run_id를 사용해야 main/vision alignment와 segment 해석이 맞기 때문이다.
- 어떻게: `experiments/run01_main_logger.py`에서 `square_pm20_pre` 선택지를 제거하고 `square_pm40_pre` point order를 `(-40,-40)`, `(40,-40)`, `(-40,40)`, `(40,40)` 순서로 바꿨다. Run 01 문서와 컴퓨터 2 PowerShell 안내도 `2026-06-05_run01_pre_square_pm40_r01` 기준으로 되돌렸다.
- 결과: 다음 square 실행 기준은 `2026-06-05_run01_pre_square_pm40_r01`이며, vision logger도 같은 run_id와 corner 순서를 사용해야 한다. Arduino/controller gain `1.25` 적용 여부와 값은 main/vision metadata 또는 별도 run note에 기록해야 한다.
- 변경 파일: `docs/plans/2026-06-05_012_restore_run01_pre_square_pm40_path.md`, `experiments/run01_main_logger.py`, `experiments/run01_main_logger_computer2_powershell.md`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서는 `square_pm40_pre` main logger를 실행하고, 컴퓨터 1 vision logger도 같은 `run_id`를 사용한다.

## Entry 013
- 무엇을: Run 01-pre `square_pm40_pre` 이동 순서를 최신 경로로 갱신했다.
- 왜: 실제 square 진행 경로가 `home -> (40,40) -> (-40,40) -> (-40,-40) -> (40,-40) -> (40,40) -> home`으로 변경되었고, main logger와 vision segment note가 같은 순서를 써야 하기 때문이다.
- 어떻게: `experiments/run01_main_logger.py`의 `square_pm40_pre` point order를 최신 순서로 변경하고, Run 01 문서의 target order와 vision handoff 설명을 갱신했다.
- 결과: 다음 square 실행 기준은 여전히 `2026-06-05_run01_pre_square_pm40_r01`이며, target order만 최신 경로로 바뀌었다.
- 변경 파일: `docs/plans/2026-06-05_013_update_square_pm40_latest_path.md`, `experiments/run01_main_logger.py`, `docs/hardware_experiment_run_01_baseline_data_collection.md`, `docs/daily_notes/2026-06-05.md`
- 다음: 컴퓨터 2에서는 `square_pm40_pre` main logger를 실행하고, 컴퓨터 1 vision logger도 같은 `run_id`와 최신 target order를 사용한다.
