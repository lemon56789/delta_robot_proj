# Daily Note - 2026-05-04

## Entry 001
- 무엇을:
  - `docs/plans/2026-05-04_001_workspace_sweep_validation.md` 계획서를 작성했다.
  - `kinematics/workspace_sweep.py`에 coarse grid 기준 workspace sweep 검증 스크립트를 추가했다.
  - `kinematics/README.md`에 workspace sweep 스크립트 설명을 반영했다.
- 왜:
  - sample point 몇 개만으로는 현재 IK/FK 구현이 작업공간 전체에서 얼마나 안정적으로 동작하는지 판단할 수 없기 때문이다.
  - fake pipeline이나 시각 시뮬레이션으로 넘어가기 전에 reachable 영역과 실패 분포를 먼저 코드 기준으로 확인해야 했기 때문이다.
- 어떻게:
  - `(x, y, z)` 범위를 step 간격으로 순회하면서 각 점마다 `IK`, `FK`, `IK -> FK` 왕복 오차를 평가하도록 구성했다.
  - 실패는 `ik_reject`, `fk_fail`, `roundtrip_error_exceeded`로 분류하고, 터미널 요약과 선택적 CSV 저장이 가능하도록 했다.
  - 기본값은 빠르게 돌려볼 수 있는 coarse grid로 두고, 추후 finer sweep으로 확장 가능하게 했다.
- 결과:
  - `kinematics/` 아래에 공간 전역 검증을 위한 첫 sweep 도구가 추가되었다.
  - 이후 workspace 경계, 대칭성, branch 선택 문제를 더 체계적으로 점검할 수 있는 기반이 생겼다.
- 변경 파일:
  - `docs/plans/2026-05-04_001_workspace_sweep_validation.md`
  - `kinematics/workspace_sweep.py`
  - `kinematics/README.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - 기본 coarse sweep를 실행해 valid/invalid 분포와 실패 사유 통계를 확인한다.
  - 필요하면 sweep 범위를 조정하거나 `z` slice별 finer scan을 추가한다.
  - fake pipeline 계획 전, sweep 결과를 바탕으로 안전한 trajectory 테스트 구간을 정한다.

## Entry 002
- 무엇을:
  - `docs/plans/2026-05-04_002_save_workspace_sweep_csv.md` 계획서를 작성했다.
  - 기본 coarse workspace sweep 결과를 `experiments/kinematics/workspace_sweep_default_2026-05-04.csv`로 저장했다.
- 왜:
  - 터미널 출력만으로는 진행 상태와 valid/invalid 분포를 나중에 다시 비교하거나 시각화하기 어렵기 때문이다.
  - 이후 finer sweep 결과와 baseline coarse 결과를 파일 단위로 비교할 기준이 필요했기 때문이다.
- 어떻게:
  - 현재 기본 sweep 설정 `x,y=-30..30 mm`, `z=-260..-180 mm`, step `15/15/20 mm`, round-trip tolerance `1e-3 mm`로 `workspace_sweep.py`를 실행했다.
  - 실행 시 `--output-csv experiments/kinematics/workspace_sweep_default_2026-05-04.csv` 옵션을 사용해 결과를 저장했다.
- 결과:
  - 기본 coarse sweep 결과가 CSV 파일로 저장되었다.
  - 이 파일을 기준으로 valid/invalid 분포, 실패 사유, round-trip error를 후속 분석에 사용할 수 있게 되었다.
- 변경 파일:
  - `docs/plans/2026-05-04_002_save_workspace_sweep_csv.md`
  - `experiments/kinematics/workspace_sweep_default_2026-05-04.csv`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - CSV 헤더와 row 수를 점검해 저장 포맷을 확인한다.
  - 필요하면 sweep 설정 메타데이터를 CSV 옆 설명 파일이나 별도 로그로 함께 남긴다.

## Entry 003
- 무엇을:
  - `docs/plans/2026-05-04_003_workspace_sweep_sidecar_json.md` 계획서를 작성했다.
  - `kinematics/workspace_sweep.py`에 sidecar JSON 저장 기능을 추가했다.
  - workspace sweep 결과와 같은 stem 이름의 JSON 메타데이터 파일을 생성하도록 변경했다.
- 왜:
  - CSV만으로는 sweep 범위, step, tolerance, 요약 통계를 파일 자체에서 바로 재현하기 어렵기 때문이다.
  - 이후 finer sweep 결과와 baseline coarse sweep 결과를 비교하려면 실행 설정과 통계가 함께 남아야 하기 때문이다.
- 어떻게:
  - `workspace_sweep.py`에서 summary dictionary를 만들고, CSV 저장 시 같은 stem의 `.json` 파일로 함께 기록하도록 구성했다.
  - JSON에는 sweep 범위, step, tolerance, 총 점 수, `ok/fail`, `reason_counts`, round-trip/FK 요약 지표, sample failure를 담도록 했다.
- 결과:
  - workspace sweep 결과를 `CSV + sidecar JSON` 한 쌍으로 보관할 수 있게 되었다.
  - 이후 CSV 파일만 보던 방식보다 재현성과 비교 가능성이 좋아졌다.
- 변경 파일:
  - `docs/plans/2026-05-04_003_workspace_sweep_sidecar_json.md`
  - `kinematics/workspace_sweep.py`
  - `kinematics/README.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - 기본 coarse sweep를 다시 실행해 JSON 파일 생성과 내용 일치를 확인한다.
  - `ik_reject` 경계 구간을 finer sweep로 다시 살펴볼 계획을 정한다.

## Entry 004
- 무엇을:
  - `docs/plans/2026-05-04_004_finer_workspace_sweep_near_z_minus_260.md` 계획서를 작성했다.
  - `z=-260 mm` 근처 finer workspace sweep를 실행하고 CSV/JSON 결과를 저장했다.
- 왜:
  - 기본 coarse sweep에서는 `ik_reject`가 전부 `z=-260 mm` 경계에서만 나타나, 실제 workspace 깊이 경계인지 규칙 문제인지 더 촘촘한 관찰이 필요했기 때문이다.
- 어떻게:
  - `x,y=-30..30 mm`, `z=-265..-255 mm`, step `5 mm`, round-trip tolerance `1e-3 mm`로 `workspace_sweep.py`를 실행했다.
  - 결과를 `experiments/kinematics/workspace_sweep_z260_band_2026-05-04.csv`와 같은 stem의 JSON으로 저장했다.
  - `z` slice별 `reason_counts`를 다시 집계해 `ik_reject` 분포를 확인했다.
- 결과:
  - 총 `507`점 중 `357`점이 `ok`, `150`점이 `ik_reject`였다.
  - `z=-265 mm`에서는 `ik_reject=103`, `ok=66`이었다.
  - `z=-260 mm`에서는 `ik_reject=41`, `ok=128`이었다.
  - `z=-255 mm`에서는 `ik_reject=6`, `ok=163`이었다.
  - 현재 데이터만 보면 `ik_reject`는 깊은 `z` 방향으로 갈수록 빠르게 증가하므로, 단순 구현 오류보다 workspace 깊이 경계 가능성이 더 커 보인다.
- 변경 파일:
  - `docs/plans/2026-05-04_004_finer_workspace_sweep_near_z_minus_260.md`
  - `experiments/kinematics/workspace_sweep_z260_band_2026-05-04.csv`
  - `experiments/kinematics/workspace_sweep_z260_band_2026-05-04.json`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - `ik_reject` 점들의 `theta` 범위와 branch 선택 조건을 함께 진단한다.
  - 필요하면 `z=-255..-250 mm` 또는 `z=-267..-263 mm`처럼 더 좁은 band를 finer step으로 다시 스캔한다.
  - fake pipeline 전에 trajectory safe zone을 `z` 깊이 기준으로 우선 제한할지 검토한다.

## Entry 005
- 무엇을:
  - `docs/plans/2026-05-04_005_diagnose_ik_reject_causes.md` 계획서를 작성했다.
  - `kinematics/inverse_kinematics.py`에 arm별 IK 실패 원인 진단 구조를 추가했다.
  - `kinematics/workspace_sweep.py`가 IK 상세 진단을 CSV/JSON에 기록하도록 확장했다.
  - 진단 모드 기준 `z=-260 mm` 근처 sweep 결과를 다시 저장했다.
- 왜:
  - 현재 `ik_reject`가 실제 workspace 경계인지, 각도 범위 제한 때문인지, branch 후보가 사라진 것인지 구분되지 않았기 때문이다.
  - 다음 단계 판단을 위해 reject를 arm별 원인으로 분해할 필요가 있었다.
- 어떻게:
  - IK에서 arm별로 `discriminant_negative`, `denominator_singular`, `angle_out_of_range`, `no_valid_branch`, `ok` 상태를 계산하도록 했다.
  - 기존 `delta_ik`의 성공/실패 동작은 유지하고, 별도 진단 함수로 상세 정보를 접근하게 했다.
  - workspace sweep가 각 점마다 arm1/2/3 상태와 전체 `ik_failure_summary`를 CSV/JSON에 남기도록 확장했다.
- 결과:
  - `ik_reject`를 단순 카운트가 아니라 arm별 상세 사유로 분석할 수 있는 경로가 생겼다.
  - 진단 sweep 결과, 현재 `ik_reject`는 전부 `angle_out_of_range`로 분류되었다.
  - `z=-265 mm`에서는 arm1 `39`, arm2 `33`, arm3 `33`건의 `angle_out_of_range`가 나타났다.
  - `z=-260 mm`에서는 arm1 `15`, arm2 `13`, arm3 `13`건의 `angle_out_of_range`가 나타났다.
  - `z=-255 mm`에서는 arm2 `3`, arm3 `3`건의 `angle_out_of_range`만 남았다.
- 변경 파일:
  - `docs/plans/2026-05-04_005_diagnose_ik_reject_causes.md`
  - `kinematics/inverse_kinematics.py`
  - `kinematics/workspace_sweep.py`
  - `kinematics/README.md`
  - `experiments/kinematics/workspace_sweep_z260_band_diagnosed_2026-05-04.csv`
  - `experiments/kinematics/workspace_sweep_z260_band_diagnosed_2026-05-04.json`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - 현재 `0 deg <= theta_i <= 90 deg` 제한이 실제 workspace를 과도하게 자르는지 검토한다.
  - 필요하면 angle range나 branch rule 해석을 별도 계획으로 검토한다.

## Entry 006
- 무엇을:
  - `docs/plans/2026-05-04_006_theta_min_sweep_for_workspace_boundary.md` 계획서를 작성했다.
  - `workspace_sweep.py`에 `--theta-min`, `--theta-max` 인자를 추가했다.
  - `theta_min = 0, -5, -10, -15 deg` 비교 sweep를 실행하고 결과를 저장했다.
- 왜:
  - `ik_reject`가 모두 `angle_out_of_range`로 분류된 만큼, 현재 `theta_min=0 deg` 제한이 nominal workspace를 실제보다 과도하게 자르고 있는지 직접 확인해야 했기 때문이다.
  - 모델링상 약간의 `+z` 방향 상향 자세가 가능해 보인다는 관찰을 수치적으로 검증할 필요가 있었다.
- 어떻게:
  - `workspace_sweep.py`가 angle range를 실행 인자로 받도록 확장했다.
  - `x,y=-30..30 mm`, `z=-265..-255 mm`, step `5 mm`, `theta_max=90 deg` 고정 조건에서 `theta_min = 0, -5, -10, -15 deg`를 비교했다.
  - 비교 결과를 `experiments/kinematics/theta_min_sweep_z260_band_2026-05-04.csv`와 같은 stem의 JSON으로 저장했다.
- 결과:
  - `theta_min=0 deg`에서는 총 `507`점 중 `ik_reject=150`, `ok=357`이었다.
  - `theta_min=-5 deg`에서는 `ik_reject=4`, `ok=503`으로 크게 개선되었다.
  - `theta_min=-10 deg`와 `-15 deg`에서는 `ik_reject=0`, `ok=507`로 전부 통과했다.
  - 현재 nominal geometry와 수식 기준에서는 `theta_min=0 deg` 제한이 workspace를 상당히 보수적으로 자르는 것으로 보인다.
- 변경 파일:
  - `docs/plans/2026-05-04_006_theta_min_sweep_for_workspace_boundary.md`
  - `kinematics/workspace_sweep.py`
  - `kinematics/README.md`
  - `experiments/kinematics/theta_min_sweep_z260_band_2026-05-04.csv`
  - `experiments/kinematics/theta_min_sweep_z260_band_2026-05-04.json`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - `theta_min=-5 deg` 또는 `-10 deg`가 하드웨어 관점에서도 허용 가능한지 기구 간섭/조립 제약 기준으로 검토한다.
  - 문서상의 provisional angle range를 유지할지, nominal analysis용 범위를 별도로 둘지 결정한다.

## Entry 007
- 무엇을:
  - `docs/plans/2026-05-04_007_record_nominal_vs_hardware_angle_ranges.md` 계획서를 작성했다.
  - `docs/system_data_flow.md`, `docs/ik_structure_note.md`, `fulltext.md`에 angle range를 `hardware-safe provisional`과 `nominal-analysis`로 구분해 기록했다.
- 왜:
  - `theta_min` sweep 결과상 nominal geometry 기준으로는 음수 각도 허용이 더 자연스러웠지만, 실제 하드웨어 허용 범위는 아직 확정되지 않았기 때문이다.
  - 분석용 범위와 하드웨어 확정 범위를 같은 문장으로 두면 이후 fake pipeline, trajectory test, 하드웨어 연동에서 혼선이 생길 수 있기 때문이다.
- 어떻게:
  - `docs/system_data_flow.md`의 IK contract에 `hardware-confirmed`, `hardware-safe provisional`, `nominal-analysis` 범위를 분리해 적었다.
  - `docs/ik_structure_note.md`에는 현재 selection rule이 하드웨어 보수 범위를 쓰되, nominal analysis 후보 범위를 별도로 둔다는 점을 반영했다.
  - `fulltext.md`에는 상태 요약 수준으로 두 범위의 구분만 추가했다.
- 결과:
  - 현재 문서 기준에서 `0..90 deg`는 hardware-safe provisional range, `-10..90 deg`는 nominal-analysis candidate range로 정리되었다.
  - 최종 hardware-confirmed range는 실제 조립/간섭/구동 제약 확인 후 확정해야 한다는 점이 분명해졌다.
- 변경 파일:
  - `docs/plans/2026-05-04_007_record_nominal_vs_hardware_angle_ranges.md`
  - `docs/system_data_flow.md`
  - `docs/ik_structure_note.md`
  - `fulltext.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - fake pipeline 범위를 계획서로 고정한다.
  - trajectory safe zone을 nominal-analysis range와 hardware-safe provisional range 중 어느 기준으로 잡을지 정한다.

## Entry 008
- 무엇을:
  - `docs/plans/2026-05-04_008_fake_pipeline_minimum_impl.md` 계획서를 작성했다.
  - `experiments/fake_pipeline.py`에 fake pipeline 최소 구현을 추가했다.
  - `experiments/README.md`와 `data/README.md`에 fake pipeline 결과 위치를 반영했다.
- 왜:
  - 현재 운동학과 CSV 계약이 정리된 상태에서, 실제 하드웨어 없이도 end-to-end 데이터 흐름이 실제 파일로 생성되는지 검증할 필요가 있었기 때문이다.
  - 이후 virtual sensor 입력 형식과 correction field 흐름을 실제 CSV 기준으로 점검할 준비가 필요했기 때문이다.
- 어떻게:
  - 안전한 trajectory 범위에서 deterministic target을 생성하고, IK로 `theta*_cmd`를 계산했다.
  - `theta*_meas`는 bias, lag, wobble을 섞은 deterministic fake measurement model로 생성했다.
  - `sim_x/y/z`는 command angle 기준 FK, `error_x/y/z`는 fake measured position과 simulation position 차이로 채우도록 구성했다.
  - 현재 고정 16개 CSV 컬럼 순서를 그대로 따르도록 writer를 구성했다.
- 결과:
  - Stage 6 fake pipeline의 첫 실행 스크립트가 추가되었다.
  - 실행 시 CSV와 sidecar JSON을 함께 생성해 trajectory, angle range, fake measurement model, error summary를 기록할 수 있게 되었다.
- 변경 파일:
  - `docs/plans/2026-05-04_008_fake_pipeline_minimum_impl.md`
  - `experiments/fake_pipeline.py`
  - `experiments/README.md`
  - `data/README.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - fake pipeline 스크립트를 실제 실행해 CSV와 JSON 생성, 컬럼 순서, 샘플 row를 검증한다.
  - 필요하면 trajectory 유형과 fake measurement model을 실험 목적에 맞게 분기한다.

## Entry 009
- 무엇을:
  - `docs/plans/2026-05-04_009_virtual_sensor_loader_and_shape_check.md` 계획서를 작성했다.
  - `virtual_sensor/dataset.py`에 fake pipeline CSV loader를 추가했다.
  - `virtual_sensor/check_dataset.py`에 CLI shape check 스크립트를 추가했다.
  - `virtual_sensor/README.md`에 현재 feature/target 정의를 반영했다.
- 왜:
  - 아직 virtual sensor 모델은 없지만, fake pipeline CSV가 실제 학습/추론 입력 형식으로 바로 이어질 수 있는지 먼저 확인해야 했기 때문이다.
  - 나중에 모델을 붙이기 전에 컬럼 존재 여부, float 파싱, shape, NaN 여부를 파일 기준으로 고정할 필요가 있었기 때문이다.
- 어떻게:
  - loader에서 현재 계약 기준 required columns를 검사하고, 모든 row를 float로 파싱하도록 구성했다.
  - 기본 feature는 `theta*_cmd`, `theta*_meas`, `sim_x/y/z`, target은 `error_x/y/z`로 정의했다.
  - CLI check 스크립트에서 row 수, feature/target shape, NaN 여부, feature/target column 통계를 출력하게 했다.
- 결과:
  - virtual sensor가 아직 구현되지 않았더라도 현재 fake pipeline CSV를 입력으로 받는 최소 데이터 경로가 추가되었다.
  - 이후 model code를 붙일 때 loader/shape 검증을 재사용할 수 있는 기반이 생겼다.
- 변경 파일:
  - `docs/plans/2026-05-04_009_virtual_sensor_loader_and_shape_check.md`
  - `virtual_sensor/dataset.py`
  - `virtual_sensor/check_dataset.py`
  - `virtual_sensor/README.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - shape check 스크립트를 실제 실행해 fake pipeline CSV가 문제없이 읽히는지 검증한다.
  - 필요하면 train/validation split이나 sequence windowing 구조를 추가한다.

## Entry 010
- 무엇을:
  - `python3 -m py_compile virtual_sensor/dataset.py virtual_sensor/check_dataset.py`로 구문 검증을 수행했다.
  - `python3 virtual_sensor/check_dataset.py`를 실행해 fake pipeline CSV의 row 수, feature/target shape, NaN 여부를 확인했다.
  - `docs/plans/2026-05-04_010_update_roadmap_and_fulltext_after_virtual_sensor_loader.md` 계획서를 작성했다.
  - `docs/roadmap.md`와 `fulltext.md`를 현재 구현 상태에 맞게 갱신했다.
- 왜:
  - virtual sensor 입력 경로가 실제 파일 기준으로 동작하는지 먼저 확인해야 했기 때문이다.
  - 현재 리포 상태는 FK, workspace sweep, fake pipeline, virtual sensor loader까지 진행되었는데 로드맵과 요약 문서가 이를 반영하지 못하고 있었기 때문이다.
- 어떻게:
  - fake pipeline 기본 CSV를 입력으로 shape check를 실행해 `180 x 9` feature, `180 x 3` target, `has_nan=False`를 확인했다.
  - `docs/roadmap.md`에는 Stage 3과 Stage 6을 `진행 중`으로 올리고, Stage 6/Stage 8의 current status를 추가했다.
  - `fulltext.md`에는 현재 구현 상태와 다음 작업 우선순위를 virtual sensor baseline 방향으로 다시 정리했다.
- 결과:
  - fake pipeline CSV가 virtual sensor loader를 통해 문제없이 읽히는 최소 데이터 경로가 검증되었다.
  - 로드맵과 요약 문서가 FK 최소 구현, workspace sweep, fake pipeline, virtual sensor loader 현황을 반영하게 되었다.
- 변경 파일:
  - `docs/plans/2026-05-04_010_update_roadmap_and_fulltext_after_virtual_sensor_loader.md`
  - `docs/roadmap.md`
  - `fulltext.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - `virtual_sensor/`에 baseline model 학습/추론 뼈대를 추가한다.
  - fake dataset 기준 train/validation split과 sequence windowing 구조를 정한다.

## Entry 011
- 무엇을:
  - 다음 우선순위 작업이 `Simulink/Simscape` 기반 simulation path 구현이라는 점을 Daily Note에 기록했다.
- 왜:
  - 현재 SoT 기준 primary simulation source가 `Simscape`로 정의되어 있기 때문이다.
  - virtual sensor 학습 전에 real/sim 비교에 직접 사용할 simulation data path를 먼저 갖추는 편이 우선순위상 더 자연스럽기 때문이다.
- 어떻게:
  - 오늘 작업 종료 메모로 다음 단계가 `Unity` 시각화보다 `Simulink/Simscape` input/output 및 logging path 정리 쪽이라는 점을 명시했다.
- 결과:
  - 다음 세션에서 Stage 4 실제 구현을 `Simulink/Simscape` 중심으로 바로 이어갈 기준이 Daily Note에 남았다.
- 변경 파일:
  - `docs/plans/2026-05-04_011_note_next_simulink_simscape_step.md`
  - `docs/daily_notes/2026-05-04.md`
- 다음:
  - `Simulink/Simscape` 입력 `target_x/y/z`, 출력 `sim_x/y/z`, sampling/logging 규격을 계획서로 구체화한다.
