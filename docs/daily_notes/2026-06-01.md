# Daily Note - 2026-06-01

## Entry 001
- 무엇을: `wB = 46.0 mm` geometry 기준 변경 계획서를 작성했다.
- 왜: 실제 하드웨어가 상부 암 충돌 문제를 피하기 위해 motor/upper-arm joint 기준 `wB = 46 mm`로 제작되어, 기존 nominal `wB = 24.051 mm` 기준과 Python/Simscape/문서/생성 데이터가 불일치하기 때문이다.
- 어떻게: 수정 대상 파일, 파생 데이터 재생성 대상, 영향 범위, 리스크, 검증 절차를 `docs/plans/2026-06-01_001_update_wb_geometry_to_46mm.md`에 정리했다.
- 결과: 아직 geometry 값 자체는 변경하지 않았고, 승인 후 적용할 계획만 생성했다.
- 다음: 사용자 승인 후 `kinematics/geometry.py`, Simscape parameter, SoT 문서, workspace/fake pipeline 파생 산출물을 순서대로 갱신한다.

## Entry 002
- 무엇을: 하드웨어 제작 기준에 맞춰 active geometry의 `wB`를 `46.0 mm`로 갱신하고, Python fake pipeline과 workspace 참고값을 재생성했다.
- 왜: 외곽 base/platform 변 길이와 실제 motor/upper-arm joint 기준점이 다르므로, IK/FK와 Simscape가 실제 하드웨어 기준 `wB`를 사용해야 하기 때문이다.
- 어떻게: Python nominal geometry, Simscape init parameter, SoT 문서의 `wB` 정의를 갱신했다. `experiments/fake_pipeline.py`에 출력 경로와 z 중심값 옵션을 추가하고 `data/fake_pipeline` CSV/JSON을 `wB = 46.0 mm` 기준으로 다시 생성했다. CSV 생성 시 `\n` line ending을 쓰도록 fake pipeline과 workspace sweep writer를 정리했다. `workspace_sweep.py`로 `wB = 46.0 mm` 기준 sweep artifact를 새 파일명으로 저장하고, `docs/workspace_envelope.md`의 면적/설치 높이 표를 재계산값으로 갱신했다.
- 결과: `python kinematics/validate_roundtrip.py`가 통과했고, `virtual_sensor/check_dataset.py`로 기본 fake pipeline, recomputed fake pipeline, positive-theta fake pipeline CSV 모두 shape/NaN 검증을 통과했다. Simscape comparison CSV는 MATLAB/Simscape 재실행이 필요하므로 old-geometry artifact로 남았다.
- 변경 파일: `kinematics/geometry.py`, `simulation/simulink/init_delta_params.m`, `kinematics/README.md`, `docs/system_data_flow.md`, `docs/ik_structure_note.md`, `docs/hardware_experiment_base_config.md`, `docs/roadmap.md`, `docs/workspace_envelope.md`, `fulltext.md`, `experiments/fake_pipeline.py`, `kinematics/workspace_sweep.py`, `data/fake_pipeline/fake_pipeline_sample_2026-05-04.csv`, `data/fake_pipeline/fake_pipeline_sample_2026-05-04.json`, `data/fake_pipeline/fake_pipeline_sample_2026-05-04_recomputed.csv`, `data/fake_pipeline/fake_pipeline_sample_2026-05-04_recomputed.json`, `data/fake_pipeline/fake_pipeline_sample_2026-05-25_positive_theta.csv`, `data/fake_pipeline/fake_pipeline_sample_2026-05-25_positive_theta.json`, `experiments/kinematics/workspace_sweep_default_wb46_2026-06-01.csv`, `experiments/kinematics/workspace_sweep_default_wb46_2026-06-01.json`, `docs/daily_notes/2026-06-01.md`
- 자동 생성 변경: Python 검증 실행으로 `kinematics/__pycache__/geometry.cpython-312.pyc`, `kinematics/__pycache__/inverse_kinematics.cpython-312.pyc`, `kinematics/__pycache__/forward_kinematics.cpython-312.pyc`가 갱신되었다.
- 다음: MATLAB/Simscape에서 `init_delta_params.m`를 다시 로드한 뒤 Simscape output CSV를 `wB = 46.0 mm` 기준으로 재생성하고 Python 기준과 재비교한다.

## Entry 003
- 무엇을: Simulink 테스트용 `wB = 46.0 mm` 기준 positive-theta CSV를 별도 파일로 생성했다.
- 왜: 기존 파일명과 구분되는 새 CSV로 Simulink 입력 테스트를 수행하기 위해서다.
- 어떻게: `python experiments/fake_pipeline.py --output-csv data/fake_pipeline/fake_pipeline_sample_2026-06-01_wb46_positive_theta.csv --output-json /tmp/fake_pipeline_sample_2026-06-01_wb46_positive_theta.json --z-center -332.0`를 실행했다.
- 결과: `data/fake_pipeline/fake_pipeline_sample_2026-06-01_wb46_positive_theta.csv`가 생성되었고, `virtual_sensor/check_dataset.py` 검증에서 `row_count=180`, `has_nan=False`, `theta*_cmd` 범위가 모두 양수로 확인되었다.
- 변경 파일: `data/fake_pipeline/fake_pipeline_sample_2026-06-01_wb46_positive_theta.csv`, `docs/daily_notes/2026-06-01.md`
- 다음: `simulation/simulink/load_fake_pipeline_theta.m`의 `csv_path`를 새 CSV 경로로 지정하고 Simscape 모델을 실행한다.

## Entry 004
- 무엇을: hardware base config의 외곽 치수 설명과 설치 높이 `H`를 보완했다.
- 왜: `sB=199 mm`, `sP=57.5 mm`는 하드웨어 외곽 변 길이이며 IK/FK 기준값이 아니고, 실제 설치 높이 `H=285 mm`를 Run 00 준비 기준에 반영해야 하기 때문이다.
- 어떻게: `docs/hardware_experiment_base_config.md`에서 `sB`, `sP`를 reference-only 외곽 치수로 명시하고, `installation height H [mm]`를 base origin/base center O에서 ground plane까지의 거리 `285 mm`로 기록했다.
- 결과: Run 00 준비 시 직접 사용하는 geometry 기준은 `L`, `l`, `wB`, `uP`, `H`로 분리되고, `sB/sP`는 외곽 치수 참고값으로 정리되었다.
- 변경 파일: `docs/hardware_experiment_base_config.md`, `docs/daily_notes/2026-06-01.md`
- 다음: `safe initial pose`, `homing/zeroing method`, `commissioning theta test range`, `interference risk zones`, `emergency power cutoff method`를 순서대로 확정한다.

## Entry 005
- 무엇을: Run 00 commissioning 준비를 위해 초기 자세, zeroing 기준, 초기 theta test range, no-go pose, emergency power 담당 조건을 base config에 기록했다.
- 왜: 실제 모터 구동 전에는 안전한 시작 자세와 좁은 범위의 단계적 단일축 테스트, 접근 금지 각도, 전원 차단 담당 조건을 먼저 확정해야 하기 때문이다.
- 어떻게: safe initial pose와 homing pose를 모두 `theta1=theta2=theta3=0 deg`로 두고, physical zero를 `theta_i=0 deg`로 정의했다. 초기 commissioning test는 단일축 `0 -> +5 -> 0 -> -5 -> 0 deg`에서 시작해 `+/-10 deg`, `+/-20 deg`로 확장하도록 기록했다. absolute no-go pose는 `theta_i <= -65 deg` 또는 `theta_i >= +90 deg`로 정했다. Run 00 motor-powered test는 Y가 power cutoff operator로 참석해야 한다고 명시했다.
- 결과: Y 부재 중에도 기계/절차 측면의 Run 00 초기 안전 조건은 문서화되었고, firmware command 단위, 속도/가속 수치, 물리 차단 방식은 Y 확인 필요 항목으로 남았다.
- 변경 파일: `docs/hardware_experiment_base_config.md`, `docs/daily_notes/2026-06-01.md`
- 다음: power supply/protection, Arduino command/log format, `theta*_meas` source, zero offset conversion, serial timeout/fault behavior를 Y와 확인해 채운다.

## Entry 006
- 무엇을: base config의 공통 중지 조건과 Run 00 vision 위치 확인 절차를 보완했다.
- 왜: Y 확인 전에도 관찰 가능한 위험 조건은 공통 stop condition으로 정리할 수 있고, Run 00에서 vision system이 marker 검출뿐 아니라 `base_frame` 기준 위치와 좌표 방향을 제대로 기록하는지 확인해야 하기 때문이다.
- 어떻게: abnormal vibration, link interference, marker lost, serial dropout, theta range exceeded, target range exceeded, operator judgment 조건을 관찰 기준 중심으로 작성했다. Run 00 vision check에는 calibration/homography 로드, 정지 상태 좌표 안정성, `+x/+y` 수동 이동 시 vision 좌표 방향 일치, marker loss 또는 좌표 방향 오류 시 run invalid 처리 조건을 추가했다.
- 결과: Run 00에서 모터 방향/zeroing뿐 아니라 vision 기반 `vision_x/y` 위치 확인을 함께 수행할 수 있는 절차가 문서화되었다.
- 변경 파일: `docs/hardware_experiment_base_config.md`, `docs/hardware_experiment_run_00_commissioning.md`, `docs/daily_notes/2026-06-01.md`
- 다음: power/Arduino 담당 Y와 함께 전원 보호, command/log format, serial timeout, `theta*_meas` source와 zero offset conversion을 확정한다.
