# Plan

## Goal
- `docs/system_data_flow.md`의 좌표축 표기를 대소문자 구분 없이 `x, y, z` 기준으로 통일한다.

## Files
- `docs/system_data_flow.md`
- `docs/daily_notes/2026-03-26.md`

## Changes
- `Units & Coordinate Frame` 섹션의 축 표기를 `+X`, `+Y`, `+Z`에서 `+x`, `+y`, `+z`로 변경한다.
- 관련 설명 문구의 축 표기 역시 일관되게 수정한다.

## Impact
- 좌표계 표기 규칙이 문서 전체에서 일관되게 유지된다.

## Risk
- 기존 수식/도면이 대문자 축 표기를 사용한다면 후속 문서 정합성 검토가 필요하다.

## Validation
- `docs/system_data_flow.md` 내 축 표기가 모두 `x, y, z`로 통일되었는지 확인
- Daily Note에 변경 이유와 결과가 기록되었는지 확인
